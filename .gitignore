# ── 采购 / 备料 / 台账（⛔ 不入库）────────────────────────
# 为什么不入库：
#   · 含【立创订单号】、单价、金额 —— 属于个人采购记录
#   · 会暴露其它项目的存在与 BOM 状态（如台灯项目）
#   · 备料清单相当于"个人库存"，没有公开价值
# ⚠️ 这两个文件在【初始提交】时被一起提交过（当时还没有这段 .gitignore），
#    所以它们曾经出现在公开仓库里 —— 教训：**初始化仓库要先写 .gitignore
#    再 git add，不要一把梭 add 全部**。
备料.md
电子元件已购台账.xlsx
*.xlsx

# 生成物 / 缓存（不入库）
__pycache__/
*.pyc
*.tmp
*.bak
*.fst
*.vvp
*.vcd
logs/

# Office / 系统临时文件
~$*
Thumbs.db
Desktop.ini
.DS_Store

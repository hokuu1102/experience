# FPGA 经验库（experience）

> **定位**：把每次排错的**过程**与**结论**沉淀成可复用资产。
> **约定**：按"文档类型"归档、编号接排、结论必须带状态标签、推翻旧结论要留痕。
> 本仓库 2026-09-10 本地初始化（远端自建）。

---

## 一、文档地图（按"你想查什么"来选）

| 想看什么 | 去哪 | 编号体系 | 当前规模 |
|---|---|---|---|
| 某次排错的**完整过程**（含失败路径） | `fpga/TROUBLESHOOTING.md` | `NN 号` | 21~77 号 |
| **结论式**逻辑/时序/设计经验 | `fpga/FPGA逻辑调试经验集.md` | `L##` | L01~L68（**L29 缺号**）|
| **工具/软件操作**经验（环境、命令、流程、构建产物核对） | `fpga/FPGA工具使用经验.md` | `U##` | U01~U56 |
| **环境配置 / 编译报错**类 | `fpga/FPGA问题排查手册.md` | 数字编号 | — |
| **跨语言 / 跨领域**共性教训 | `共性问题集.md` | `C##` | C01~C43 |
| 元器件资料与选型 | `器件积累.md` | `D##` | D01~D10 |
| ~~备料清单 / 已购台账~~ | ⛔ **不在本库** | — | 本地文件，已 gitignore（见 §二 第 6 条） |

---

## 二、更新约定（每次排查完问题后执行）

1. **过程** → `TROUBLESHOOTING.md`：现象 / 假设 / 验证 / **失败尝试** / 根因 / 回归结果，
   编号接排，顶部索引补行。**失败路径本身就是经验**，必须写。
2. **可复用结论** → 按类型分流：
   - 代码逻辑 / 时序 / 设计思路 → 逻辑集 `L##`
   - 工具 / 软件 / 操作流程 → 工具集 `U##`
   - 环境 / 编译报错 → `FPGA问题排查手册.md`
   - 能跨语言、跨领域迁移的 → 共性集 `C##`
3. **状态标签**（统一）：`[假设]` / `[验证中]` / `[已基本确认]` / `[已确认]` / `[已排除]`。
   **只有验证过的才能写进"最终方案"。**
4. **推翻旧结论必须当场改旧记录**（保留"错误 → 修正"痕迹，不删历史）。
5. **条目之间用编号互指**（如"见 L44 / U29 / C11"），不要复制粘贴正文。
6. **生成物与个人记录不入库**（见 `.gitignore`）：
   - 生成物 / 缓存：`*.fst / *.vvp / *.vcd / logs/ / __pycache__`
   - ⛔ **采购 / 备料 / 台账**（`备料.md`、`*.xlsx`）：含**立创订单号、单价金额**，
     还会暴露其它项目的存在与 BOM 状态 —— **本库可公开，这类不进来**。
7. ⭐ **本库只记"器件级通用知识"**（参数 / 坑 / 选型判据 / 同类对比）。
   **具体项目的选型落地与决策记录，写在各项目自己的仓库**，不写进本库。
   ⚠️ 具体表现：不要写"本项目选定 XX""我方采用 XX"，改写成
   "这种场景该选 XX"。**保留知识，去掉项目身份。**
8. ⚠️ **初始化新仓库时：先写 `.gitignore` 再 `git add`**，不要一把梭 `add -A`。
   （本库吃过一次亏：`备料.md` 与台账在初始提交时被一起提交并推到公开远端，
   事后只能 `git rm --cached` 摘出，**历史里仍然留着**。）

---

## 三、几处"血泪"索引（新问题先从这里找）

| 遇到这类问题 | 先看 |
|---|---|
| 采样/接口时序有裕量问题、相位怎么选 | **L38 / L39 / L44** |
| 数据里偶发坏值、坏点形态奇怪 | **L40 / L41 / L42**、`C08` |
| 想设计"一次否证多个假设"的实验 | **L45**、`C03` |
| 判断"问题在本地还是外部依赖" | **L33 / L34**、`C06` |
| 同一现象反复推翻自己 | **L35**、`C07` |
| 标称参数 vs 实测、配置漂移 | **L39**、`C05` / `C09` |
| 确认"板上跑的是哪版配置" | **U29**（ipc→_mod.v→.vg→时序报告） |
| 抓原始数据 / 结构诊断 | **U28**（`raw_dump` + 样本重复率 / 同翻位数） |
| 测量仪器用错、读数可疑 | **U30** |
| **示波器读数与设计值差整数倍 / 屏上乱抖 / 触发源怎么接** | ⭐ **U55**（触发源不必接 `EXT TRIG`；频率计只读**最内层重复周期**）、**L61**（判据要按**最小重复单元**算）、**L62**（时基装不下判据 / 光标随手卡 / 频率计只测触发源通道）、**T66** |
| **测试工具全绿，但工作区多出被污染的产物** | ⭐ **L63**（"会改文件再还原"的工具必须管住**它跑出来的产物**；收尾要再跑一次干净流程）、**T67** |
| PLL/IP 配置坑 | **L27 / L31 / L39**、**U22 / U23** |
| **低频段指标不好（窗口装不下周期）** | **T56 / L47**、`FPGA招新题/docs/低频段验收口径_2026-09-11.md` |
| **上位机显示时域/频谱；帧格式怎么演进** | **L50**（自描述帧）、**U32**（整机联仿）、`FPGA招新题/tools/live_scope.py` |
| **加宽寄存器/位宽后数字全错** | **L49**（double-dabble 两条铁律） |
| **PLL/IP 的「声明值」与实物不符、VCO 越规格** | **T57**、**U34**（拿数据手册边界指标反查前提） |
| **板子/目标环境不可用了，怎么继续验证** | **C13**（分层验证）、**U32**（端到端联仿 + 桩） |
| **改 `.bat` 或编码混杂的文件** | **U33 / U31** |
| **任何频率都报同一个值 / 题1 直通"对不上"** | **T59**、**U37**（双通道信号源核对）、**U38**（先抓一帧原始数据看全局量）、**U32** |
| **读数偶发/彻底冻住、参数自适应后自锁** | **T58**、**L51** |
| **某个功能单独坏（如点不开路径）/ 依赖只在 PATH 里找** | **U35**（看调用点是按 PATH 还是绝对路径） |
| **不想开 IDE 编译高云工程 / 校验编译的是哪个顶层** | **U36**（gw_sh + pin.html 核对引脚）、**U52**（完整流程 + 通过后必看四样） |
| **"零 error / 生成 bitstream 了"但心里没底：时序到底收没收敛** | ⭐ **U52**（不写 `.sdc` ⇒ 时序分析是**空的**，`TA1132`） |
| **工具报了个警告，要不要慌（尤其"和旧事故同签名"）** | ⭐ **C16**（严重性由**下游指标**裁决）、**U23**（`PR1014` 的判定三步与成因查法）、`TROUBLESHOOTING.md` 62 号 |
| **串行音频/总线的位对齐与格式定案** | **L53 / L54**、`TROUBLESHOOTING.md` 61 号 |
| **两份权威参考互相矛盾** | **C15**、**U51**（器件规格三条挖掘路径） |
| **要写"某某状态不许跳变/不许清零"这类断言** | ⭐ **L55**（写成**不变量**：增量必须等于期望值；给合法跳变留 `jump_ok` 出口；采样打一拍）、**L52**（先证明它会触发） |
| **配置文件写完程序起不来 / 脚本中文乱码 / 报"内容明明没问题"的语法错** | ⭐ **C17**（BOM 归属由**读取方**决定；写完回读首字节） |
| **配了"开机自启 + 崩溃重拉"，状态却显示 Ready 而进程还在跑** | ⭐ **C18**（分离式启动让自愈失效；用**进程树父链**判归属，先清干净再重启） |
| **跨层调用报"路径格式不正确 / 参数缺失"这类与现场不符的怪错** | ⭐ **C19**（把外壳语法**固化进脚本文件**，别在调用方拼字符串） |
| **命令"找得到"但一跑就失败、还不报错** | ⭐ **C20**（存在性 ≠ 可用性；**实跑一次**兜底，解释器路径做成可注入配置） |
| **程序对同一个错误码反应不对，且状态里"看不出发生过降级"** | ⭐ **C24**（别拿人类可读文案当解析锚点；同一码中英文文案不同 ⇒ 静默降级到兜底分支，范围/时长全错且**台账为空**） |
| **脚本批量重写表格/记录后，字段"串行"了（总数却全对）** | ⭐ **C25**（写入型 API 的 `None` 常是"**跳过**"不是"置空"：`openpyxl` 的 `cell(value=None)` 不清旧值 ⇒ 旧行数据静默串到新行；**行数/序号/总数全对**，只有**逐字段 diff** 才发现；改前先备份） |
| **批量导入订单/台账后，把"没成交的"记成了库存** | ⭐ **C26**（按**生命周期状态**过滤：`交易关闭` 行字段齐全、与成交行无差别；同一商品会同时有"关闭/成功"两笔 ⇒ **去重键必须含状态**；状态空值多为同单**续行**，不能一刀切；⚠️ **用户说"我没买过"是有效判据**） |
| **同一个"故障"反复修、每次修完都显示正常，过阵子又出现** | ⭐⭐ **C43**（**先证明故障真的存在，再修**：`openpyxl` 的 `freeze_panes` getter 返回的是 **`topLeftCell`（滚动位置）**，而决定冻结行数的是 XML 里的 **`ySplit`** ⇒ 读出 `A596` 看着像"冻住 596 行"，**实际三个版本 `ySplit` 恒为 1**，"冻结整表"**从未发生**；⚠️ 误诊被写进交接文档**代代继承**，且会掩盖真故障。⭐ 判据锚在**真正驱动行为的那一层**） |
| **要囤一批电阻/电容，不知道该买哪些值、各买多少** | ⭐ **D10**（按**用途分档**买，⛔ 不按 E24 全系列；电阻黄金六件套 `0Ω/100Ω/1K/4.7K/10K/100K`；**pF 档必须 C0G/NP0**，nF/uF 档 X5R≈X7R；⚠️ 介质别名 `C0G=NP0=NPO=COG` **是同一料**；0603 大容值有**耐压天花板**、Class 2 有 **DC Bias 缩水**） |
| **面向位图的输出（SVG→PNG / 图表 / PDF）里符号变"空框"，而编辑器预览正常** | ⭐ **C27**（**"有码位" ≠ "有字形"**：没有任何字体同时覆盖 emoji 与中文 ⇒ 中文字体把 emoji 画成 `□`，反之亦然；⭐ **判据要在最终产物上验**，⛔ 不是在编辑器预览里验）、**L64** |
| **排期/分工建立在"队友没动"上，一 `fetch` 发现前提是错的** | ⭐ **C28**（**关于外部状态的判断必须现查**：`fetch` 是**规划动作**不是收尾动作；并行线**取编号前先 fetch 或往大跳号**；⚠️ 本地一切自洽、**只有 fetch 才暴露**）、`TROUBLESHOOTING.md` 68 号 |
| **测试"全绿"里混着 `skipped`，却仍报 PASS** | ⭐⭐ **C29**（**"跳过"绝不能被算作"通过"**：框架只统计"没抓到" ⇒ `skipped`（`$null`）不计入失败 ⇒ **模块在无任何保护下被测出全绿**。⭐ **通过条件要写成白名单**（"全部 caught 才算过"），⛔ 不是黑名单）、**L65**、`C21` / `C23` |
| **命令明明成功，却被报 `[exit code: 1]`（PowerShell 调 git/docker/npm）** | ⭐ **C30**（**退出码不是唯一判据**：`git push` 把正常进度写 stderr ⇒ 当它是**最后一条语句且带 `2>&1`** 时，pwsh 退出码被置 1，**覆盖真实的 `$LASTEXITCODE=0`**；⚠️ 真假失败退出码**相同**。✅ 末尾补 `; $null` 或**不写 `2>&1`**；⭐ 判结果看**独立证据**如 `git rev-parse origin/main` vs `HEAD`）、`C11` / `C22` |
| **仿真/测试全绿，但换一个工具就"整个单元被丢弃"** | ⭐⭐ **C31**（**"能跑通" ≠ "能被工具接受"**：宽松前端会掩盖严格前端的拒绝；⚠️ 报错签名是 **`is ignored` / `was skipped` / `not included`** ⇒ 该单元**根本没进产物**；⭐ 判据要落在"**产物里有没有它**"上，如日志里必须有 **`Compiling module`**，⛔ 只有 `Analyzing` 不算）、`TROUBLESHOOTING.md` **70 号**、**L66** |
| **写 RTL 时哪些语法能用（尤其 `.sv` 文件）** | ⭐⭐ **L66**（`.sv` 扩展名 ≠ 按 SV 编译；⛔ 别用 `N'()` / `longint` / `logic` / `always_ff` / `typedef`；⚠️ Verilog-2001 **不能对 `integer` 参数做位选**） |
| **模块"编译全对、功能全废"（输出恒为初值）** | ⭐⭐ **L67**（`always @(*)` 里**不读任何信号**的组合块，仿真器**直接不执行**：警告 `found no sensitivities so it will never trigger`；⚠️ 综合器却会正常折叠 ⇒ **两个前端行为不一致**）、**C32**、`TROUBLESHOOTING.md` **71 号** |
| **回归/测试脚本跑很久没动静，怀疑是不是卡住了** | ⭐⭐ **TROUBLESHOOTING.md 72 号**（`& $vvp` **裸调用无超时** ⇒ 一个 tb 挂死 ⇒ **整套验收永久静默阻塞**，且**与"跑得慢"无法区分**；✅ 修法 = `Start-Job` + `Wait-Job -Timeout` + 超时**判失败**；⚠️ `Start-Job` 里**拿不到退出码**）、**C29** |
| **判据失败，不确定是被测有 bug 还是我的 tb 写错了** | ⭐⭐ **C33**（先怀疑"**我的时基/假设**"；⭐ 区分**电平 vs 脉冲/使能**语义——**宽度有信息**；判据要落在**稳态平台**上，⛔ 不是过渡过程）、`TROUBLESHOOTING.md` **73 号**、**C21** |
| **给脚本加了超时/改成后台跑之后，产物不见了（但结果还是"全绿"）** | ⭐⭐ **C34**（换执行方式会改变**工作目录**这个隐含前提 ⇒ **相对路径的产物静默丢失**，而工具**只警告不改退出码**；⭐ **"全绿" ≠ "证据还在"** ⇒ 验收脚本要**同时校验产物存在**；⚠️ 修完要跑"最脏"场景——**先删产物目录**）、`TROUBLESHOOTING.md` **74 号** |
| **变异体/用例报"漏检"，但判据看起来没问题** | ⭐⭐ **C35**（**先问"运行真的产生结论了吗"**：⭐ "没有结论"是**第三种状态**，而框架默认**当成成功** ⇒ 写死 `exit=0` / 空 `catch` / 默认成功 **都是把"未知"变成"通过"**；✅ 显式跨边界传递结论 + **取不到结论也判失败**）、`TROUBLESHOOTING.md` **75 号**、**C29** |
| **加了判据但变异体还是漏过（有盲区）** | ⭐⭐ **C36**（⭐ **探针必须打在"真正被改的那条线"上**——测上游等于没测；⭐ 判据写成**不变量**而非硬编码期望值；⭐ **测试数据要真的落在被观测的位/区间里**，否则两种实现输出相同、证明不了任何事；⚠️ 变异体本身先自检**是否真的改变语义**）、`TROUBLESHOOTING.md` **76 号**、**C23** |
| **工具报告里"有数"，但那数比手算小得多 / 要证明的东西不见了** | ⭐⭐ **C37**（⭐ **为自检方便写死的常量会把"要被证明的电路"优化掉**——综合器**合法地**删除恒不变的寄存器；⚠️ 而**仿真侧完全正常**；✅ 把输入引到顶层、⛔ 不写常量；⭐ **判据 = 报告的数要对得上手算值**）、`TROUBLESHOOTING.md` **77 号** |
| **参数化 RTL 里某段逻辑"功能全废"但语法全对** | ⭐⭐ **L67** / `TROUBLESHOOTING.md` **78 号**（`always @(*)` 块在参数取默认值时**退化成常量** ⇒ **没有敏感信号 ⇒ 仿真器从不执行**；⚠️ 综合器却正常折叠 ⇒ 两端不一致） |
| **"改文件再还原"的测试工具被中途杀死（超时 / Ctrl-C）** | ⭐⭐ **C38**（世界停在**半改**状态；⭐ 更糟的是后续判断把这个状态**当成基准**——"锚点找不到"报 SKIP、收尾的"干净回归"跑在**改过的代码**上；⚠️ `git status` 只显示 1 个文件改动、极易忽略；✅ 启动时自检"上次是否干净退出"并按**字节**恢复）、`TROUBLESHOOTING.md` **79 号** |
| **筛选后一个用例都没选中，却报"全部通过"** | ⭐ **TROUBLESHOOTING.md 80 号**（⭐ **"什么都没验证"被报成"全部通过"**——同族 `C29`/`C35` 的**第三次复发**；✅ **空选择直接判失败**；⚠️ 脚本顶层用 `return` ⇒ **退出码仍是 0**，要用 `exit N`） |
| **变异测试/校验"全绿"，但失败的原因不是你想验的那个** | ⭐⭐ **C39**（⭐ **"编译失败"被当成"判据抓到了"**——`exit != 0` 无法区分失败来源 ⇒ **一个判据都没跑过的变异体显示绿色**，让"全绿"可由编译错误刷出；✅ **给"无效"单列一个状态**，与"跳过"同级；⭐ 变异体的前置条件是"**它能编译**"）、`TROUBLESHOOTING.md` **81 号** |
| **"模块级自检通过"但一集成就炸（时序/接口/集成）** | ⭐⭐ **C40**（⭐ 自检工具**只检查了它检查的那几项**——本例"只跑综合、不跑 PnR"⇒ **根本不查时序**；⚠️ 限定写在注释里、没人读；✅ 新东西落地要跑**最完整的那条流程**；⭐ 定位时先看"**最差路径集中在哪个模块**"）、`TROUBLESHOOTING.md` **82 号** |
| **调外部黑盒的脚本"最后一步"挂死（无返回/无异常/无输出），而更简单的变体是成功的** | ⭐⭐ **C41**（⭐ **"简单变体跑通" ≠ "完整变体可用"**——记录"成功"必须**连同前置条件**；⭐⭐ 定位靠**把变量一个个摘掉做最小复现**（文本✅/图片❌ ⇒ 一句话判据）；⭐ 挂死要用**落盘日志**+**超时**（stdout 会被丢）；⭐ 隔离清楚后**算"绕开"的代价**，通常比重修黑盒便宜）、`fpga/FPGA工具使用经验.md` **U56**、`TROUBLESHOOTING.md` 72 号 |
| **"它在动"但数值不对 / 计数器·分频器·序号类输出** | ⭐⭐ **C42**（⭐ **"在动" ≠ "数值对"**——脉冲/状态看起来全对，**只有把数值数出来**才能发现自锁与恒定差 1；⭐⭐ **推进条件的门控里⛔ 不许放"由该状态算出的信号"**=自锁标准形态；⭐⭐ 测间隔**采样方式本身会造假抖动**；"极差=0"要按分频类型**分档**）、`fpga/FPGA逻辑调试经验集.md` **L68**、`TROUBLESHOOTING.md` **76/77 号** |
| **相位累加器 / 小数分频（DDS 式节拍）怎么写才对** | ⭐⭐ **L68**（⭐⭐ **`推进` 与 `出脉冲` 必须解耦**：推进条件里⛔ 不放进位；脉冲携带的数据必须**同拍锁存本拍的值**，⛔ 不是 `next`；⭐ `stop` 保相位、`reset` 清相位，判据写成**不变量**）、`TROUBLESHOOTING.md` **76/77 号** |

---

## 四、当前工程主线（截至 2026-09-11）

- 器件：高云 **GW5AT-LV60PG484AC2**（基石板）+ 微相 **ADA107**（3PA1030 ADC / 3PD5651E DAC）
- 教学模块：P0~P16 全过仿真；题1/2/3 已上板；题4/5 有量化墙（5mVpp = 0.51 LSB）
- **坏值根因已结案**（T53：采样相位落在 ADC 数据跳变窗口内 → 135° 修复，板上 A/B 实证）
- **阶段2a-2 已完成**（T56）：`raw_dump` 抽取回传（`S` 命令 + `#DEC` 头）→ 题2"时域 ≥2 周期"
  在 100Hz~2MHz 全段成立；上位机 `live_scope.py` 按 `#DEC` 自适应横轴
- ⚠️ **2026-09-11 起板子不可用 → 只能理论/仿真验证**：固件回归用 `ADDA/sim/run_sims.py`
  （快 ≈140s；`--long` 追加整机端到端 ≈450s），上位机链用 `tools/run_scope.bat --selftest`
- 未闭环：① 低频 Vpp(<50kHz)/频率(<20kHz)/频谱(<20kHz) 三条限制（修复路径见验收口径文档 §8）
  ② 假穿越"迟滞 v2 是否真修好低频"（需一帧 1kHz 原始数据，发 `R`）
  ③ **PLL 越规格运行**（声明 100MHz / 实物 50MHz → VCO≈350MHz < 下限 700MHz）：
     实测正常（80s / 99,894 行 0 异常），**已决策「现阶段不改、只修口径」**；
     有板后首选「只把 VCO 拉回规格、输出仍保持 25MHz」。见 T57 / U34、
     `PLL输入频率声明修正_利弊与计划_2026-09-11.md`
- **2026-09-10 结案**：采集"偶发坏值"根因 = 采样时钟 `clkout1` 相位落在 ADC 数据跳变窗口内
  → 相位 **270°→135°** 后异常样本 3.7%/2.7% → **0**（A/B 实测）
- 关联仓库：`C:\Users\hokuu\Desktop\FPGA`（`fpga-recruit-2026`）
  - 权威结论文档：`FPGA招新题/docs/坏值问题_结论与排查方法_2026-09-10.md`

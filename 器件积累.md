# 电子器件积累

> **用途**：记录用过的、值得记住的电子元器件。每条给出**功能、关键参数、应用场景、使用要点（踩坑）和同类对比**，方便以后选型时快速回忆。
>
> **约定**：每用一个新器件就追加一条，按类别归类，并在目录索引补行。
> **创建**：2026-08-31

---

## 目录索引

| 编号 | 器件 | 类别 | 一句话功能 | 资料完整度 |
|---|---|---|---|---|
| D01 | 74HC595 | 数字逻辑 / 接口扩展 | 8 位串入并出移位寄存器（带输出锁存），3 根线扩展 8 个 IO | ✅ 完整（含实战踩坑） |
| D02 | CH440R | 模拟开关 / 信号切换 | 4 通道 SPDT 低阻模拟开关（Ron~4 Ω，带宽 500 MHz+），主推 USB/高速差分切换 | ✅ 已按手册填实 |
| D03 | 高速 AD/DA 模块（ADA107 / AD9280+AD9708） | 数据转换 / 模拟前端 | FPGA 用高速 ADC/DAC 模组，用于信号采集与波形输出 | ✅ 已对比选型 |
| D04 | 磁轴 + 霍尔传感器 | 传感器 / 人机交互 | 非接触测**连续行程** → 力度 / 表情输入 | ✅ 选型完成（器件待定） |
| D05 | FSR 压阻式压力传感 | 传感器 | 压力→电阻的力敏薄膜；**某次评估后未采用** | ✅ 含干扰结论 |
| D06 | 多通道 SPI ADC（MCP3208 类） | 数据转换 / 模拟前端 | 8 通道 12 bit SPI ADC，做传感器阵列「**就地数字化**」 | ✅ 含方案对比与决策依据 |
| D07 | 霍尔开关（数字霍尔） | 传感器 | 内部自带比较器，**直接输出 0/1**；只用来拿「事件」 | ✅ 含四类区别与选型要点 |
| D08 | LED 驱动方案对比（WS2812 / I2C 驱动 IC / 移位寄存器） | 电源 / 驱动 | LED 指引的驱动方式选型；核心判据是**每通道能否独立 PWM** | ✅ 含对线性霍尔的干扰分析 |
| D09 | 串口屏（淘晶驰 TJC X2 类） | 显示 / 人机交互 | UART 组态屏：**不用写屏驱动**，FPGA 只发指令帧 | ✅ 含协议与难度评估 |

---

## 一、数字逻辑 / 接口扩展

### D01. 74HC595 — 8 位串入并出移位寄存器 ✅

**基本信息**

| 项 | 值 |
|---|---|
| 类型 | 8 位串行输入 / 并行输出移位寄存器 + 输出锁存器 |
| 系列 | 通用 74HC 逻辑系列（NXP / TI / ON / 国产兼容型号众多） |
| 封装 | DIP-16 / SOIC-16 / TSSOP-16 |
| 供电 | 2 ~ 6 V |
| 引脚 | SER(14) 串行数据 / SRCLK(11) 移位时钟 / RCLK(12) 锁存时钟 / OE(13) 输出使能 / SRCLR(10) 移位寄存器清零 / QH'(9) 级联输出 / QA~QH 并行输出 |

**核心功能**

- **串入并出**：只用 3 根控制线（SER + SRCLK + RCLK）换出 8 个并行输出
- **两级结构**（关键优点）：
  - 第一级**移位寄存器**：SRCLK 每个上升沿把 SER 的数据往里推一位
  - 第二级**输出锁存**：RCLK 上升沿才把移位寄存器的内容一次性打到输出
  - ⇒ 串行移位过程中**输出完全不变**，不会像 164 那样产生"拖影"
- **级联**：QH' 接下一片的 SER，N 片级联得到 8N 个输出，**控制线仍然只有 3 根**
- **三态输出**：OE 拉高时输出高阻，可实现多路复用总线

**关键参数（典型值 @5 V，具体查 datasheet）**

| 参数 | 典型值 |
|---|---|
| 移位时钟频率 | 最高约 25 ~ 30 MHz（电压越高越快） |
| 每脚输出电流 | ±20 ~ 35 mA |
| **总电流上限** | 约 70 mA（VCC 或 GND 引脚合计，务必核对） |
| 传播延迟 | 十几 ns 级 |

**应用场景**

1. **数码管 / LED 点阵驱动**（最经典）—— 段选 + 位选各用一片或级联
2. **MCU / FPGA 的 IO 口扩展** —— 用 3 个 IO 换 8 个（甚至 16、24 个）
3. **继电器 / 电磁阀阵列控制** —— 配合 ULN2803 等驱动
4. **多路 LED 指示灯 / 跑马灯**
5. **减少长距离走线** —— 3 根线代替 8 根，适合板间连接

**使用要点（实战踩坑）**

1. **★ 级联时 QH' 必须是"组合输出"**
   如果写 Verilog 行为模型来仿真 595，**`qh_o` 必须写成 `assign qh_o = diff[7];`，绝不能写成寄存器**（`qh_o <= diff[7]`）。
   写成寄存器会导致下一级采样到"上一次"移位的值 → **每级联一级丢 1 bit**，表现为段码整体错位。
   （真实器件的 QH' 是移位寄存器最后一级的直接引出，靠传播延迟保证下一级采到"移位前"的值。）

2. **SRCLK 和 RCLK 必须分开控制**
   先把一帧数据全部移完（N 个 SRCLK 脉冲），**再给 RCLK 一个正脉冲**统一锁存。
   常见错误是移位和锁存混着来，导致输出闪烁。

3. **输出电流要算总账**
   每脚 20 mA 不代表 8 脚能同时输出 160 mA —— VCC/GND 引脚有总电流上限（约 70 mA）。
   驱动多个 LED 时：**限流电阻要够大**，或改用 TPIC6B595（功率版）/ 加三极管、ULN2803。

4. **级联的数据顺序**
   先移入的数据最终会"沉"到链路末端。多片级联时，软件要先发**最后一片**的数据。
   这个顺序搞反是最常见的数据错位原因。

5. **VCC 旁必须加 0.1 µF 去耦电容**，尤其是级联多片或时钟频率高时。

6. **FPGA 驱动时注意时序余量**
   RCLK 脉宽、SRCLK 频率、dio 对 SRCLK 的建立/保持时间都要留余量。
   本次项目实测：SRCLK 12.5 MHz、RCLK 脉宽 40 ns 可以工作，但余量不大。

**同类对比（选型参考）**

| 型号 | 差异 | 什么时候选它 |
|---|---|---|
| **74HC164** | **无输出锁存** | 移位时输出会"逐位推进"产生拖影，**不适合驱动数码管**；只在对输出毛刺不敏感时用 |
| **74HC165** | **并入串出**（反方向） | 扩展**输入**口（读按键、拨码开关） |
| **TPIC6B595** | 功率版，灌电流大（150 mA 级），带过压保护 | 直接驱动 LED 数码管、继电器，省掉外部驱动 |
| **74HC4094** | 也是串入并出带锁存 | 时序与 595 略有差异（有个 STR 和数据有效窗口），替换时要注意 |
| **CH423 / TM1637 / MAX7219** | 专用数码管/LED 驱动（自带扫描 + 译码） | 想省掉 MCU 的扫描开销时，比"595 + 软件扫描"更省事 |

---

## 二、模拟开关 / 信号切换

### D02. CH440R — 4 通道 SPDT 低阻模拟开关 ✅

> 资料来源：`C:\Users\hokuu\Downloads\CH440R.pdf`（沁恒 CH44x 系列手册）
> 以下参数均按手册填实（5 V 供电、TA = 25 °C 条件下的典型值 / 最大值）。

**基本信息**

| 项 | 值 |
|---|---|
| 型号 | **CH440R**（同系列：CH440G / CH440P，仅封装不同） |
| 类型 | **4 通道 SPDT 模拟开关**（4 个独立二选一开关，即 4 × 2:1 MUX） |
| 厂商 | 沁恒 WCH |
| 封装 | **TSSOP-16**（宽 4.4 mm / 173 mil，脚距 0.65 mm / 25 mil） |
| 供电 VCC | **4.0 ~ 5.5 V**（典型 5.0 V） |
| 控制脚 | `IN`（A/B 选择）、`EN#`（使能，低有效） |

**引脚结构（16 脚）**

| 引脚 | 说明 |
|---|---|
| S1A/S1B、S2A/S2B、S3A/S3B、S4A/S4B | 4 组开关各自的 2 个信号端（共 8 脚） |
| DA / DB / DC / DD | 4 组的公共端（共 4 脚） |
| `IN` | 选择：**0 → 接通 A 组（SxA），1 → 接通 B 组（SxB）** |
| `EN#` | 使能，**低电平有效**；拉高则 4 组全部关断 |
| VCC / GND | 电源（16 脚 VCC / 8 脚 GND，具体见手册引脚图） |

**核心功能**

- 用 **1 路数字信号（`IN`）同时切换 4 组**二选一通路（4 组同步动作）
- **双向**：信号可从任一端流向另一端（不像逻辑门有方向性）
- **低导通电阻**（典型 4 Ω），信号衰减小 —— 相对 CD4066 这类通用模拟开关的核心优势
- **高速**：带宽数百 MHz、开关时间纳秒级，**手册主推用于 USB / 高速差分信号切换**

**关键参数（5 V 供电，TA = 25 °C）**

| 参数 | 典型值 | 最大值 | 说明 |
|---|---|---|---|
| **Ron @ VCOM = 0 V** | 4 Ω | 6 Ω | 导通电阻（信号电压 0 V 时） |
| **Ron @ VCOM = 2 V** | 5 Ω | 8 Ω | |
| **Ron @ VCOM = 3.4 V** | 11 Ω | 16 Ω | Ron 随信号电压升高而变大（平坦度） |
| **Ron @ VCOM = 5 V** | 8 Ω | 12 Ω | |
| **BW（-3 dB 带宽）** | 500 ~ 550 MHz | — | 高速差分信号的保障 |
| **Ton / Toff** | 3 ns | 7 ns | 手册首页标称 **< 5 ns** |
| CON（导通电容） | 9 pF | 14 pF | @ 1 MHz |
| COFF（关断电容） | 6 pF | 10 pF | @ 1 MHz |
| CIN（控制脚电容） | 3 pF | 6 pF | @ 1 MHz |
| **信号幅值范围** | **-0.3 ~ VCC + 0.3 V** | — | **超出会削波 / 闩锁** |
| VIH（高电平输入） | 2.0 V | VCC | 控制脚电平门限 |
| ICC（静态电流） | 0.1 µA | 10 µA | 极低功耗 |
| **ESD** | **2 kV HBM** | — | 接口处仍建议加 TVS |

**应用场景**（手册明确提到 USB 与高速差分）

1. **USB 2.0 差分信号切换**（主推）—— 4 对差分线在两路主机/设备间切换（如 USB 共享切换器、KVM）
2. **高速差分信号路由** —— LVDS、MIPI 辅助通道、HDMI 低速通道等
3. **音频 / 视频信号 A/B 源切换** —— 4 路立体声或视频在两组信源间切换
4. **通信总线冗余切换** —— UART / SPI / I2C 在主备两路间切换
5. **测试设备（ATE）信号源选择** —— 被测件在两个激励源之间切换
6. **多传感器 / 多摄像头切换** —— 4 路模拟视频或传感器信号选通

**使用要点（踩坑）**

1. **★ 信号幅值绝不能超 `-0.3 V ~ VCC + 0.3 V`**
   手册明文限制。要过**负电压**或**超过 VCC** 的信号，必须选双电源 / 轨到轨型号，否则削波、失真，严重时闩锁（latch-up）损坏芯片。

2. **Ron 会随信号电压变化（4 Ω → 11 Ω）**
   这是手册里最值得注意的一点：**导通电阻不是恒定的**，随信号电平从 4 Ω 变到 11 Ω（平坦度）。
   意味着不同电平下的衰减不一致，会引入**非线性失真**。音频 / 精密测量场合要留意，后级用高输入阻抗缓冲。

3. **4 组同步切换，无法独立控制**
   `IN` 一个信号同时控制 4 组。若需要各组独立选择，要用多片或换型号。

4. **切换后要等稳定再采样**
   电荷注入 + 3~7 ns 建立时间。ADC 采样前留出足够建立时间（开关时间 + 后级运放建立时间）。

5. **`EN#` 不要悬空**
   低有效使能脚，悬空会导致状态不定或全部关断。不用时按需求上拉（关断）或主动驱动。

6. **未使用的信号端不要悬空**
   悬空会拾取噪声并耦合到导通通道，建议接固定电平（GND 或 VREF）。

7. **ESD 仅 2 kV HBM，接口处要加 TVS**
   模拟开关输入端通常直接对外（USB 座、探头），是最容易被打坏的地方。手册标称 2 kV HBM，实际建议外加 TVS 管。

8. **高频走线注意寄生电容与阻抗**
   CON 9~14 pF、COFF 6~10 pF。高速应用要做阻抗匹配，差分对尽量等长、少过孔。

**同系列选型（沁恒 CH44x 家族）**

| 型号 | 通道结构 | 封装 | 供电 |
|---|---|---|---|
| **CH448F** | 16:1 MUX | QFN24 | 5 V |
| **CH444G / CH444P** | 4:1 MUX | SOP16 / QFN16 | 5 V |
| **CH445P** | 4 × SPDT（3.3 V 优化版） | QFN16 | 3.3 V |
| **CH440G / CH440P / CH440R（本条）** | **4 × SPDT（2:1）** | SOP16 / QFN16 / **TSSOP16** | 5 V |
| **CH442E** | 2 × DPDT | MSOP10 | 5 V |
| **CH443K** | 1 × SPDT | SC70-6L | 5 V |

> 该系列还有 3.3 V / 2.5 V / 1.8 V 供电的低电压版本（手册标注如 CH445P 为 3.3 V 优化），
> 选型时先看**供电电压**再定型号，不要只看通道数。

**同类对比（跨厂商）**

| 型号 | 特点 | 什么时候选它 |
|---|---|---|
| **CD4066** | 4 路 SPST，Ron 大（~100 Ω+），便宜 | 只切数字信号、对衰减不敏感、成本优先 |
| **74HC4051 / 4052 / 4053** | 8 选 1 / 双 4 选 1 / 三 2 选 1，Ron ~几十 Ω | 经典多路复用，性价比高 |
| **CH440R（本条）** | 4 × SPDT，Ron ~4 Ω，带宽 500 MHz+ | **USB / 高速差分切换**，要低 Ron + 高带宽 |
| **CH446（沁恒）** | 16 × 8 模拟开关阵列 | 任意点对点矩阵切换（比多个 MUX 灵活） |
| **ADG 系列（ADI）** | Ron 极低、带宽高、电荷注入小 | 高精度 / 高频 / 测试仪器，价格高 |
| **TMUX / SN74CB3T（TI）** | 总线开关，低 Ron、支持电平转换 | 数字总线切换 / 电平转换场景 |

---

## 三、数据转换 / 模拟前端

### D03. 高速 AD/DA 模块 —— ADA107 vs 8 位 AD9280+AD9708 ✅

> 选型背景：2026 年 FPGA 招新选拔赛 ADC/DAC 信号处理方向，输入信号 100mVpp~3Vpp、最高 2MHz，要求测 Vpp 并 DAC 回放。

**基本信息**

| 模块 | 核心芯片 | ADC | DAC | 模拟量程 | 连接器 | 参考价格 |
|---|---|---|---|---|---|---|
| **微相 ADA107** | 3PA1030 + 3PD5651E | **10 位 / 50 MSPS** | **10 位 / 125 MSPS** | ±5 V / ±4.3 V | 40pin 排针 | ~90 元 |
| 引力边界 3PA9280/3PD9708 | 3PA9280 + 3PD9708 | 8 位 / 32 MSPS | 8 位 / 125 MSPS | ±5 V | 排母 IDC | ~80 元 |
| 正点原子 ATK-HS-ADDA | AD9280 + AD9708 | 8 位 / 32 MSPS | 8 位 / 125 MSPS | ±4.5 V | 2×20pin 排母 | ~130 元 |

**核心功能**

- 同步完成**模拟信号采集**（ADC）和**模拟波形输出**（DAC）。
- 配合 FPGA 可做 DDS 信号源、简易频谱分析、信号调理实验。

**关键参数（决定选型胜负）**

按 ±5 V 满量程计算：

| 位宽 | LSB | 100mVpp 对应 LSB 数 | 5mVpp 对应 LSB 数 |
|---|---|---|---|
| 8 位 | ~39 mV | **~2.6 LSB** | **< 0.13 LSB** |
| 10 位 | ~9.77 mV | **~10.2 LSB** | **~0.51 LSB** |

- 8 位模块的 LSB（39 mV）已经大于基本部分 100mVpp 的容限（±10 mV），测量会抖动。
- 10 位模块在 100mVpp 上有约 10 个码，基本部分更稳。
- 提高部分 5mVpp **两款都测不了**，必须额外加 PGA / AGC。

**应用场景**

1. FPGA 竞赛中的正弦波采集 + DAC 回放
2. DDS 任意波形发生
3. 峰峰值 / 频率测量前端
4. 简易频谱仪、扫频仪

**使用要点（踩坑）**

1. **不要只看板子元件多寡**。
   第二个模块上电容、电感、按钮多，大部分是**电源去耦、输出重建滤波、参考源缓冲**。它们能让信号更干净，但**不能把 8 位 ADC 变成 10 位**。有效分辨率由 ADC/DAC 芯片本身决定。

2. **8 位测 100mVpp 必须用算法补救**。
   不能直接 `vpp = max - min`，要用：
   - 正弦拟合（最小二乘求幅度）
   - 多周期平均
   - 少量高频 dithering + 平均
   否则 Vpp 误差会远超 ±10 mV。

3. **两款模块都需要自己画转接板**。
   引脚定义不同，不能直接把为某一款画的板子换到另一款上。

4. **ADC_OE 必须拉低**。
   ADA107 的 `ADC_OE` 高电平 = ADC 输出高阻。转接板上直接接地最稳妥，不要悬空。

5. **时钟和电源走线比元件数量更重要**。
   - `ADC_CLK`、`DAC_CLK` 走最短路径，两侧包地。
   - 5 V / 3.3 V 电源在转接板上多打过孔、铺铜，地线阻抗要低。

6. **提高部分必须上 PGA**。
   无论买哪款，想测 5mVpp 都要在前端加程控增益放大器（把 5mVpp 放大到几百 mVpp 再进 ADC）。

**同类对比 / 选型建议**

| 你的情况 | 推荐选择 |
|---|---|
| 想最快跑通 16_AD_DA_TEST 例程，不在乎分辨率 | 正点原子 ATK-HS-ADDA（资料最多，代码几乎不改） |
| **基本部分求稳、性价比最高** | **微相 ADA107（10 位、50MSPS、90 元）** |
| 只看价格，100mVpp 精度可以接受算法补救 | 引力边界 8 位模块 |
| 想冲提高部分（5mVpp） | 任一款 + 外接 PGA/AGC |

> **本次结论**：引力边界 8 位模块只比 ADA107 便宜 10 元，但分辨率差一档，不建议买。

### ⚠️ ADA107 使用踩坑补充（2026-09-10）

**1. 数据位线的"手工飞线"会变成间歇性错误位 → 必须做信号完整性**
- 背景：ADA107 的 `ADC_D5` 原走线连到 FPGA 一个 CPU 专用 ball（不可作普通 IO）
  → 只能**手工飞线**到旁边的测试点 P17
- 后果：该位长期**间歇性翻转**（窗口 max/min 出现**固定的 32 LSB 偏移**，即 bit5 权重）
  → 表现为峰峰值 vpp 在两簇之间跳、示波器上图"毛刺"很多，而 period/FFT 完全正常
- **教训**：**手工改过的线，优先怀疑那一位**。可用"掩码该位"（强制置 0）做 A/B 快速确认
- **飞线补救顺序**：缩短 → **与 GND 绞合成双绞（两端接地）** → 源端串 33Ω（仅振铃时）→ 换成有正规走线的空闲 IO
- ⚠️ **对"边沿迟滞"型错误，串电阻会让边沿更慢、反而加重**；33Ω 只在确见振铃/反射时才加

**2. 模块"插不到位"是静默故障**
- 排针没插到底 / 插错位 → 现象可以是**完全无输出**或**全通道乱码**，而 RTL 一点问题都没有
- 先重插、再查线、最后才怀疑代码（详见问题排查手册 #21）

**3. 供电与量程**
- 实测 5V=4.9V、3.3V=3.2V（正常）
- 板载 1/5 调理 + 10bit ⇒ 满量程约 ±5V / **≈9.8mV per LSB**
  → **100mVpp ≈ 10 LSB（勉强）**，5mVpp 必须外接 PGA/AGC（题4 小信号段的硬墙）

---

### D06. 多通道 SPI ADC（MCP3208 类）—— 传感器阵列「**就地数字化**」

> **结论**：**多通道传感器阵列的采集，优先选「就地数字化」** ——
> **每个传感器板上放一片多通道 SPI ADC，把模拟量在板上就地数字化，长线只走 SPI 数字。**
> 备选方案（外置多路复用器 + FPGA 片内 ADC）的对比与落选理由见下方「★ 决策记录」。
>
> 📌 **本文只记「器件与架构层面」的通用结论。**
> 具体项目里的选型落地（选哪个型号、通道数、接口分配）**记在各自项目仓库**，
> 不写进本库 —— 本库是对外可公开的通用知识。

**基本信息**

| 项 | 值 |
|---|---|
| 类型 | **多通道逐次逼近型（SAR）ADC**，SPI 串行接口 |
| 主选型号 | **MCP3208**（8 通道 / 12 bit / SPI） |
| 备选型号 | ADS7828（8 通道 / 12 bit / **I2C**）；同类 4/8/16 通道 SPI ADC |
| 封装 | DIP-16 / SOIC-16（MCP3208 常见） |
| 供电 | **2.7 ~ 5.5 V**（可用 3.3 V） |
| 通道数 | 8（**可按每块板一片的方式拼出任意 8 的倍数**） |
| 分辨率 | 12 bit |
| 吞吐 | 约 **100 ksps** 量级（8 通道轮流转换 → 每通道约 12.5 ksps） |
| 接口 | `SCLK / MOSI / MISO / CS`，另有 `VREF`（外部基准） |

**核心功能**

- 把 **N 路模拟量** 轮流切换进**同一个 SAR 转换器**，逐次输出数字结果
- 12 bit 分辨率 ⇒ 对"4 mm 行程"这类应用**远超需求**（要 100 级力度也只需 ~7 bit）
- **总线化**：不管多少路模拟量，到 FPGA 侧只占 **4 根 SPI 线**（多片级联只需多一根 CS）

**★ 决策记录：「就地数字化」 vs 「多路复用 + FPGA 片内 ADC」——为什么选前者**

> ### ⚠️ 先破一个常见误解
> **多通道 ADC 芯片内部，本来就是一个「N:1 模拟多路开关 + 一个 SAR 转换器」。**
> 所以「就地数字化」**并不是"不需要多路复用"**，而是——
> **多路复用发生在 ADC 芯片内部，而不是板上外置。**
>
> 于是两条路的真正区别只剩两点：**① mux 在哪　② 数字化在哪**。

| 对比维度 | **① 就地数字化**（板上 ADC） | **② 多路复用 + 片内 ADC** |
|---|---|---|
| **mux 位置** | **ADC 芯片内部** | **板上外置**（CD4051 / 74HC4067 类） |
| **数字化位置** | **传感器板** | **FPGA 板（片内 ADC）** |
| **跨板信号** | **5 根数字**（SPI×4 + CS） | 2 根模拟 + 3～4 根数字（地址） |
| **模拟路径长度** | ⭐ 短，板内可包地 | ⚠️ 需跨板（除非加运放缓冲） |
| **抗干扰** | ⭐ **好** —— 跨板只走数字 | ⚠️ 较差 —— 模拟跨板，且与地址线并行 |
| **Ron 分压误差** | ✅ **不存在**（片内 mux，误差已计入芯片规格） | 🔴 **存在** —— Ron 与传感器输出阻抗分压 → **增益误差**，且 **Ron 随温度/通道漂移** |
| **有效分辨率** | 12 bit | 60 dB SNR ≈ **10 bit**，再叠 Ron 误差 |
| **采样率** | ⭐ 100 ksps 级，需求（16 ksps）**余量极大** | ⚠️ 受片内 ADC 速率限制（规格只标「1 kHz 信号带宽」，**实际速率未核实**） |
| **接口变更** | ✅ 现有接口已按 SPI 定稿，**不用改** | 🔴 **接口必须重新定稿**（模拟出 + 地址线） |
| **平台耦合** | ⭐ **零** —— SPI 是标准接口，换任何 FPGA 都可用 | 🔴 **强** —— 依赖特定板/器件的 ADC 能否引出、速率够不够 |

**✅ 选 ① 的三条理由**

1. **误差链更短** —— ② 的 `Ron` 分压是个"半标不掉"的误差源：固定温度下逐通道尚可标定，但**随温度漂移**。而霍尔本身也要标定（零点/灵敏度离散 + 装配公差）。**两个漂移源叠加**，力度一致性更难保证。① 从物理上消掉了这个误差源。
2. **抗干扰更好** —— 传感器阵列采集的通用原则是「**模拟量在传感器端就地数字化，长线只走数字**」。② 要用模拟跨板，属于**弥补一个本来可以不存在的问题**。
3. **不依赖平台** —— ① 是标准 SPI 器件，**换 FPGA 型号、换板子都不用改**；② 要同时满足"ADC 引脚能引出 + ADC 速率够 + 接口改版"三个条件。

**⚠️ ② 并非没有价值（条件式保留）**

② 的真实优势是：**少 2 片芯片、更便宜、更好焊**。**若**下面同时成立，值得重新评估：
1. 片内 ADC 速率核实下来**很充裕**（如 ≥50 ksps）
2. **两块板实际距离很短**（< 5～10 cm，模拟跨板噪声可控）
3. 愿意在传感器板加**一级运放缓冲**（低输出阻抗驱动长线）
4. 想省器件数与成本

**结论：主走 ①，② 作为两条退路的备选** ——
- 退路一：多通道 ADC **采购出问题**（缺货/涨价）时顶上
- 退路二：后面**需要更多通道**时，片内 ADC 方案扩展更灵活

**★ 推论：凡是"多通道 ADC"，都不必担心"多路复用"这件事本身**

> 只要 mux 在**芯片内部**，就自动规避了外置 mux 的全部麻烦：
> **无 Ron 分压、无建立时间难题、无额外器件、无通道一致性校准需求**。
> ➡️ **通用原则：需要多路采集时，优先选"多通道 ADC 芯片"，而不是"外置 mux + 单通道 ADC"。**

**关键参数（按实际型号核实）**

| 参数 | 说明 |
|---|---|
| 通道数 | **8 通道 × 片数**（要几路就放几片） |
| 分辨率 | 12 bit（对数毫米行程的连续位置量**远超需求**） |
| 每通道速率 | **1 kHz 通常足够**（连续位置/力度类输入）；多通道合计 = 通道数 × 每通道速率 |
| 通道间时间偏斜 | 微秒级 → 对"同时动作"的**毫秒级判定无影响** |
| 是否需要同步采样 | **一般不需要**（专用同步采样 ADC 成本高，仅在相位敏感场合才需要） |
| 基准 VREF | ⚠️ **必须与传感器输出摆幅匹配**，不匹配就要加运放做偏置+缩放 |

**使用要点（踩坑）**

1. **⭐ 量程/基准必须和传感器一起定**
   传感器的输出摆幅 → 决定 `VREF` 与是否需要运放缩放。**这两者分开定一定会返工。**
2. **⭐ 必须做逐通道标定**
   传感器零点与灵敏度**逐片、逐轴不同**（+ 装配公差）⇒ 需**上电自校 + 少量固定系数**。
3. **算清总吞吐，别只看单通道**
   `总吞吐 = 通道数 × 每通道速率`。例如 16 路 × 1 kHz = 16 ksps，对 100 ksps 级芯片余量充足；
   但**通道是轮流转换的**，通道越多，单通道实际速率越低。
4. **模拟走线要短、要包地、旁边要有相邻地回流**；数字/模拟**分区、分地**。
5. **与 LED 驱动走线分开**，必要时**分时**（LED 刷新避开 ADC 采样窗）。
6. **⚠️ 长线只走数字是这套方案的全部价值所在** ——
   若把 ADC 放到 FPGA 板、让多路模拟跨板，就**把方案的最大优点丢掉了**。
7. **接口记得留备用线**：按 `3V3 / GND×2 / SPI×4 / CS1 / CS2 / I2C×2` 预留 →
   **换型号/加通道都不用改板**（"留几针"比"接什么"更该提前定）。

**同类对比（选型参考）**

| 方案 | 差异 | 什么时候选它 |
|---|---|---|
| **多通道 SAR ADC（MCP3208 类）** | 8 通道 12 bit SPI，**mux 在片内** | ⭐ **多路采集、要总线化、要抗干扰时的首选** |
| **ADS7828 类** | 8 通道 12 bit **I2C** | 想省一根线、速率要求更低时；⚠️ I2C 速率低于 SPI |
| **外置 mux + 单通道 ADC** | mux 在**板外** | ❌ **不推荐** —— Ron 分压误差、建立时间、额外器件，全是白送的麻烦 |
| **外置 mux + FPGA 片内 ADC** | 用上片内 ADC，省一片芯片 | 见上方「决策记录」② —— **条件式保留**，需满足 4 个前提 |
| **Σ-Δ（过采样）ADC** | 高分辨率、低速；⚠️ 建立时间长 | 慢速高精度场景（称重、温度）；**不适合需要快速切换的多路扫描** |
| **专用同步采样 ADC（多路同时采样）** | 所有通道同一时刻采样 | 需要**严格同时**采集相位敏感量时；一般多路扫描用不上 |

**其他注意**

- **不要选"数字输出型"传感器凑数** —— 那是把 mux 问题换成了"IO 数量"问题（见 D04）
- 通道数**一次留够**：宁可买 8 通道用 4 路，也别买 4 通道不够用
- **逐通道标定系数要能存**（上电自校）→ 这部分在 FPGA 侧，但硬件要能提供稳定的参考状态

---

## 四、分类体系（后续新增器件按此归类）

| 编号 | 类别 | 典型器件 | 收录情况 |
|---|---|---|---|
| 一 | **数字逻辑 / 接口扩展** | 74HC595、74HC165、74HC138、74HC245、IO 扩展芯片 | D01 |
| 二 | **模拟开关 / 信号切换** | CH440R、CD4051、CH446、ADG 系列 | D02 |
| 三 | **数据转换 / 模拟前端** | 高速 AD/DA 模块、PGA、ADC/DAC 芯片、仪表放大器 | D03、D06 |
| 四 | **电源 / 驱动** | LDO、DC-DC、电机驱动、LED 驱动（TPIC6B595）、MOSFET 驱动 | **D08** |
| 五 | **通信接口** | UART/RS485、CAN、SPI/I2C 桥片、USB 转串口（CH340、CP2102） | 待收录 |
| 六 | **传感器** | 温湿度、加速度、霍尔、光耦 | D04 磁轴+霍尔、D05 FSR、**D07 霍尔开关** |
| 七 | **存储** | EEPROM（24Cxx）、Flash、SD 卡、FRAM | 待收录 |
| 八 | **显示 / 人机交互** | 数码管、LCD、OLED、LED 点阵、按键/编码器、蜂鸣器 | **D09 串口屏** |

---

## 六、传感器 / 人机交互

### D04. 磁轴 + 霍尔传感器 —— 按键连续行程 / 力度输入 ✅（器件待定）

**基本信息**

| 项 | 值 |
|---|---|
| 类型 | 机械轴体（内置磁铁）+ 霍尔传感器，**非接触**测量按键行程 |
| 组成 | 轴体（弹簧 + 磁铁）+ 定位板 + 轴座 PCB + 霍尔 IC |
| 输出 | **数字霍尔 → 0/1 开关量**；**线性霍尔 → 随磁场连续变化的模拟电压** |
| 供电 | 3.3V / 5V（视型号） |
| 线性霍尔候选 | SS49E / DRV5053 / MT9105 类（**灵敏度、量程、封装均待核实**） |

**核心功能**

- 磁铁随轴上下运动 → 霍尔测磁场变化 → 换算为行程
- 全行程模拟量输出 ⇒ 可直接做 **Velocity（力度）/ Expression（表情）**，这是普通机械键盘做不到的
- 无机械触点 ⇒ **无磨损、寿命长**，且不像电位器那样会"爆米花噪声"
- ⭐ **提供真实的按压手感** —— 这是 FSR / 电容都没有的：行程、段落感、回弹都是"摸得到"的演奏反馈
  → **一个硬件可以支持两种乐器形态**：
  - **键盘类乐器**：一排键位 = 音阶键盘
  - **指板类乐器**：手指横向划过一排 = 滑奏/扫弦
- 抗环境干扰好：汗手、湿度、温度都不影响（对比电容触摸）

**⚠️ 使用要点（踩坑）—— 最重要的一条**

> **"数字霍尔"和"线性霍尔"是两个完全不同的东西，选错会直接毁掉力度维度。**
>
> | | 数字霍尔（键盘磁轴最常用） | 线性霍尔 |
> |---|---|---|
> | 输出 | **只有 0 / 1**（带可调触发点） | 随磁场**连续变化**的模拟电压 |
> | 能拿到的信息 | 是否按下、触发深度阈值 | **完整行程曲线** → 力度 / 表情 |
> | 接口 | 每路 1 个 GPIO | 需 ADC（多路建议走多通道 ADC 芯片） |
> | IO 消耗 | **1 IO / 轴** —— 轴一多就吃光所有 IO | **总线化**：12 轴也只要 4 个 IO（SPI） |
>
> **判断方法**：看输出引脚类型；或看 ADC 读数是否只在两个值之间跳。
> **典型现象（数据侧）**：把"行程 ↔ 输出电压"曲线画出来，如果只有几档跳变、不成连续单调曲线，就是数字霍尔。

> ⚠️ **务必区分"手感卡顿"和"数据阶跃"—— 两者根因完全不同，别互相误判！**
>
> | | 症状 | 根因 | 搞错会怎样 |
> |---|---|---|---|
> | **手感卡顿** | **手指能感觉到**的段落 / 涩 / 卡滞 | **机械结构 / 轴体** | 去查 ADC、改代码 → 白忙 |
> | **数据阶跃** | 手感顺滑，但行程读数只有几档 | **数字霍尔**（只有 0/1） | 去改结构 → 白忙 |
>
> **手感问题只能用"手按"验证；数据问题只能用"画曲线"验证。**

**⚠️ 使用要点（踩坑二）：DIY 时用 3D 打印件做定位板/轴座，公差是最大坑**

磁轴键盘的轴体靠**定位板 + 轴座 PCB** 保证同轴。用 FDM 打印件替代时：

| # | 坑 | 机制 | 对策 |
|---|---|---|---|
| 1 | **打印孔偏小** | FDM 受**材料收缩 + 挤出膨胀**影响，孔径常比模型小 **0.1～0.3 mm** | 孔径**放大 0.15～0.3 mm**；或用**钻头/铰刀修孔**；**先打小样量孔径再定稿** |
| 2 | 孔内壁层纹台阶 | 层层台阶与轴体滑轨摩擦 | 打磨内壁；或后处理平滑 |
| 3 | 打印摆放方向 | 圆孔椭圆度随摆放变化 | 让**孔轴与 Z 轴平行**，或把零件竖起来打 |
| 4 | 同轴度差 | 孔、轴体、磁铁/PCB 位置偏差 → 侧向受力 | 假组装先检查；用**台阶孔 + 定位柱** |
| 5 | **磁吸附加阻力** | 磁铁会吸**铁质**件（铁螺丝、钢定位板）→ 额外吸力与偏磨 | 靠近磁铁的结构件用 **铝 / 黄铜 / 尼龙 / PLA**，螺丝用非铁材料 |

> **二分诊断法（最快定位）**：把轴**从结构里拆出来单独手按** ——
> 裸轴也卡 ⇒ **轴体问题**（换轴）；裸轴顺滑、装进去才卡 ⇒ **配合/公差问题**（修孔）。

**⚠️ 使用要点（踩坑三）：磁轴/按键与其他传感面「混平面」→ 手指移动必然卡顿**

实测结论（2026-09-12）：**磁轴本身手感可接受，但"手指从磁轴面移到旁边电容感应面"明显卡顿。**

四个根因：

| # | 根因 | 表现 |
|---|---|---|
| 1 | **高度差（Z 不共面）** | 键帽是凸起"高地"，电容面是另一层 → 手指要"翻"过去 |
| 2 | 接缝台阶 / 沟槽 | 两零件拼接缝，手指被绊 |
| 3 | 摩擦系数突变 | 打印件 vs 电极覆膜 → 阻力突然变化 |
| 4 | **必须先抬手 → 动作断裂** ⭐ | 体感的根本来源；这一条**共面化也解决不了** |

> ## 🔴 由此得到一条比"选哪种传感器"更靠前的通用设计原则
> ### **凡是要靠手指移动来切换的操作，必须发生在同一个平面、同一高度上。**
>
> - 同平面滑动（按键 → 按键）= **顺滑**
> - 跨面移动（按键 → 另一层传感面）= **卡顿**
>
> **解决层级（改动量从小到大）**：
> | 层级 | 做法 | 评价 |
> |---|---|---|
> | **L4 交互分离** | 让手指根本不需要跨面（调制交给**另一只手**的独立控制区） | ✅ 零改动，优先考虑 |
> | L1 共面化 | 统一高度 ±0.1mm + 填缝 + 矮帽/平顶 + 摩擦统一 + 过渡倒角 | 改动小，但"必须先抬手"解决不了 |
> | L2 嵌入式 | 另一类传感器的电极**嵌在按键之间的空隙里**，磨到同一高度 | 需实测可用性 |
> | L3 柔性面 | 整块柔性盖板覆盖在阵列上方 | 效果最好但工程难度大 |

**💡 用法提示：手指划过一排按键 = 天然的"滑奏"输入**

- 一排同高度的按键，手指横向划过 ≈ 键盘划过一行，本身就是连续动作
- **顿挫感未必是缺点**：真实吉他滑弦也会经过品格、有"格子"触感，是**乐器感的一部分**。
  要做的是别让它变成**硬台阶** → **矮帽/平顶帽 + 小间隙 + 统一高度**
- **★ 双压插值技巧**：手指划过时若同时压住相邻两轴，用**两轴行程比例插值**可算出**连续位置** ——
  把离散的开关阵列变成"准连续"位置输入（触发瞬间的"双压"就是插值依据）
  - 前提：**轴间距 < 手指宽度**（约 6～8mm），使手指能同时压到两轴
  - ⚠️ 插值抖动取决于霍尔线性度与装配一致性，需实测

**★ 设计技巧：一根轴当两个维度用（行程频段分离）**

一个轴只有一路行程信号，但**按频段拆开就能同时承载两个维度**：

```text
行程 Z 信号
   ├─ 触发瞬间取值 ──→ 采样保持 ──→【力度 Velocity】   (阶跃/直流)
   ├─ 慢变分量(<1Hz) ─────────────→【音量 / 表情】
   └─ 4～7 Hz 分量 ───────────────→【颤音 Vibrato】
```

- **核心思路：触发瞬间锁定力度，之后的变化全部让给颤音**
- 符合真实乐器逻辑 —— 力度在"击弦"那一瞬就已确定，之后手指的动作都是"表情"
- ⚠️ **必须把"音量"从 Z 解耦**（音量只在触发瞬间取值），否则颤音会带着音量一起抖
- 推广：任何"单路连续量 + 需要承载多个表达维度"的场景都可套用
  （低频给慢参量、高频给表情量），前提是**两个维度的频段不重叠**

**关键参数（需按实际器件核实）**

| 参数 | 说明 |
|---|---|
| 行程 | 常见键盘磁轴 3.5～4 mm |
| 线性度 | 决定力度曲线的真实性；磁铁↔霍尔间距敏感 |
| 采样率 | 力度/行程类应用 **1 kHz 足够**；多轴可分时复用 |

#### ⭐ 线性霍尔选型：**三问定生死**（2026-09-13 实测/查证后补）

> **背景实测数据**：磁轴磁铁的磁场范围约 **120 ～ 700 G**（商家标称，**在特定间距下测的**）

| # | 问 | 门槛 | 不达标怎么办 |
|---|---|---|---|
| **1** | ⭐ **磁场范围 ≥ 使用中的峰值 G？** | 🔴 **硬性** | ❌ **直接淘汰** —— 会饱和，最高段读数被削平 |
| **2** | **灵敏度 ≥ ~0.7 mV/G？** | 🟡 舒适线 | ⚠️ 低于也能用（**理论下限约 0.2 mV/G**），只是力度档位变粗 |
| **3** | **供电含目标电压（如 3.3 V）？** | 🔴 **硬性** | ❌ 淘汰；或接受"加 5 V 轨 + 电平处理"的复杂度 |

**候选横比（已查证的部分）**

| 参数 | ⭐ `HAL49E` | `HAL4904` | `HAL4903` | `DH49E` | `SS49E` | `DRV5053` |
|---|---|---|---|---|---|---|
| **磁场范围** | **min ±600 / typ ±1000 G** | **±1000 G** | ⛔ **±450 G** | ⛔ ±600 G | 待核实（较大） | 待核实 |
| **灵敏度** | **min 1.7 / typ 2.0 / max 2.5 mV/G** | 1.5 mV/G | 3.7 mV/G | 2.9 mV/G | 1.4 mV/G | 多档 |
| **供电** | ✅ **3～6.5 V** | ✅ 2.5～6.5 V | 2.5～6.5 V | ⛔ 4.5～5.5 V | ⛔ 4.5～10.5 V | ✅ 2.5～5.5 V |
| 输出 | 待核实 | ⭐ **轨到轨** | 轨到轨 | 0.8～4.2 V | 0.8～4.2 V | 待核实 |
| 可得性 | ✅ **好买** | ⚠️ 不好买、1～2 元 | — | — | 进口 | 进口 |

**⭐ 三条关键教训**

| # | 教训 |
|---|---|
| **1** | ⭐⭐ **设计用「最小值 / 最大值」，绝不用「典型值」** —— `HAL49E` 典型 ±1000 G 但**保证值只有 ±600 G**；按典型值设计，**买到一批下限件就会饱和**，而且**在现场才暴露、最难排查** |
| **2** | ⭐ **「高灵敏度」是双刃剑** —— `HAL4903` 灵敏度 3.7 mV/G 看着很好，但**量程只有 ±450 G**，700 G 直接出局。**量程 × 灵敏度大致是常数** → **先保证量程，再看灵敏度** |
| **3** | ⭐⭐ **「磁铁-霍尔间距」是唯一的量程/阈值调节旋钮** —— 磁场随距离快速衰减（近似 `1/r³`）→ **量程不够就把霍尔放远一点**，反之移近。**所以器件量程差一点不是死结**，只要结构留出**可调的安装高度 / 可换垫片** |

**同类对比**

| 方案 | 力度维度 | 成本 | 可 3D 打印验证 | 备注 |
|---|---|---|---|---|
| **磁轴 + 线性霍尔** | ✅ 连续 | 中低（轴体便宜 + 霍尔稍贵） | ✅ 可（孔位 + 定位板） | ⭐ **要连续行程时的首选** |
| 磁轴 + 数字霍尔 | ⚠️ 仅阈值 | **最低** | ✅ 可 | 只有开关量，无连续行程 |
| FSR | ✅ 连续 | **偏高**（≥7 元/件） | ❌ **不可**（外购薄膜） | 见 D05 |
| 电容触摸 | ⚠️ 仅面积近似 | 低 | ✅ 可 | 无压力维度 |

**其他注意**

- 轴体分**线性轴**与**段落轴**：需要连续行程**必须用线性轴**
  （段落轴会带来与电路无关的物理"卡顿"，**别误判成采样问题**）
- 磁铁↔霍尔间距直接决定量程与线性度，装配公差敏感 → 需逐个标定，或选容差宽容的方案

---

### D05. FSR（压阻式压力传感）—— ⚠️ 某次评估后未采用，但结论值得记住

**基本信息**

| 项 | 值 |
|---|---|
| 类型 | 压阻式力敏薄膜（Force Sensing Resistor） |
| 原理 | 压力 ↑ → 电阻 ↓（典型从 >1 MΩ 降到几 kΩ 量级） |
| 用法 | 与固定电阻**分压** → ADC 读电压 |
| 形态 | **外购薄膜件**（带背胶 / FPC 引线）—— **不能自行加工，也不能 3D 打印** |
| 价格 | **≥ 7 元/件**（2026-09 实际询价） |

**⚠️ 使用要点（踩坑）**

1. **不能 3D 打印** —— 能打印的只有它的压板 / 限位框，**传感器本体必须外购**。
   → 在"机械结构尚未定型、要靠反复打样验证手感"的阶段，选 FSR 等于**把验证环节直接堵死**。
2. **成本并不便宜** —— 单件 ≥7 元，比"磁轴 + 线性霍尔"更贵。**"FSR 便宜"是个直觉误区。**
3. **迟滞与重复性一般**，需机械限位防止过压，长期有漂移。
4. **与电容触摸共存时，它的干扰是几种方案里最大的**（见下）。

**★ 关键结论：和电容触摸放在一起，FSR 的干扰远大于磁轴**

> 先纠正一个常见误解：**磁轴对电容的干扰来源不是"磁场"，而是"磁铁是导体"。**
> **静磁场对电容测量基本没有影响**（磁场会干扰霍尔，但不会干扰电容）。

| 干扰源 | 物理机制 | 性质 | 能否标定消除 |
|---|---|---|---|
| **磁轴**：磁铁（导体）靠近电极 | 增加**静态**寄生电容 → 拉低基线 | 静态污染 | ✅ **上电标定 + 基线跟踪即可化解** |
| **磁轴**：轴体上下往复 | 磁铁↔电极间距变化 → 寄生电容小幅波动 | 动态（幅度小） | ⚠️ 主要靠拉开距离 |
| **FSR**：薄膜覆盖在电极上方 / 邻近 | **面状导电膜屏蔽手指与电极间的电场** → 灵敏度大幅下降 | **结构性** | ❌ **无法消除** |
| **FSR**：受压形变 | FSR↔电极间距随压力变化 → **压力被误读成位置移动** | **动态 + 与压力耦合** | ❌ **无法标定**（不是偏移，是耦合） |
| 手指出汗 / 湿度 | 介电常数变化 | 动态 | ⚠️ 靠基线跟踪缓解（两者都有） |

**一句话**：**磁轴的干扰是"静态污染"（洗得掉），FSR 的干扰是"动态耦合"（洗不掉）。**

**由此得出的通用设计原则（电容触摸 + 其他传感器共存时）**

1. 电容电极与任何干扰源（磁铁、FSR、引线）**水平净距 ≥ 15～20 mm**（经验值，**需实测验证**）
2. 两者之间走**接地隔离带 / guard ring**；电容电极背面铺**接地屏蔽层**
3. 开机做一次**基线标定**，运行中保持慢速**基线跟踪**
4. **先做小样板实测**（例如 3 个磁轴 + 1 条电容条）再定最终布局
5. 电容电极要测哪个方向，**电极就必须沿那个方向分段**：
   横向长条（电极沿长度排）测到的是 **X**；要测 **Y** 需多条横向电极带**竖直叠放** + 相邻插值

---

### D07. 霍尔开关（数字霍尔）—— 只用它拿「事件」，不要拿它拿「量」

**基本信息**

| 项 | 值 |
|---|---|
| 类型 | **霍尔开关（Hall Switch）** —— 内部自带**比较器 + 迟滞**，**直接输出 0/1** |
| 别名 | 常被叫作「**数字霍尔**」（与之相对的是「线性霍尔」，见 D04） |
| 输出 | **数字**：开漏（需上拉）或推挽 |
| **需要 ADC 吗** | ❌ **不需要** —— 直接进 MCU/FPGA 的 GPIO |
| 代表型号 | `HAL2032`/`HAL2033`/`HAL2034`/`HAL2035`（霍尔微）／`AW86516EB`（艾为） |

> ⚠️ **同一项目里，两类霍尔要**分开选**（这是本条的**核心认知**）：
>
> | 用途 | 该用哪类 | 为什么 |
> |---|---|---|
> | **要连续行程 → 力度** | **线性霍尔**（D04） | 需要**连续变化**的模拟量 |
> | **只要"碰到了没有" → 事件** | **霍尔开关**（本条） | 只要 **0/1**，数字霍尔**正好合适** |
>
> 📌 **推论**：**"数字霍尔 vs 线性霍尔"没有绝对优劣，取决于你要的是"量"还是"事件"。**

#### ⚠️⚠️ 四类霍尔开关 —— **选错一类完全不能用**

| 类型 | 原理 | 适合"靠近→离开"的动作吗？ |
|---|---|---|
| ⭐ **全极性（Omnipolar / 无极）** | **N 或 S 任意一极**靠近都触发 | ✅ **最省心** —— 磁铁方向不用管 |
| **单极性（Unipolar）** | 只对**指定的一极**响应 | ✅ 可用，但**磁铁极性必须装对** |
| ⛔ **双极锁存型（Latching）** | 必须 **N / S 交替**才翻转 | ⛔ **不能用** —— 靠近触发后，**必须反向磁极才能复位**；"离开"不会复位 → 第二次拨不动 |
| ⚠️ **微功耗 / 低功耗型** | 内部**周期性采样**省电（约 20～100 Hz） | ⚠️ **慎用** —— **触发延迟 10～50 ms** |

> 🔴 **一句话：要「全极性 + 连续工作型」。**
> 📌 **查证方法**：看 **"响应频率 / 采样率"** 参数 —— **< 1 kHz 就要警惕**。

#### 选型必须核的 5 个数

| # | 参数 | 判据 |
|---|---|---|
| **1** | **极性类型** | **全极性**（或单极性 + 磁铁极性固定）→ ⛔ 不能双极锁存 |
| **2** | **响应频率 / 采样率** | **≥ 1 kHz** → ⚠️ 避开微功耗采样型 |
| **3** | **`Bop` / `Brp`（动作 / 释放磁通密度，Gs）** | 与**磁铁实际场强**匹配 |
| **4** | **供电范围** | 必须含目标逻辑电平（如 **3.3 V**） |
| **5** | **输出类型** | **开漏** → 需**上拉电阻**；**推挽** → 输出 = VDD |

#### ⭐ 使用要点

1. **⭐ 阈值靠「间距」调，不靠换芯片**
   磁场随距离快速衰减（近似 `1/r³`）→
   **一直处于触发态** ⇒ **把磁铁放远** ；**拨到最大也不触发** ⇒ **把磁铁放近**。
   > 📌 通用原则：**「间距」是霍尔类器件唯一的阈值/量程调节旋钮**（D04、D06 都适用）。
2. **开漏输出别忘了上拉电阻**（否则读不到高电平）
3. **⚠️ 别用在需要"量的分辨"的场合** —— 磁轴键盘用数字霍尔，就会丢掉整个力度维度（见 D04）

**同类对比（触发类器件的横向对比）**

| 器件 | 延迟 | 要 ADC？ | 要标定？ | 动作感 | 寿命 |
|---|---|---|---|---|---|
| ⭐ **霍尔开关 + 磁铁** | **µs 级** | ❌ | ❌ | ⭐⭐ 拨动即触发 | ✅ 无接触 |
| 线性霍尔 + **外部比较器** | µs 级 | ❌ | ⚠️ 要设阈值 | ⭐⭐ | ✅ |
| 光电对管 + 比较器 | **µs 级（最低）** | ❌ | ⚠️ 阈值 + 遮光 | ⭐⭐⭐ 最像"拨" | ⚠️ 怕灰 |
| 磁簧开关（Reed） | 0.5～2 ms + **要去抖** | ❌ | ❌ | ⭐⭐ | ⚠️ 玻璃管怕冲击 |
| 微动开关 | 🔴 **去抖 1～10 ms** | ❌ | ❌ | ❌ 是"按"不是"拨" | ⚠️ 磨损 |

> ⭐ **延迟对比的实用结论**：**在 10 ms 级的项目预算里，µs 与 1 ms 的差别听不出来**（人耳约从 10 ms 量级才敏感）
> → **所以"延迟最低"往往不构成决定性优势，手感/可靠性/省事程度才是。**

---

### D08. LED 驱动方案对比 —— 核心判据是「每通道能否独立 PWM」

**基本信息**

| 项 | 值 |
|---|---|
| 要解决的问题 | 一组 LED 点亮/调色，怎么用最少的 IO 与最少的 FPGA 资源驱动 |
| **核心判据** | ⭐ **每个通道能否独立做 PWM** —— 这决定**能不能调色（RGB）**，与"是否可寻址"无关 |
| 全彩的数学 | 3 通道 × 8 bit = **256³ ≈ 1677 万色**，是"每通道独立 PWM"**调**出来的 |

**六种驱动方式**

| 方案 | 线数 | 关键限制 |
|---|---|---|
| GPIO 直驱 | **N 根**（12 灯 = 12 根） | IO = 灯数，多了不现实 |
| **74HC595 类移位寄存器** | **3 根** | ⛔ **做不了 RGB** —— 只有开关量、无逐通道 PWM；OE 只能"全局"调光 |
| 恒流驱动 IC（`TLC5940` 类） | 4～5 根 | 逐灯 12 bit PWM，需 GSCLK + IREF 电阻 |
| **可寻址灯珠（`WS2812`/`SK6812`）** | **1 根** | ⚠️ 时序严格（±150 ns）、**刷新期间不可中断**、**单灯坏整链断**、电流大 |
| ⭐ **I2C / SPI 驱动 IC（`IS31FL3731` 类）** | **2 根** | ✅ **FPGA 负担最小**：只写寄存器、**芯片自刷新不抢时间片**、**单灯坏不影响其他** |
| 独立灯带 | 1～3 根 | 与传感器完全解耦，但**指不到具体位置** |

**⭐ 两条重要判断**

| # | 判断 |
|---|---|
| **1** | **要 RGB 的话，"加驱动 IC"比 `WS2812` 更适合** —— 只写寄存器、不占时间片、不与音频引擎抢资源、坏一个灯不影响其他 |
| **2** | **`74HC595` 做不了 RGB 调色** —— 这是硬限制，不是配置问题 |

**⚠️ 与霍尔（磁场传感器）共板时的干扰分析**

| 项 | 说明 |
|---|---|
| LED 芯片本身 | **不含铁磁材料 → 对霍尔几乎无直接影响** |
| **真正的干扰源** | **驱动电流产生的磁场**：`B = μ₀·I / (2πr)` |
| **量级** | `WS2812` 全白 12 灯（≈720 mA）@5 mm ≈ **2.9 Gauss**；单色 12 灯（≈120 mA）≈ **0.5 Gauss** |
| 对比信号 | 霍尔要测的**磁铁磁场有几十～几百 Gauss** → 干扰只占 **~1 %** |
| ⚠️ **决定性差别** | **不是量级，而是"能不能标定掉"**：<br>**静态直流** → ✅ **上电基线标定可消除**<br>**PWM 交流纹波** → ❌ **标不掉**，会产生与 PWM 频率同步的**周期性读数抖动**<br>**`WS2812` 只要灯亮就持续有纹波**（内部恒流 + PWM 调光） |

> ⭐ **为什么"别人用 WS2812 没事，不代表你的场景也能行"**：
> 多数开源键盘用**数字霍尔（0/1 阈值判断）**，纹波引起的抖动根本不影响；
> 而**用线性霍尔做连续行程时，对读数稳定性的要求高一个数量级**。
> 📌 **判据极简单便宜：点亮灯，看霍尔读数抖不抖。**

**降低干扰的措施（按有效性排序）**

| 排序 | 措施 |
|---|---|
| ★★★ | **去流与回流走线成对紧贴**（环路磁场互相抵消） |
| ★★★ | **灯电流走线远离霍尔**（按 `1/r` 衰减） |
| ★★ | 不与霍尔模拟走线**平行**走长距离 |
| ★★ | **降低灯电流 / 限制全局亮度** |
| ★★ | 提高/避开 PWM 频率，或**分时**（LED 刷新避开 AD 采样窗） |
| ★ | 磁屏蔽 |

---

### D09. 串口屏（淘晶驰 TJC X2 类）—— **不用写屏驱动**的显示方案

**基本信息**

| 项 | 值 |
|---|---|
| 类型 | **UART 组态屏（HMI）** —— 屏内置 MCU + 组态软件，主控只发**指令帧** |
| 代表品牌 | 淘晶驰 TJC（X2 系列 7 寸等）／大彩／迪文 DGUS |
| 接口 | **UART**（一般 4 根：`5V / GND / TX / RX`） |
| ⚠️ 串口电平 | **待核实**（5 V TTL 还是 3.3 V）→ 决定要不要电平转换 |
| 资料 | `http://wiki.tjc1688.com/`（⚠️ 该站用 **HTTP**，HTTPS 会失败） |

**⭐ 核心价值：字库、画点、触摸扫描全部由屏内部处理，主控完全不碰**

| 对比 | 传统 LCD | **串口屏** |
|---|---|---|
| 主控要做的 | 写驱动、建字库、画点、扫触摸 | **只发指令帧 + 解析返回帧** |
| 工作量 | 大 | **小（属"小模块"级别）** |

**指令与返回格式**

| 方向 | 格式 |
|---|---|
| 主控 → 屏 | 文本/16 进制指令，如 `t0.txt="123"`、`n0.val=`、`page` |
| 屏 → 主控 | **统一以 `FF FF FF` 三个字节结尾** → **定界清晰，状态机好写** |

**常用返回码**

| 返回码 | 含义 |
|---|---|
| **`0x65`** | **控件点击事件** ⭐ 触摸 |
| **`0x70` / `0x71`** | **字符串 / 数值变量数据返回** ⭐ **双向参数同步**（屏 → 主控） |
| `0x66` / `0x67` | 当前页面 ID / 触摸坐标 |
| `0x88` | 系统启动成功（握手） |
| `bkcmd`（系统变量） | 控制是否返回执行结果（**默认 2 = 只返回出错**）→ 可关掉省带宽 |

**⭐ 值得注意的控件（TJC X2 实测目录里有的）**

| 控件 | 用途 |
|---|---|
| ⭐ **曲线波形控件** | **屏自带！** 波形/频谱可以**屏自己画**，主控只周期发数据点 |
| ⭐ **滑块控件** | 屏上直接拖，还能**回传数值**给主控 → 适合"精确量"参数 |
| 下拉框 / 单选框 / 双态按钮 | 模式切换、选项选择 |
| 进度条 / 指针控件 | 显示连续量（力度、深度） |
| 文本 / 数字 / 虚拟浮点数 | 数值显示 |
| 定时器 / 变量控件 | 屏内自动化 |

**⚠️ 真正的限制：UART 带宽**

| 波特率 | 速率 | 用途判断 |
|---|---|---|
| 115200 | ≈ **11.5 KB/s** | 一屏 20 个数值（≈300 B）= **约 26 ms/帧** ✅；画波形（100 点 ≈400 B）= **35 ms/帧 ≈ 28 fps** 🟡 勉强 |
| 921600（若支持） | ≈ **92 KB/s** | 波形刷新**完全够用** → ⚠️ **需核实屏支持的最高波特率** |

> ➡️ **结论：串口屏适合「数值 / 文字 / 按钮」，不适合「高频图形动画」。**

**⭐⭐ 两条通用结论**

| # | 结论 |
|---|---|
| **1** | **串口屏的定位是"参数面板 + 文字显示"，不是"高刷显示器"** —— 高频图形要么交给屏的**曲线控件**（降低刷新需求），要么另走视频输出 |
| **2** | 🔴 **串口屏延迟不可控（触摸扫描 + 界面处理 + UART 多级延迟）→ 绝不能进低延迟控制通路**，只做 UI |

---

## 待补充信息（CH440R）

请补充以下信息以便完善 D02（可直接告诉我，或把 datasheet 路径给我）：

- [ ] 通道数是多少？几路？SPST / SPDT / 多路复用器（几选一）？
- [ ] 封装形式（SOP-16 / TSSOP / QFN ？）
- [ ] 供电电压范围、是否支持双电源 / 轨到轨
- [ ] 典型导通电阻 Ron（多少 Ω）
- [ ] 带宽 / 最高信号频率
- [ ] 你打算用它做什么（ADC 多通道？音频切换？总线切换？）

---

*最后更新：2026-09-13（新增 D06 多通道 SPI ADC · 就地数字化架构对比；D07 霍尔开关；D08 LED 驱动方案对比；D09 串口屏）*
*口径：本库只记**器件级通用知识**（参数、坑、选型判据、同类对比）。*
*具体项目的选型落地与决策记录见各自项目仓库，不写进本库 —— 本库可公开。*
*配套文档：*
- *`经验\fpga\FPGA逻辑调试经验集.md` —— 代码逻辑 / 时序 / 调试方法论*
- *`经验\fpga\FPGA工具使用经验.md` —— 工具操作流程与注意事项*
- *`FPGA\FPGA问题排查手册.md` —— 环境配置 / 编译报错*

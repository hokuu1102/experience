# 跨语言共性问题集（Common Issues Across Languages）

> **定位**：记录**在不同编程语言 / 开发环境（Verilog、C、Python、MATLAB、Shell…）之间重复出现**的共性问题与方法论。
> FPGA/硬件专属问题请查 → `fpga/FPGA问题排查手册.md`（环境/操作）与 `fpga/FPGA逻辑调试经验集.md`（逻辑 L）/ `fpga/TROUBLESHOOTING.md`（完整排查 T）。
> **约定**：每次遇到"这毛病在别的语言里也见过"的共性问题，追加到本文档，编号 C01、C02…，顶部索引补行。
> **创建**：2026-09-09

---

## 目录索引

| 编号 | 类别 | 案例 | 一句话 |
|---|---|---|---|
| C01 | 命名 | 变量名用了语言保留字（Verilog `buf`） | 任何语言的保留字都不能做标识符；报错常是"看似语法错误" |
| C02 | 验证 | 仿真/单测通过 ≠ 真实环境正确（iverilog 全对 vs 上板错乱） | 开发环境通过只证明"行为符合你写的"，不代表真实平台行为一致 |
| C03 | 调试 | 一次只改一个变量做 A/B 对比 | 多个改动同下时即使问题消失也不知道谁生效；恢复原状问题重现 = 基本确认 |
| C04 | 调试 | **"派生量/差值"异常 → 拆回原始分量分别观测** | 差值吃掉了"哪一端出问题"的信息；拆开后偏移量恒定 = 离散位/固定干扰，随机 = 噪声/时序 |
| C05 | 流程 | **配置与注释/文档漂移 → 配置类事实一律查源文件** | GUI/向导生成的配置不会写进手写注释、也未必提交版本控制；排查先查"实际生效值" |
| C06 | 调试 | **"局部性检验"：区分布式耦合 vs 近场耦合（也适用于"问题在本地还是外部依赖"）** | 逐个屏蔽干扰源，比较"相邻源"与"远端源"的改善幅度：相邻≫远端 = 近场；两者相当 = 分布式。前提是**把干扰源做成可逐项开关**（否则只能猜）|
| C07 | 调试 | **同一现象里可能叠加多个独立机制 → 先按"依赖条件"分簇再归因** | 两个机制混在一起讨论时，任何"改一个变量就变好/变坏"的结论都不可靠；分簇标志：某条件下 A 消失而 B 仍在 |
| C08 | 调试 | **证据形态要匹配问题类型**：判"有没有坏"用统计量，判"坏成什么样"必须用**原始序列** | 聚合量（均值/极值/计数）在数学上已经把单点事件的形状抹掉了。2026-09-10 实测：vpp/计数折腾**一整天**定不了案，抓 1024 点原始采样后**30 秒**定案 |
| C09 | 流程 | **标称值≠实测值**：频率、时钟、分压比、量程这类前提，必须**实测或从数据特征反推**后再用于任何计算 | 向导里输入频率填错（100 vs 50MHz）会让所有输出**静默减半**、不报任何错；用它算出的时序裕量"最优/最差"会正好反转。反推技巧：看样本是否成对重复（重复率≈100% 且游程全偶 = 采样率减半）|
| C10 | 验证 | **"没测到异常"必须先确认激励真的到达（空测试陷阱）** | 三帧"0 异常"实为信号源没开输出；该故障只在特定条件（码跳变）下才触发，激励为零时它根本没机会发生。**用与主判据无关的独立量做存在性检查**（定频拟合 0.67 LSB vs 期望 209 LSB）|
| C11 | 验证 | **判据要用"独立于被测量的真值"** | 用被污染的量（`max−min`）判断"有没有被污染"是循环论证。必须另有一条独立通道取真值（外部参考／模型拟合／第二种原理），才能把"**读数误差**"与"**对象故障**"分开 |
| C12 | 逻辑/判据 | **任何"过阈值就触发"的判据都必须留死区（迟滞）** | 单点阈值 `(上一拍<TH)&&(本拍>=TH)`：噪声把某拍推回阈值另一侧就多算一次事件。实测过零计数偶发偏高 +6.8%/+14.4%，而 2/4 帧完全正确 → 单帧永远发现不了。定量 `avg=(16−N)·P/16`（N=1→468、N=2→437，与实测吻合）|

| C13 | 验证方法论 | **失去真实系统后：把"验证"分成强度递减的层，并标注每层的证据强度** | 最大风险不是"做不下去"，而是**把弱证据当强证据**；报告里每条结论都要标注它来自哪一层 |
| C14 | 验证/判据 | **负向测试"故意打 ERROR" 与 "扫 ERROR 日志判失败" 直接冲突 → 让被测者给出显式结论** | **证据（evidence）与判定（verdict）必须分开表达**：被测者打印 `PASS/FAIL` + 非零退出码，运行器只认这两样，`ERROR` 行只计数 |
| C15 | 资料/规格 | **两份"都能工作"的参考互相矛盾时：用结构判据 + 宽容性实验收敛** | 别投票、别猜边界：读**结构**（数据占哪几位/计数方向/时隙长度），再找"多余的空闲位照样工作"这类**宽容性证据**界定范围 |
| C16 | 排错/判定 | **警告的严重性由下游结果指标裁决，不由警告自己裁决** | 先找下游可观测后果（全局时钟等级 / 违规端点数 / 实测指标），再决定要不要动手；⛔ 别把"消警告"当目标（容易改成更坏的方案）；**同签名 ≠ 同病** |
| C17 | 编码/文件 | **配置文件该不该带 BOM，由读取方决定** | JSON 不能带、`.ps1` 必须带、`.cmd` 不能带 —— 同一个"写文件"默认只满足一种；写完回读首字节验证 |
| C18 | 进程/托管 | **分离式启动会让守护/自愈变成摆设** | 托管者必须**前台持有**进程并**原样透传退出码**；"状态字段与进程列表矛盾"本身就是判据（指向多实例竞态/孤儿） |
| C19 | 跨层调用 | **外壳语法要固化进对应层的文件，不要在调用方拼字符串** | 引号/转义是**乘法关系**；报错指向不存在的语法问题时先怀疑多层转义 |
| C20 | 环境/命令 | **存在性检查 ≠ 可用性检查**（同名命令可能是存根） | 判断能力是否可用必须**实跑一次**；静默失败 + 无错误信息是高价值线索 |
| C21 | 验证方法论 | ⭐⭐⭐⭐ **"测试全绿"证明不了任何事 —— 必须证明判据会失败（变异测试）** | 分两层变异：**被测对象** + **判据自身**；自动还原 + 逐字节核对；每个历史缺陷都做成变异体；激励覆盖强度也要用变异再验一次 |
| C22 | 判定规则 | ⭐⭐⭐ **判定规则"太严"与"太松"都会错，错法相反** | 边界必须**双向验证**（应判失败 + 应判通过的样本各若干）；依赖**约定格式**（行首+冒号）而非"恰好没出现关键词"；判定的最终执行者也要端到端验证 |
| C23 | 变异测试 | ⭐⭐⭐ **看到"变异体没被抓到"先问：这个变异体真的违反规格了吗？** | LEAK 的第一诊断是"变异体弱"而不是"判据弱"；⛔ 别造"恰好等于限值"的变异（那合规）；改控制位不一定改行为；⛔ 不要为变异体量身定做断言 |
| C24 | 解析/国际化 | ⭐⭐⭐ **拿"人类可读文案"当解析锚点 = 把逻辑绑死在一种语言上** | 同一错误码在中/英文界面文案不同；正则只认中文 ⇒ 英文环境解析失败并**静默降级到兜底分支**（本例把"只锁一个模型"误判成"锁整个账号"）；⚠️ 降级后**台账字段为空**，从状态里看不出降级发生过 |
| C25 | 写入 API/数据 | ⭐⭐⭐ **写入型 API 的 `None`/缺省参数常是「跳过」语义，不是「置空」语义** | `openpyxl` 的 `cell(value=None)` 实测**不清除旧值**（`'TEST'` 还在）⇒ 批量重写时旧行数据**静默串到新行**（CH340X 拿到了 B5817WS 的参数）；行数/序号/总数全对，**只有逐字段 diff 才发现**；改数据前先备份 |
| C26 | 数据导入/生命周期 | ⭐⭐ **批量导入前必须按"生命周期状态"过滤：同一条目会同时存在"成功"和"关闭"两笔** | 导出文件里 `交易关闭` 的行**字段齐全、看不出异常**，直接入库会把**没成交的记成库存**；且按"名称+款式"去重会把"关闭/成功"两笔**错误合并** ⇒ 去重键必须含状态；⚠️ 状态空值多为**同一订单的续行**（继承上一行），不能一刀切 |
| C27 | 编码/字形 | ⭐⭐⭐ **"有码位" ≠ "有字形"：字体可以声明覆盖某码位却没有对应字形 ⇒ 渲染成"空框"** | 面向位图的输出（SVG→PNG、图表、PDF）里混用 **emoji 与中文**时必然踩：中文字体把 emoji 画成 `□`，emoji 字体把中文画成 `□`，**没有一种字体同时覆盖两者**。⚠️ **编辑器/浏览器预览走的是系统字体回退链，与最终渲染器不同** ⇒ 预览正常不代表产物正常。⭐ 判据必须**在最终产物上验** |
| C28 | 流程/协作 | ⭐⭐⭐⭐ **"关于外部/他人状态的判断"必须现查，⛔ 不能用上次的快照当事实** | 单人推进多人项目时，因"连续几轮没看到队友新提交"就把整轮排期建立在「队友没动」上（据此产出接管清单、任务顺序、分工表）⇒ 推送前 `fetch` 才发现远端已多 **8 提交 / 91 文件 / +9601 行**，**前提从头到尾就是错的**。⚠️ 最危险的是**本地一切自洽、只有 `fetch` 那一下才暴露** ⇒ 错误前提可能存活一整轮工作 |
| C29 | 测试工具/判据 | ⭐⭐⭐⭐ **"跳过"绝不能被算作"通过"** —— 测试框架必须把 `skipped` 当失败 | 变异测试的锚点字符串**在 RTL 重构后失配** ⇒ 该变异体变成 `skipped`，但脚本只统计"没抓到"（`Caught -eq $false`），**`skipped` 是 `$null`、不计入任何失败** ⇒ ⛔ **整个模块在无任何回归保护的情况下，被测出"全绿"**。⚠️ 指纹：**摘要里有 skipped 行，但结论仍是 PASS**。⭐ 通用化：任何"跳过/忽略/未执行"状态（`skip`/`xfail`/`pending`/`inconclusive`/`0 tests`）都必须**显式失败**，⛔ 否则"跑过了"与"没跑"无法区分 |
| C30 | 工具/判据 | ⭐⭐⭐ **退出码不是唯一判据：PowerShell 会把 native 程序的 stderr 判成失败** | `git push` 把**正常进度**写 stderr（`main -> main`）⇒ 当它**是脚本最后一条语句且带 `2>&1`** 时，pwsh 退出码被置 **1**，**覆盖掉真实的 `$LASTEXITCODE = 0`** ⇒ 成功被报成 `[exit code: 1]`。⚠️ **真假失败的退出码完全相同**，退出码本身无法区分。✅ 修法：命令后补一条语句（`; $null`）或**不写 `2>&1`**。⭐ 判定结果要看**独立权威证据**（`git rev-parse origin/main` vs `HEAD`），⛔ 不是退出码 |
| C31 | 工具链/前端 | ⭐⭐⭐⭐ **"能跑通"与"能被工具接受"是两回事：宽松前端会掩盖严格前端的拒绝** | 代码**长期测试全绿**（仿真器接受 SystemVerilog 专有语法），交给**更严格的前端**（综合器按 Verilog-2001 子集）时被**整个单元丢弃**：`Syntax error` + **`Module 'x' is ignored due to previous errors`** ⇒ ⛔ **该模块根本没进产物**，而测试报告仍是绿的。⭐ 危险在于**两者结论同时"看起来正常"**，只有真跑一次才暴露。⚠️ 识别"静默丢弃"签名：`is ignored` / `was skipped` / `not included` / `0 items` ⇒ 含义是**该单元没进产物**。📌 判据要落在**产物里有没有它**，⛔ 不是"我有没有提交它" |
| C32 | 判据/前端差异 | ⭐⭐⭐⭐ **"这段代码会执行"本身是一个假设 —— 退化/空转的代码在宽松前端下会静默消失** | 参数化组合逻辑在**默认参数下退化成纯常量赋值**（不读任何输入）⇒ 仿真器**根本不执行它** ⇒ ⛔ **模块"语法全对、编译无错"但功能完全没生效**（输出恒为初值）。⚠️ 而综合器会把常量块**正常折叠**、算出"正确"结果 ⇒ **同一份代码两个前端行为不一致**，"仿真通过"因此**失去意义**。⭐ 修法：⛔ 别用隐式敏感表（HDL 里即 `always @(*)`）表达组合逻辑，改用**连续赋值/显式依赖**；把"零依赖"警告当**错误**（它说的是"这段永远不会跑"，⛔ 不是"被优化掉了"） |
| C33 | 验证/时间语义 | ⭐⭐⭐ **"被测对象的时间语义"必须先确认，再写判据 —— 否则会去改对的代码** | 判据失败，⭐ **第一反应是"被测设计有 bug"，实际两处都是测试自己的错**：① 使能信号"拉高一段"就行 → 真相是**脉冲**，**宽度本身有意义**（拉长 N 倍 = 跑了 N 步）；② 拿"过渡过程"数值当判据 → 过渡值**取决于恰好跑了多少拍**、不可复现。⚠️ 若直接去改被测 RTL，会**把正确的代码改坏**，且"改完判据过了"还会**强化错误结论**。⭐ 纪律：判据失败先怀疑**我的时基/假设**；判据落在**稳态/平台**上；📌 **约定文档就是用来消除这类假设的，写 tb 前先读它** |
| C34 | 执行环境/产物 | ⭐⭐⭐⭐ **换"执行方式"会悄悄改变"工作目录"这个隐含前提 —— 而产物丢失是不报错的** | 给验收脚本补超时（前台调用 → 后台作业 `Start-Job`）后，**结果全绿**，但日志夹一行 `Unable to open build/sim/… for output` ⇒ ⛔ **证据静默丢失，而验收报告是绿的**（判定只看 `FAIL:` 行）。**根因**：⭐ 后台作业的**子进程工作目录 ≠ 原目录** ⇒ **相对路径**产物解析到别处。⚠️ 更值得记：该脚本**编译前**已扫源码预建目录（用项目根），但**运行期工作目录变了** ⇒ 预建目录没用上 ⇒ 📌 **"一个坑修过一半"比没修更危险**。⭐ 修法：作业里**显式 `Set-Location`**。📌 **"全绿" ≠ "证据还在"** ⇒ 验收脚本要**同时校验产物存在**；修完要跑**最脏**场景（先删产物目录） |
| C35 | 跨边界/结论传递 | ⭐⭐⭐⭐ **跨边界时"结论"不会自然继承 —— 而"取不到结论"默认会被当成成功** | 为"绕过工具限制"（后台作业拿不到子进程退出码）而**把退出码写死成 0** ⇒ ⛔ **任何"跑挂了但没打印 `FAIL:`"的情况都被判 PASS**，变异测试出现 `GREEN(LEAK!)` 时第一反应是"判据漏了"，实际是**结论在跨边界时丢了**。⭐ 通用化：凡是**结论要跨一层边界传递**的场合（子进程→父进程、作业→调度器、容器→宿主、远程→本地、异步回调→主流程、服务→调用方），都要问：**这一层能不能表达"没有结论"？** ⛔ 若默认值是"成功"，则"失败/未知"会**静默变成通过**。📌 与 `C29` 同族：**"跳过/取不到/未执行"绝不能被算作"通过"** |
| C36~C42 | — | ⚠️ **索引未补行**（历史遗留） | 见正文 `## C36.` ~ `## C42.`；已补记：`C43` |
| C43 | API 语义/误诊 | ⭐⭐⭐ **一个访问器可能同时代表"两个字段" —— 先证明"故障真的存在"再修** | `openpyxl` 的 `freeze_panes` getter 返回 **`topLeftCell`（滚动位置）**，而决定冻结行数的是 XML 里的 **`ySplit`**；三个版本 `ySplit` 恒为 1 ⇒ **"冻结 596 行"从未发生**，是**误诊被写进交接文档并代代继承**；⚠️ "修一个不存在的 bug"会形成永远修不完的假故障，且会掩盖真故障 |

---

## C01. 变量/信号名用了语言保留字（Verilog `buf`）

**现象**：声明 `reg [15:0] buf [0:63];` 编译报 `Syntax error in variable list`，代码看着完全正常。
**根因**：`buf` 是 Verilog **保留关键字**（缓冲门原语），不能作标识符。报错信息误导（报"语法错误"而非"保留字"）。
**解决**：改名（`buf` → `wbuf`/`data_buf`），立即编译通过。
**跨语言迁移（为什么进共性问题集）**：
- C/C++：`int class;` / `int new;` → 编译报错（class/new 是关键字）
- Python：`def` / `class` / `lambda` 不能做变量名（SyntaxError）
- MATLAB：`end` / `for` / `if` 不能做变量名（解析错）
- Shell：`if` / `then` / `for` 特殊字
- JavaScript：`var class = 1` → 语法错误
**经验**：① 报"看不懂的语法错误"时先查**该语言保留字表**；② 命名习惯上用前缀/后缀避雷（Verilog 用 `_r`/`_w`/`buf_` 前缀，其它语言类似）；③ 语法高亮若变量名变色 = 踩保留字（高亮器会当关键字着色）。

## C02. 开发环境通过 ≠ 真实环境行为一致（Verilog 仿真 vs 上板）

**现象（Verilog 具体案例 → 见 T01/L27）**：自写 64 点 FFT 在 iverilog 仿真 100% 通过（bin=16/mag=400），上板后 bin 乱跳、mag=5 万。换厂商 IP 后正常。
**根因**：仿真器（iverilog）把行为级 RTL 当"理想执行"；综合器（Gowin）把同一 RTL 映射到硬件（BSRAM 推断、initial→ROM、乘法优化）后行为可能不同。
**跨语言迁移**：
- 软件：单测通过 ≠ 生产环境 OK（Python 本机通过 ≠ 服务器无依赖；C 编译器优化开 O2 后未定义行为 UB 才暴露）
- 嵌入式：Keil/arm-none-eabi 编译 OK ≠ 目标板跑 OK（中断/内存对齐/字节序）
- Web：Chrome 正常 ≠ IE/Safari 正常（兼容性）
- 数据库：SQLite 查询 OK ≠ MySQL 行为（方言差异）
**经验**：① 开发环境是"草稿"，目标平台验证才是"定稿"；② 行为级/高层写法若在目标平台要"重新解释"，先确认映射语义（RAM/ROM/优化）；③ 快速区分：写"绕过外部依赖的最小目标平台测试"（隔离测试）而非继续在开发环境改。

## C03. 调试时一次只改一个变量（A/B 对比）

**现象（Verilog 具体案例 → T01）**：vpp 毛刺怀疑多个原因（PLL 失锁/跨时钟域亚稳/窗口长度），连续尝试多次修改效果混叠，无法判断谁有效。
**正确做法**：A/B 对比——回滚某个改动观察现象是否恢复原状：
- 回滚后现象回到"修改前状态" = 该改动是加重因素（基本确认）
- 回滚后现象不变 = 该改动无关（干净排除）
**跨语言迁移**：
- 软件：二分查 bug 时一次改一个条件；`git stash` 回滚对比行为
- 配置：改一个配置项测一次，别一次改 5 个（MySQL/nginx 调优经典教训）
- 电路：怀疑多个元件时先替换一个看现象，别同时换三个
**经验**：① 现象"变好/变坏"必须能归因到**单个改动**才有信息量；② A/B 对比（改→测→回滚→测）是最强归因工具；③ 记录每次改动的现象（含"无变化"），避免重复试。

---

## C04. "派生量/差值"出问题时 → 拆回原始分量分别观测（2026-09-10）

**症状**：某个"算出来的量"不稳定/偶发异常（例：峰峰值 = max − min、ΔT = t2 − t1、
差分电压、误差 = 实测 − 期望），但它的**两个来源量看起来都正常**。

**为什么难查**：**差值把"是哪一端出问题"这个关键信息吃掉了一半**。
`max−min` 跳了 32，你只知道"结果偏了"，不知道是 max 被抬高还是 min 被压低。
（本例中两簇差值恒定 32，若直接看差值只能猜；拆开后立刻看到"min 分两簇、差正好是一个 bit"。）

**做法**：
1. **临时改上报/输出**：把派生量替换成它的**原始分量**（分开两个通道/两个字段）
2. 直接观察：是哪一端被污染、污染成什么值、偏移量是否恒定
3. **偏移量恒定 = 强线索**：恒定 ⇒ 某个离散位/固定干扰；随机 ⇒ 噪声/时序

**通用性**：跨语言通用（Verilog 的 max-min、MATLAB 的差分、Python 的 `a-b`、
示波器的 ΔV/ΔT 全适用）。**只要出现"派生量异常、来源量正常"，先拆分量。**

**实例**：本次 `vpp=max−min` 长期两簇跳，拆成 `<max码>,<bin>,<min码>` 上报后，
30 秒内定位到 `min` 两簇、差值 = **32 = bit5** → 根因是 ADC 的 bit5 手工飞线信号完整性差。
（详见 `fpga/FPGA逻辑调试经验集.md` L30）

---

## C05. 配置与注释/文档"漂移"——配置类事实一律查源文件（2026-09-10）

**症状**：代码/文档里写的参数值，和工具**实际生效**的值不一致。

**本次实例**：`top_system.v` 注释写 `PLL 180°`，实际 `gowin_pll.ipc` 里是 `270°`，
且该配置改了一天多都没提交 git → 基于注释做的一整轮时序分析**全部跑偏**。

**通用规律**：**由 GUI/向导生成的配置，不会同步写进你手写的注释，也未必提交版本控制。**
（Verilog 的 IP 向导、STM32 的 CubeMX、MATLAB 的 App、IDE 的编译选项……全都一样。）

**纪律**：
1. 排查**先查"实际生效值"**：配置文件 / 生成物 / 编译产物，而不是注释和文档
2. 用 `git status` 看这些"生成类文件"**是否改了没提交**
3. 改完配置**当场回写注释并 commit**，或直接注明"以 XX 文件为准"
4. **结论一旦建立在某个假设上，先验证那个假设本身**（本例若一开始就查 .ipc，能省一轮）

---

## C06. 「局部性检验」——区分"近场耦合"与"分布式耦合"（2026-09-10）

**症状**：怀疑"某个干扰源耦合进了敏感信号"，但干扰源在物理上遍布整块板/整条总线，**没法逐个拔掉**。

**本次实例**：怀疑 ADC 某条数据位被 DAC 数据线的串扰影响。
做法：在 RTL 里给 DAC 数据位做**逐位冻结开关**（置 0 后该位停止翻转 = 关掉这个干扰源），
一次运行循环 5 档对照：

| 档 | 冻结谁 | 目的 |
|---|---|---|
| L0 | 不冻 | 基线 |
| L1 | 与 `ad_data[5]`@P17 **同一 IOB tile / 紧邻**的两位（`da_data[4]`@N17 + `da_data[2]`@R17）| 近场源 |
| L2 | **远端**位（`da_data[8]`@Y18）| 远端对照 |
| L3 | 另一位（`da_data[1]`@F20，与 `ad_data[3]`@F19 同 tile）| 第二组近场 |
| L4 | 全部冻结 | 自检（应≈0） |

**判据**：冻**相邻源**改善 ≫ 冻**远端源** → **近场（局部）耦合**；
两者改善相当 → **分布式耦合**（模块内共阻抗 / 电源地 / 整条排线）。
→ 本次实测：**连 L4（冻结整条 DAC 总线）也只改善约 15%** ⇒ DAC 侧**不是主因**，
更谈不上"某两条线"的近场耦合；剩下 85% 得往别的方向找
（后来查明是**采样建立裕量**，与 DAC 无关 —— 这正是分簇的价值，见 C07）。

**通用化**：同一思路也用于判断"问题在本地还是在外部依赖"——
把本地环节**逐个开关**（或替换成等价的外部实现），看改善到底来自哪一侧。

**⚠️ 前提（否则会自欺）**：**开关本身不能改变被测对象**。
本次的教训是：屏蔽 **ADC 侧**某一位会在每个 `2^N` 边界**自造** `~(2^N−1)` LSB 的跳变（L34），
高位档指标被污染，只有 `2^N−1 < 阈值` 的低 4 位是干净档；
所以"局部性检验"要选**开关不会自造故障的那个维度**（冻 DAC 侧不会造出 ADC 侧的跳变，反之会）。

**关联**：`FPGA逻辑调试经验集` L33 / L34；TROUBLESHOOTING 44 号

---

## C07. 同一现象里可能叠加多个独立机制 → 先按"依赖条件"分簇再归因（2026-09-10）

**症状**：同一批"坏数据"，改 A 有效、改 B 也有效、再改 C 又变回原样，**结论反复**，每天都在推翻昨天。

**本次实例**：那一批"vpp 毛刺"其实是**两个独立机制**：
- **M1**：`ad_data[5]`（手工飞线到 P17）间歇翻转，**依赖** DAC 数据是否在翻转（近场耦合型）
- **M2**：多位同时翻转时的"新旧位混合码"，**不依赖** DAC（采样建立裕量型）

两者长期混在同一个"毛刺"里讨论 → 每轮实验只看到**混合后**的效果，谁也没法定案。

**分簇标志（关键）**：**某条件下 A 消失而 B 仍在**。
本例：把 `ad_data[5]` 强制为 0 → min 码那两簇（差 32）**合并了**（M1 消失），
但 `max≈848 / min≈384` 的第二类离群**仍在**（M2 仍在）→ 一刀把两个机制切开。

**做法**
1. 先列出所有候选"依赖条件"（是否翻转、是否跨中码、是否高温、是否只在某幅度…）
2. 做一轮**分簇实验**：找出"哪一类现象随哪个条件消失"
3. 再对每一簇**单独归因**；**不要**在同一次讨论里让两簇共用结论

**代价对比**：不分簇 = 每天反复推翻自己；分簇 = 多花一次实验，之后每次结论都可靠。

**关联**：`FPGA逻辑调试经验集` L35 / L45；TROUBLESHOOTING 44 / 45 号

---

## C08. 证据形态要匹配问题类型：判"有没有坏"用统计量，判"坏成什么样"必须用**原始序列**（2026-09-10）

**症状**：手上一堆均值/极值/计数，却怎么也定不了案。

**本次实例**：为定"偶发坏值"的根因，整天在用两个数——
`vpp`（1024 点 max−min）和 `glitch_count`（相邻差 >16 LSB 的点数）
→ 只得到"速率约 0.3~0.5%""幅度落在位权重上"，**定不了"坏点长什么样"**。
改成用串口抓 **1024 点原始采样序列** → **30 秒**看清四件事：
"只出现在中码穿越处 / 是逐位混合的整齐码 / MSB 最后稳定 / 坏码也成对重复" → 根因定位。

**为什么聚合量不行（数学原因）**
1. 均值/极值/计数是**单点事件的压缩映射**，形状信息在压缩时就已经丢了；
2. 阈值型计数还有**选择性偏差**：`>16 LSB` 只看得见 MSB 级事件。
   本例按原始序列做拟合残差，**真实异常率是 2.7~3.7%，是计数值的 5~10 倍**。

**纪律**
1. 动手前先问：**"我要的证据形态是什么？"**（速率？形状？位置？相关性？时序关系？）
2. 需要"形状/位置"类证据 → **必须拿到原始序列/原始帧**（回传慢也要拿：1024 点 @500kbps ≈ 0.12s，完全可接受）
3. 拿到原始数据后，用**独立方法取真值**（本例：正弦最小二乘拟合的幅度 2A），
   把"**读数误差**"和"**故障本身**"分开算 —— 见 C11

**关联**：`FPGA逻辑调试经验集` L40；`FPGA工具使用经验` U28；共性问题 C11

---

## C09. 标称值 ≠ 实测值：作为前提的量必须先实测（或从数据特征反推）再用于计算（2026-09-10）

**症状**：每一步推导都"很严密"，但结论与实测**正好相反**。

**本次实例**：PLL IP 向导里 `ClkinClockFrequency` 填了 **100MHz**，而板载晶振实际 **50MHz**
→ `fout = fclk_in × MDIV / (IDIV × ODIV)` = `fclk_in / 2`
→ 两路输出**静默减半**（期望 50MHz，实际 25MHz）。
后果：按 50MHz 算出的"采样相位 270° 裕量 10ns、是**最优点**"；
按真实 25MHz 算，裕量只有 **5ns、是**最差点**。**一个前提错，结论从最优反转成最差**——为此绕了一整天。

**特征**：这类错误**不报任何错**（综合/布线零 error 零 warning），只能靠实测发现。

**反推技巧（本次真正管用的）**：看数据本身的**结构特征**：
> 每个源样本在缓冲里出现 2 次 ⇒ `v[2k]==v[2k+1]` 比例 ≈100%，且**相同值游程长度全为偶数**
> ⇒ 采集时钟 = 缓冲写速率的一半 ⇒ 25MHz。
**不需要任何额外仪器，比直接测时钟更可靠。**

**纪律**
1. 时钟频率、晶振、分压比、量程、采样率这类"计算前提"，**实测或用数据特征反推**后再用
2. **能算出来的就别只测**：PLL 输出频率由参数直接决定，先算出来做交叉验证
   （本例示波器鳄鱼夹读到 14.6ns，与算术 40ns 矛盾 → 判为**测量无效**，见 U30）
3. **结论建立在某个标称值上时，先验证那个标称值本身**（与 C05 同源）

**关联**：`FPGA逻辑调试经验集` L39；共性问题的 C05；`FPGA工具使用经验` U30

---

> **新实例（2026-09-11）**：`gowin_pll.ipc` 声明输入 **100MHz**，而板上实物 **50MHz** ——
> 声明值不是「小误差」，它直接决定 PLL 的实际 VCO 频率（≈350MHz），而数据手册要求 ≥700MHz
> ⇒ **规格外运行**。教训：**生成物里的「声明参数」必须当成「待实测的前提」**，
> 代进公式算出实际值、再用器件手册的边界指标卡一遍。详见 TROUBLESHOOTING 57 号 / 工具集 U34。

## C10. "没测到异常"必须先确认激励真的到达（空测试陷阱）（2026-09-10）

**案例**：FPGA 采集链在排"偶发坏值"。某一轮抓了 3 帧，判据全部为 0（工具还打印了"★ 这一帧数据是干净的"）。
**实际是信号源没开输出**，ADC 输入悬空 → 只有 ±7 LSB 的噪声围绕中码。
而该故障**只在"多位同时翻转的码边界"触发**（中码 511↔512 时 10 位同翻）
→ 噪声带里根本没有触发条件 → 这次"干净"**完全无效**，一度以为问题已经好了。

**识别方法（廉价、必做）**
- 用**与主判据无关的独立量**做"存在性检查"：本例对整帧做定频正弦拟合，
  100kHz 成分幅度只有 **0.67 LSB**，而正常应为 **209 LSB** → 3 秒判定"输入根本没进来"。
  （比"看波形对不对"更硬：直接给数字。）
- 反问三句：**激励开了吗？到达被测点了吗？强度够触发该故障吗？**
  第三句最易被忽略 —— 有些故障需要特定条件才出现，**激励"存在" ≠ "触发条件存在"**。

**其他领域的同型陷阱**
- 接口测试没报错 → 请求真的发出去了吗？
- 性能测试没变慢 → 负载真的打上去了吗？
- 协议解析没异常 → 对端真的在发吗？
- 单元测试全绿 → 测试真的跑到那行代码了吗？（覆盖率为 0 也可能全绿）

---

## C11. 判据要用"独立于被测量的真值"（2026-09-10）

**案例**：要判断"采集数据里有没有坏点"，一开始用 `max−min`（峰峰值读数）当判据 ——
但 `max−min` **本身就是被坏点污染的量**（实测 398，真值 106），
用它做判据是循环论证；而且它把"信号真实峰值、噪声尖峰、坏点"三者混在一起，无法归因。

**改法**：另开一条**独立通道**取真值 —— 对整帧做**正弦最小二乘拟合**
（扫描周期 + 解 `[1, sin, cos]` 三参），得到与读数无关的真实幅度 `2A` 与残差 σ：
- 读数与 `2A` 之差 = **测量链的误差**（本例 +275%）；
- 残差里的大偏离（成簇出现）= **故障本身**（本例 3.7%）。

两者分开后，"读数准不准"和"数据坏不坏"才能分别判、分别修。

**通用规则**：给测量链做验证时，**先问"我的真值从哪来"**。
可用来源：外部标准仪器、模型拟合、第二种测量原理、已知激励的解析值。
**绝不能用被怀疑的那个量自己去证明自己。**

---

## C12. 任何"过阈值就触发"的判据都必须留死区（迟滞）（2026-09-10）

**案例**：过零计数**偶发**偏高。同一批 4 帧数据里 **2 帧完全正确**，另外 2 帧报 **+6.8% / +14.4%** 的误差，
而正确性判据是 ±5% → **直接判失败**。

**机理**：单点阈值判据 `(上一拍 < TH) && (本拍 >= TH)`。
当信号**朝远离阈值的方向**穿越时（本例是下降穿越），噪声只要把某一拍推回阈值另一侧，就多算一次事件。

**定量（数值规律本身就是证据）**
```
设窗口里混进 N 个假事件 → 16 个触发间隔只覆盖 (16−N) 个真周期
  avg = (16 − N) × P / 16
  N=1 → 468.75（实测 468 ✓）     N=2 → 437.5（实测 437 ✓）
⇒ 看到 468/437 就能排除"时钟/信号源不稳"，直接锁定"多算了 1~2 个边沿"
```

**通用规则**：任何"过阈值就触发"的判据，先问一句 **"阈值附近抖动会怎样？"**
修法一律是**加死区（迟滞）**：进入用高门限、退出用低门限，并用**状态**（而不是"上一拍"）记忆当前在哪一侧。
代价：幅度小于 2H 的信号不再触发 → **H 必须按"最小可用信号"反推**。

**同型场景**：过零检测 / 按键消抖 / 比较器回差 / 控制环的开关点 / 软件浮点相等比较 /
          重试与告警的触发门限 / 缓存命中率抖动阈值 / 抖动（jitter）测量。

**关联**：`FPGA逻辑调试经验集` L46；`FPGA工具使用经验` U31；TROUBLESHOOTING 54 号

---


## C13. 失去真实系统之后：把"验证"分成强度递减的层，并标注每层的证据强度（2026-09-11）

**背景**：项目做到一半，目标系统不可用了（本例：板子之后无法再上板）。
此时最大的风险不是"没法继续做"，而是**把弱证据当强证据**——"仿真过了"被说成"功能好了"。

**做法：按"离真实系统多近"分层，每层说清能证明什么、证明不了什么**

| 层 | 能证明 | 证明不了 |
|---|---|---|
| ① 单元/模块 TB | 该模块在给定激励下的行为（判据最好是数学性质） | 顶层接线、跨模块握手、真实接口时序 |
| ② **整机端到端联仿**（真实接口时序 + 打桩顶掉不可仿的第三方） | "命令→数据→文本格式"整链；**跨设备约定**（协议/格式） | 与真实器件的电气差异；**综合后**行为 |
| ③ 上位机离线回放（合成**真实格式**的数据） | 解析/算法/绘图层；格式兼容性 | 真实数据里的噪声与畸形样本 |
| ④ 静态复核（读代码/常量/注释） | 明显的不一致（端口名、位宽、参数） | 任何动态行为 |

**三条纪律**：
1. **每层都要有"独立于实现"的判据**（如"循环斜坡帧内相邻差 ≡ D (mod 16)"）——
   否则改动一小处就得重写测试，等于没有回归。
2. **报告里必须写明证据层**：`[板上实测]` 与 `[仿真验证]` 严格区分，逐条标注来源。
   本例的验收口径文档就是按这个原则写的（哪些是实测数字、哪些只有仿真）。
3. **把"暂时不可验证的部分"写成待办 + 需要的最小动作**（本例：“假穿越闭环需一帧 1kHz 原始数据，
   发 `R`”）——不让它消失在“应该没问题”里。

**同型场景**：云端服务的本地 mock、硬件驱动的软件模型、协议实现对着 RFC 单测、
         数据库迁移在影子库预演、任何"目标环境暂时拿不到"的工程。

**关联**：`fpga/TROUBLESHOOTING.md` 56 号；`fpga/FPGA工具使用经验.md` U32
## C14. 负向测试"故意打 ERROR" 与 "扫 ERROR 日志判失败" 直接冲突 → 让被测者给出**显式结论**（2026-09-17）

**症状**：验证体系里有一类测试**必须故意失败才能证明有效**（负向测试：断言自检、错误注入、
故障注入、异常路径、边界/权限用例）。而最常见的"跑完扫日志，出现 `ERROR` 就判失败"的自动化写法，
会把这类**预期内的错误**判成失败。于是两条路都被堵死：
① 索性不写负向测试（怕流水线变红）→ **没人知道断言到底会不会触发**；
② 为了过流水线把 `ERROR` 字样删掉或改成别的词 → **断言丧失可读性**，等于把报警灯涂黑。

**正解（约定优先于脚本）**
1. ⭐ **由被测者给出显式结论**：每个测试自己打印 `PASS: …` / `FAIL: …`，
   失败时用**非零退出码**结束（`$fatal(1,…)` / `sys.exit(1)` / `assert`）。
2. 运行器只认 **【退出码】+【结论标记（行首 `FAIL`）】**；
   `ERROR` / `WARN` 行**只计数与展示**，不参与判定（它们属于"证据"，不属于"结论"）。
3. **把这条约定写进验收文档**，否则下一个人一定会把"扫 ERROR"加回来
   （因为它看起来更"严格"，实际是**用错了层**）。

**为什么值得单列**：这是"判据形态"问题，与 `C08` 同源 ——
**"日志里有没有 ERROR" 是给人看的证据，"过没过" 是结论**；两者混在一个判据里必然出错。
一句话：**证据（evidence）与判定（verdict）必须分开表达。**

**附带收益**：分开之后，负向测试的输出反而变成**最有价值的证据**——
日志里那几行"预期内的 `ERROR`"正是"断言确实会触发"的证明，可以留档进回归报告。

**同型场景**：pytest 的 `xfail` / `pytest.raises`；编译器的 expected-error 测试（`#error` / `-Werror` 白名单）；
混沌工程与故障注入；安全测试里"故意触发告警"；压测里"故意制造超时"；lint 的 baseline 豁免；
以及任何"预期会失败"的用例与"看到失败就报警"的调度器共存的系统。

**关联**：`C08`（证据形态要匹配问题类型）、`C10`（空测试陷阱）、
`fpga/FPGA逻辑调试经验集.md` `L52`（断言必须先证明会触发）、`fpga/TROUBLESHOOTING.md` 60 号

---

## C15. 两份"都能工作"的参考互相矛盾时：用**结构判据 + 宽容性实验**收敛（2026-09-17）

**场景**：同一个第三方器件（或协议、寄存器定义），两份**都声称验证过**的实现给出不同参数
（极性 / 对齐 / 帧长 / 时序）。抄哪一份都可能错，而且**看波形看不出来**。

**做法（三步）**
1. ⭐ **先分清"哪一类参数"**：
   | 类别 | 例子 | 处理 |
   |---|---|---|
   | ① 决定"能不能工作"的 | 位序、数据对齐、时隙长度 | **必须搞对**，否则功能全错 |
   | ② 只决定"哪个是哪个"的 | 左右声道、引脚/通道映射 | 留成参数，最坏只是换个名字 |
   | ③ 纯实现风格 | 用 PLL 还是分频器、状态机怎么写 | 不影响正确性，按自己约束选 |
2. ⭐ **回到结构判据**：不看结论、看实现 —— "数据占哪几位 / 计数往哪个方向 / 时隙多长"这些**结构**
   能反推出真实格式（本次正是靠结构把 LSBJ 右对齐定死的）。
3. ⭐ **找"宽容性证据"界定范围**：有没有"**额外的空闲位 / 多余的时钟照样工作**"这类事实？
   本次找到"某已验证驱动在每声道时隙里塞了 8 个空闲 BCK 仍正常" ⇒ 说明**边界比想象的宽**，
   于是不必在"32 fs 还是 64 fs"之间二选一。

**通用化**：任何"两份权威文档/实现打架"的场合（两个版本的协议栈、两家厂商的寄存器定义、
两个 SDK 的初始化序列、两个人的同一份规格）。⭐ **别投票，去读结构；别猜边界，去找宽容性证据。**

**关联**：`C05`（配置与注释漂移：以生效值为准）、`C09`（标称值≠实测值）、
`fpga/FPGA工具使用经验.md` `U51`、`fpga/FPGA逻辑调试经验集.md` `L53`

---

## C16. 警告/异常的**严重性由下游结果指标裁决**，⛔ 不由它自己裁决（2026-09-17）

**场景**：同一个工具告警签名（高云的 `PR1014`：时钟走了通用布线）在两次事件里都出现：
一次是"**板子全死**"事故的伴生现象；另一次同样出现、**完全无害**（同一个设计**每次构建都有它**）。
⇒ 若按"看到这个警告就断定坏了"处理，两次里有**一次必然判错**。

**可迁移的做法**
1. ⭐ **告警本身不是判据**：先找**下游结果指标**再下结论。
   本次的指标 = ① 全局时钟表里时钟的等级（`PRIMARY`？）② 时序违规端点数 ③ 实测 `Actual Fmax` ④ 引脚表逐脚核对。
2. ⭐ **把"结果指标"写进排查清单**，而不是把"警告消失"当目标 ——
   ⚠️ 追求"消警告"很容易改成**更坏的方案**（本次若为消警告去挪时钟脚 ⇒ 板子直接不工作）。
3. ⭐ **能查成因就去查**（用工具自己的输出、数据手册的边界值、结构判据）：**"已知成因 + 有界的后果"才叫结论**；
   查不出成因的警告只能标 `[未确认]`，⛔ 不许当"没问题"糊过去。
4. ⭐ 与 `C13` 配合：**给每条结论标注证据强度** —— "工具打了条警告"是**弱证据**，
   "时序报告里的数字 / 实测波形"才是**强证据**。
5. ⭐ **同签名 ≠ 同病**：签名只说明"现象相似"，根因要各自查（本次一个根因是**缺约束文件**，另一个是**板载时钟脚非专用脚**，风马牛不相及）。

**通用化**：编译器/链接器/静态检查/CI 的告警，日志里的异常栈，体检报告里的"偏高"指标 ——
**先问"它的下游可观测后果是什么、有没有超标"，再决定要不要动手**。

**关联**：`C13`（验证分层与证据强度）、`fpga/FPGA工具使用经验.md` `U23` / `U52`、`fpga/TROUBLESHOOTING.md` 62 号

---

## C17. 配置文件该不该带 BOM，由**读取方**决定，不由写入方决定（2026-09-17）

**场景**：一次部署里同时存在三类"程序 A 生成、程序 B 读取"的文件：JSON 配置、PowerShell 脚本、批处理脚本。
用同一条"写文本文件"的命令（默认 **UTF-8 with BOM**）产出，结果三种下场：

| 文件 | 读取方 | 带 BOM 的后果 |
|---|---|---|
| `.json` | Go `encoding/json` 等严格解析器 | ⛔ 直接报 `invalid character '\ufeff' looking for beginning of value`，服务起不来 |
| `.ps1` | Windows PowerShell 5.1 | ✅ **必须**有 BOM —— 否则按本地代码页（中文 Windows = GBK）解码，中文注释/字符串全乱码 |
| `.cmd` | `cmd.exe` | ⛔ **不能**有 BOM —— 否则第一行被当成非法命令 |

⇒ 三类文件三种要求，而"写文件"的默认行为**只满足其中一种**。

**可迁移的做法**
1. ⭐ 写"给别人读的"文件前，先确认**读取方的解码假设**（是否容忍 BOM、按哪个代码页解），再决定编码与 BOM。
2. ⭐ **把 BOM 显式化**：不要依赖语言的 `Set-Content` / `Path.write_text` / 重定向默认值 ——
   用能明确指定"带 / 不带 BOM"的 API（如 `UTF8Encoding($true/$false)`）。
3. ⭐ 写完**回读首字节验证**：UTF-8 BOM = `239,187,191`。别用"文件能打开 / 编辑器显示正常"当通过判据
   （编辑器会自动吞 BOM，观感与字节真相不一致）。
4. ⚠️ 编码问题的**症状会伪装成内容错误**：报"JSON 语法错误"、"批处理命令不存在"、
   "配置项解析失败"，其实根因在字节层。看到"文件内容明明没问题却报语法错"先查 BOM。

**通用化**：一切跨程序边界的文本产物（配置、清单、脚本、数据交换、生成代码）——
**编码契约归读取方所有**；跨语言/跨平台边界处必须显式声明，不能靠双方默认值碰巧一致。

**关联**：`C05`（配置类事实一律查源文件 —— BOM 属于"源文件事实"）、`C08`（判"字节对不对"要看原始字节，不看渲染）

---

## C18. 分离式启动会让"守护 / 自愈"变成摆设：托管者必须**前台持有**进程（2026-09-17）

**场景**：把常驻服务交给"登录时自启 + 崩溃自动重拉（`RestartCount=N`）"的计划任务托管。
启动脚本里用了**启动即返回**的写法（不等待子进程，例如 `Start-Process` 不带 `-Wait`）。结果：

- 启动脚本**立刻正常退出**（退出码 0）⇒ 宿主（计划任务）认为"任务成功结束"
- 服务进程被留下，但父链上的宿主**已消失** ⇒ 它是**孤儿**，不受任何托管
- ⛔ 配好的"失败自动重拉"**永远不触发**：宿主视角里任务从未失败；而服务真崩了也没人知道
- ✅ 上游修复：启动器改为**前台等待** + 把子进程退出码**原样返回**，自愈才真正生效

**最坑的地方（值得单独记）**：现场表现为 `任务状态 = Ready` 与 `进程列表 = 服务在跑` **同时成立**。
这两个信号看起来互相矛盾，会把排查方向**误导到配置错误**上（去查任务定义、触发器、权限），
而真实原因是**上一轮的多实例竞态留下了孤儿进程链** —— 与配置无关。

**可迁移的做法**
1. ⭐ 托管者判断"活没活"的依据是**它自己启动的那个进程**。
   所以托管脚本必须**前台等待**它，并**原样透传退出码**；否则信息链被切断。
2. ⭐ **自愈策略的价值 = 退出码的保真度**。分离式启动把"被托管者的状态"换成"启动动作是否成功"，
   于是重拉/重启/告警全部退化为装饰。
3. ⭐ 排查归属用**进程树父链**，不要只看"进程还在不在"：
   父进程已消失 = 孤儿，归零重来。四层链（宿主 → 脚本宿主 → 外壳 → 目标）逐层核对最直观。
4. ⭐ **"状态字段与进程列表矛盾"本身就是判据**：它指向**多实例竞态 / 孤儿残留**。
   先彻底清干净（停服务 → 确认无残留进程 → 再启动），⛔ 不要在脏状态上继续叠加改动 ——
   在脏状态上做的每次修改，结论都不可信（同 `C03`）。
5. ⚠️ 停止/重启请统一走**同一个入口**；手搓"停止 + 启动"两条原语连发容易撞出上面那种竞态。

**通用化**：systemd / supervisor / CI runner / Docker 的 `restart:` / k8s 探针 ——
凡是"宿主托管子进程"的结构，**前台持有 + 退出码保真**都是自愈能工作的前提。

**关联**：`C03`（一次只改一个变量）、`C05`（查实际生效的配置）、`C19`（同一件事的另一个"外壳层"教训）

---

## C19. 跨层调用时，把**外壳语法固化进脚本文件**，不要在调用方拼字符串（2026-09-17）

**场景**：需要把某程序的 stdout + stderr **合并**重定向到一个日志文件。
在调用方（PowerShell）里拼 `cmd /c "..."` 形式的命令行 ⇒ 触发**两层解析器的引号规则互相破坏**，
报出与真实问题**毫无关系**的错误：`文件名、目录名或卷标语法不正确`（但路径确实存在）。

三次尝试的失败路径（失败路径本身是经验）：
1. 直接 `Start-Process -RedirectStandardOutput/-RedirectStandardError` → 只能分两个文件；
   要么两个日志、要么另想办法（不满足"合并成一个文件"）。
2. 调用方拼 `cmd /c "... >> log 2>&1"` → 引号被两层解析，报路径格式错（**症状指向错误方向**）。
3. ✅ **把重定向写进一个 `.cmd` 文件**，调用方只调用"这个文件" → 引号只经过一层解析，立刻正常。

**可迁移的做法**
1. ⭐ 当语言 A 要使用语言 B 的**外壳（shell）语法**时，**把 B 的语法写进 B 的文件里**，
   调用方只调用"这个文件"，不做字符串拼接。
2. ⭐ **判据**：若一条命令的报错**指向一个不存在的语法问题**（路径明明存在却报"路径格式不正确"、
   参数明明传了却报"缺少操作数"），**先怀疑多层引号转义**，而不是去改路径/参数。
3. 同类"外壳里的外壳"场景一律适用：`sh -c`、`bash -c`、`powershell -Command`、
   CI 配置里的 `run:` 字段、`docker exec`、计划任务的"操作"字段。
4. ⭐ 附带好处：重定向、环境变量这类**运行契约跟着文件走**，人和工具看到的是**同一份事实**，
   而不是散落在调用参数里（对"下次谁来看这个部署"极友好）。

**通用化**：跨层调用（语言 → shell → 程序）的**引号/转义是乘法关系**，每多一层就多一次解析。
把层数压到最少，或把语法**下沉**到对应层的文件里，是比"多打几个反斜杠"可靠得多的解。

**关联**：`C01`（报错信息误导）、`C05`（事实查源文件）、`C18`（同为托管链路上的坑）

---

## C20. 同名命令可能解析到"存根"而非真实实现：**存在性检查 ≠ 可用性检查**（2026-09-17）

**场景**：程序按**名字**调用某解释器（约定名 `X3`，如 `python3`）。
`Get-Command X3` / `which X3` **能找到**、路径看着正常（甚至就在用户目录下的"应用"路径里），
但**一执行就失败，且常常不给任何错误信息**。真实实现装的是**另一个名字**（`X`）。
Windows 应用商店的**应用执行别名（App Execution Alias）** 会放一个同名存根，是这类陷阱的常见来源。

**可迁移的做法**
1. ⭐ 判断"某能力是否可用"必须**实际执行一次**（`--version` / 最小用例 / 探针请求），
   ⛔ 不能只查 PATH、注册表、包列表、`which` 的返回值。
2. ⭐ 把"解释器 / 工具路径"做成**可注入配置**（环境变量覆盖）优先于改 PATH。
   若上游已提供这类逃生口（例如"用环境变量指定解释器名"），**优先用配置**：可移植、可回溯、不污染全局。
3. ⭐ 排查"静默失败"时，把"命令**是否存在**"与"命令**是否成功**"做成**两条独立检查**
   （与 `C10` 同源：存在性检查要用**与被测量无关的独立量**）。
4. ⭐ **错误信息为空 / 被吞掉**是高价值线索：存根常直接退出、不打印任何东西。
   此时**手动原样跑一次**那条命令，比继续读上层日志更快定位。
5. ⚠️ 别把"用配置指定名字"当成已修复 —— 要**验证配置真的生效**（跑一次并确认成功），
   否则只是把同一个坑挪到了配置文件里。

**通用化**：`python`/`python3`、`node`/`nodejs`、容器里 `sh`/`bash`、
`gcc`/`cc`、包管理器的 shim 目录（scoop/asdf/pyenv）—— **shim 层的存在就是为了骗过存在性检查**。
凡按名字调外部程序，就应假定"名字 → 实现"这条映射**可能被劫持**，用**实跑**兜底。

**关联**：`C10`（空测试陷阱：先确认激励/能力真的到达）、`C09`（标称值 ≠ 实测值）、`C05`（配置类事实查源文件）

---

## C21. ⭐⭐⭐⭐ "测试全绿"证明不了任何事 —— 必须证明**判据会失败**（变异测试）（2026-09-17）

**场景**：一个**没有硬件**的项目，所有验收结论都来自自动化测试。
测试套件"全绿"被当成"做完了"。但同一批判据在几天内被查出**四类自身缺陷**：
① 阈值笔误（`> 100000`，而实际最大值只有 69632）⇒ **恒 FAIL**，没人发现是因为从没跑过；
② **覆盖缺口**：一个 tb 声称覆盖 6 个输出，实际有 2 个**既没接进测量也没断言**（"8/8"给人已覆盖的错觉）；
③ **判据结构性不敏感**：验收模型"检测到时钟沿后打一拍取数"⇒ 对"数据在哪个边沿变化"**完全不敏感**，
   于是"逐点一致 + 单谱峰 + SNR 93 dB"全绿，而**上板必然抢跑**；
④ 判定正则**双向都错**（见 `C22`）。

⇒ ⭐ **核心结论**：**"所有测试都通过"本身不能证明任何事** ——
如果判据根本不会失败，那么"全绿"与"没测"**没有区别**。

**可迁移的做法：变异测试（mutation testing）**
1. ⭐ 拿**真实被测对象**，每次**只改一处已知缺陷**，然后跑整条回归；**要求它必须变红**。
   仍然全绿 ⇒ 说明套件对该类缺陷**有盲区**（而不是"缺陷不存在"）。
2. ⭐ **分两层变异**：
   · 第一层：变异**被测对象**（逻辑、时序、极性、位序、截断方向…）；
   · 第二层（更关键）：变异**判据自身**（禁用某条断言、把阈值改坏、让模型读旧值）——
     若"检查器坏了而它的自检仍通过"，那**自检全绿也是假的**。
3. ⭐ **自动化 + 自动还原 + 逐字节核对**：脚本备份 → 注入 → 跑回归 → 还原 → **比对字节**，
   还原失败就**显式报错退出**（⛔ 绝不能把变异留在工作区）。
4. ⭐ 把**每个历史缺陷**都做成交互式变异体：这样"修完了"才等于"**有判据守着它不再回来**"。
   本项目的 13 个变异体全部被抓（含 `R-03`/`K-01`/`K-03`/`K-42`/`R-08` 等历史缺陷）。
5. ⭐ 覆盖强度也要测：**激励只覆盖一小片值域时，某些缺陷天然抓不到**
   （例：只测 64 个正弦值 ⇒ "某一位恒错/截断取错方向"可能蒙混过关）。
   ⇒ 补**全值域随机 + 逐位走查 + 边界值**三种激励，并**再变异一次**确认新 tb 真的抓得到。

**通用化**：单元测试、静态检查规则、CI 门禁、监控告警、类型系统 ——
任何"判定对错"的机制，其价值都取决于**它在错误发生时会不会真的报错**。
**没有变异测试的"全绿"只是没跑过而已。**

**关联**：`C14`（证据与判定分开）、`C16`（严重性由下游指标裁决）、`C13`（证据强度分层）、
`fpga/TROUBLESHOOTING.md` 63 号（判据结构性不敏感的实例）

---

## C22. ⭐⭐⭐ 判定规则"太严"与"太松"都会错，而且错法相反（2026-09-17）

**场景**：回归脚本用一条正则从测试日志里判"这个用例失败了吗"。
原规则 `^\s*FAIL`。**放宽**与**收紧**各错一次，且症状完全不同：

| 版本 | 规则 | 错在哪 | 症状 |
|---|---|---|---|
| 原版 | `^\s*FAIL` | 要求 `FAIL` 前**只有空白**；且 `\s` 在**多行模式含换行**（可跨行匹配） | **太松的一侧**：tb 若打印 `"  ⛔ FAIL: …"`（前面有装饰符）⇒ **静默漏判**（该失败却报通过） |
| 放宽 | `^\s*.*FAIL` | 行内**任何位置**出现 `FAIL` 都算 | **太严**：日志里讲"这一档本来就应该失败"的**说明性文字**（`<- MUST FAIL`）被当成失败 ⇒ **假失败**（用例其实 PASS） |
| ✅ 定稿 | `^[ \t]*[^A-Za-z]*FAIL:` | 行首允许**空白 + 非字母装饰符**，且必须带**冒号** | 两个方向都对：`⛔ FAIL:` 抓到；`MUST FAIL` 不抓 |

**可迁移的做法**
1. ⭐ **判定规则的边界必须双向验证**：造**应当判失败**的样本 + **应当判通过**的样本各若干，
   逐条确认（尤其"说明性文字里出现了关键词"这种**假阳性**样本，最容易漏做）。
2. ⭐ **"关键词出现在日志里"与"该用例失败"是两件事**：判定要依赖**约定的格式**
   （如行首 + 冒号 + 固定前缀），并把该约定**写进文档、让所有产出方遵守**；
   ⛔ 不要依赖"自然语言里恰好没出现这个词"。
3. ⭐ **假失败和假通过一样有害**：假通过让缺陷漏网；假失败让人**开始怀疑正常的东西**
   （本项目一度以为是 RTL 坏了，其实是判定正则把说明文字当失败）。
4. ⭐ **判定的最终执行者也要端到端验证**：临时造一个"故意失败"的用例，确认整条链路
   （产出方 → 日志 → 判定 → 退出码 → 汇总）真的判失败并返回非零。
5. ⭐ **元模式**：`\s` 在"多行模式"下含换行、`.*` 贪婪跨行、`^`/`$` 与 `\r\n` 的交互 ——
   写"行级"判定时**显式用 `[ \t]` 而不是 `\s`**，并优先用 `(?m)` 下的**锚定 + 显式字符类**。

**通用化**：日志扫描、CI 的 `allow_failure`/关键词门禁、告警规则、静态检查白名单、
错误分类器 —— **任何"从文本里判状态"的机制**，都要**同时**防"漏判"与"误判"，
并把这个双向验证做成**可复跑的用例**。

**关联**：`C14`（显式结论 vs 扫日志）、`C21`（判据本身也要被验证）、`C16`（同签名 ≠ 同病）

---

## C23. ⭐⭐⭐ 看到"变异体没被抓到"先问：**这个变异体真的违反规格了吗？**（2026-09-17）

**场景**：用变异测试（`C21`）守一条新加的、有手册依据的硬件序列。给这条序列加了 2 个变异体，
结果**两个都 LEAK**（回归没变红）。第一反应是"判据有盲区，赶紧加断言"。
⛔ **这个反应是错的** —— 一查发现，**两次 LEAK 都不是盲区，而是变异体本身没造出违规**：

| 变异体 | 改成什么 | 为什么 LEAK |
|---|---|---|
| ① 把某个"锁存位"改成不置位 | 以为会因此产生"第二次下降沿" | ⛔ **它根本没产生第二个下降沿**（另一条赋值仍把输出保持在低）⇒ 被测行为**没变** |
| ② 把脉冲宽度缩到 `HALF_DIV` | 以为会违反 `tw ≥ 500 ns` | ⛔ 缩完**恰好 = 500 ns**，**正好满足**手册下限 ⇒ 判据**正确地**放过了它 |

**可迁移的做法**
1. ⭐⭐ **LEAK 的第一诊断不是"判据弱"，而是"变异体弱"**：先**证明这个变异体真的违反了规格**
   （算一遍数值、看一遍波形/日志），再决定要不要动断言。
2. ⭐ **"恰好等于限值"是变异体设计的高危区**：想造"违反 `≥ X`"的变异，必须造出 **< X**，
   ⛔ 不能造出 **= X**（那**合规**）。⇒ 缩到 `X/2` 而不是 `X`。
3. ⭐ **改"控制位"不一定改"行为"**：改一个状态锁存位/使能位，可能被下游的其它赋值"救回来"。
   ⇒ 变异后要**确认输出真的变了**（最省事的办法：看那个变异体的 tb 日志有没有出现预期的新现象）。
4. ⛔ **不要为了消灭 LEAK 而给变异体量身定做断言** —— 那会加出"只对这个变异体敏感、
   对真实缺陷不敏感"的**假判据**，比没有判据更危险（它会给你虚假的安全感）。
5. ⭐ **正确的 LEAK 是有价值的**：它说明"这个变异体是等价变异体（equivalent mutant）"，
   把它**改对**（或标注为等价、从清单里移除）都是对套件的净化。

**通用化**：变异测试、fault injection、混沌工程、负向测试用例设计 ——
凡是"故意注入一个错误，看检测机制是否报警"的做法，都要先回答
**"我注入的这个东西，按规格它真的错了吗？"**。⛔ 否则"没报警"会被误读成"检测机制不行"，
而真相是"我注入的不是错误"。

**关联**：`C21`（必须证明判据会失败）、`C22`（判定规则太严/太松都会错）、
`C16`（严重性由下游指标裁决）、`fpga/TROUBLESHOOTING.md` 64 号

---

## C24. ⭐⭐⭐ 拿"人类可读文案"当解析锚点 = 把逻辑绑死在一种语言上（2026-09-18）

**场景**：一个多账号 API 网关要处理上游的**限流**响应。上游用同一个业务码 `6004`
表达"**该模型**的用量超限，切其它模型可继续用"，并在 `msg` 里给出**权威重置时刻**。
网关的正则只认中文文案：

```
softRateResetPattern = `将在 (.+?) 重置`
```

国内版（中文）匹配正常；**国际版返回英文**，同一业务码、同一语义：

```
{"code":6004,"msg":"usage exceeds frequency limit, but don't worry,
 your usage will reset at 2026-09-18 21:14:07 UTC+8,
 alternatively, you can switch to the other models to continue using it."}
```

⇒ 正则不匹配 ⇒ **解析不出重置时刻** ⇒ 走不到"模型级精确冷却"分支，
**静默降级**到兜底的"账号级有界退避"（基数起、指数翻倍、封顶）。

**实测后果（两个方向都错）**

| 维度 | 应有行为 | 实际行为 |
|---|---|---|
| 冷却范围 | 只锁 `deepseek-v4.1-flash` 这一个模型 | ⛔ **锁掉整个账号** |
| 冷却时长 | 精确对齐上游 21:14 重置 | ⛔ 指数退避顶到 2h 封顶 |
| 可观测性 | `/status` 的 `rate_limited_models` 台账应有一行 | ⛔ **台账为空** —— 状态里完全看不出发生过降级 |

⭐ 最刺眼的反证来自日志：**同一个账号上另一个模型请求成功了**
（`hy3` → 200），而触发限流的模型持续 429 —— 这本该一眼看出是"模型级"，
但状态接口**什么都没说**，只能靠翻原始日志才定位。

**可迁移的做法**
1. ⭐⭐ **解析要锚定"结构化字段 + 多语言表"，不要锚定自然语言句子**：
   优先用**业务码 / 错误类型 / 独立字段**判定；需要从文案里取信息（如时间）时，
   **把各语言的正则/模板做成一张表**，并让**缺失语言走显式报错**而不是静默兜底。
2. ⭐⭐ **"解析失败"必须与"正常路径"在可观测性上可区分**：
   本例的兜底分支完全等价于"上游没给时间"，于是**降级无痕**。
   ⇒ 兜底时**打日志 + 在状态里留痕**（哪怕是 `parse_failed: true` 这类字段）。
3. ⭐ **多语言/多区域产品要把"文案差异"当成一等测试维度**：
   同一错误码至少准备**中/英两份真实抓包样本**做用例
   （本例两份样本一跑就分叉，成本极低、收益极高）。
4. ⭐ **"降级到兜底"永远是最危险的路径**：它通常"还能工作"，所以不会被发现；
   但它改变了**语义**（范围、时长、粒度）。凡有兜底，就要问
   **"降级后语义偏到哪去了？谁会因此受影响？"**。
5. ⭐ 判据设计参照 `C16`：**症状（持续 429）本身不是判据**，
   要拿**权威来源**（上游文案里的重置时刻、另一个模型是否可用）来裁决归因。

**通用化**：错误分类器、限流/重试策略、日志解析、爬虫、协议适配层、
任何"从**别人给的文本**里提取语义"的代码 ——
文本是**给人看的**，语义是**给机器用的**；二者之间必须有一层**显式的、可测的映射**，
⛔ 不能靠"我们的用户都看中文"这种隐含假设。

**关联**：`C05`（配置类事实查源文件）、`C09`（标称值≠实测值）、
`C16`（严重性由下游指标裁决）、`C20`（存在性≠可用性：都要**实跑**才作数）、
`C22`（判定规则太严/太松都会错）

---

## C25. ⭐⭐⭐ 写入型 API 的 `None`/缺省参数常是「跳过」语义，不是「置空」语义（2026-09-18）

**场景**：用 `openpyxl` 批量重写一张 586 行的元件库存表——按一级分类重新分组插入
新行、重排序号、重建汇总 Sheet。写入循环写成"把整行 10 个字段一次性塞进去"：

```python
for c in range(1, 11):
    cell = ws.cell(row=rr, column=c, value=vals[c-1])   # vals 里有些是 None
```

**现象**：脚本零报错跑完，行数、序号连续性、总件数、分类计数**全部正确**。
但抽查时发现**串行污染**：

| 行 | 名称 | 参数（应为空） | 封装（应为空） |
|---|---|---|---|
| CH340X USB转串口芯片 | 串口芯片 | ⛔ `电压:20V 电流:1A` | ⛔ `SOD-323` |
| USB转TTL CH340G | 串口芯片 | ⛔ `电压:30V 电流:200mA` | ⛔ `SOD-123` |
| SS8050 贴片三极管 | 三极管 | ⛔ `有效防止浪涌、瞬态事件……` | SOT-23 |

`电压:20V 电流:1A / SOD-323` 本是 **B5817WS 肖特基二极管**的数据 —— 它被搬到了 CH340X 行上。

**根因（实测证实）**：`openpyxl` 的 `cell(row, col, value=None)` **不清除旧值**，
它等价于"这次不写这一格"，于是**留在该行位置上的旧内容原样保留**：

```python
ws.cell(row=5, column=2, value='TEST')
ws.cell(row=5, column=2, value=None)     # B5 仍是 'TEST'！
```

重排后新行落到旧行曾占用的行号上，`None` 字段就把**前任的内容**继承了下来。
正确写法：`ws.cell(row=r, column=c).value = v`，或显式 `cell.value = None`。

**为什么这个 bug 特别危险**
1. ⭐ **所有"粗粒度校验"都会放过它**：行数对、序号连续、总数对、分类计数对 ——
   因为污染只发生在**字段级**，不改变任何计数。
2. ⭐ **污染值"看起来合理"**：搬过来的是同表内另一条真实元件的**合法**参数，
   不是乱码，肉眼扫过很容易当成正常数据。
3. ⭐ **影响面与"重排"耦合**：不重排（纯追加到表尾）时碰不到；一旦排序/插入/删除，
   旧行位置被新行复用，就必然踩上。

**可迁移的做法**
1. ⭐⭐⭐ **改数据前先备份**，且备份要**在同一批次内**可回滚（本例靠备份原样恢复后重跑）。
2. ⭐⭐⭐ **写完必须做「备份 vs 当前」逐字段 diff**，不要只核总数：
   逐行比对 `(名称, 参数, 封装, 数量)` 四元组，把"丢失/变更"与"新增"分别列出来；
   本例正是靠这一步在交付前抓到了污染（原先只看了行数）。
3. ⭐⭐ **批量重写整表时，用"整行覆盖"而不是"逐格填空"**：
   把目标行当作一个完整记录写入，每个字段都显式赋值（含空串/None）；
   或先把目标区域**整块清空**（`delete_rows` / 重建 sheet）再写。
4. ⭐ **凡遇"传 None 想置空"的 API，先做一次最小实验验证语义**：
   `writer(cell, None)` 到底是"置空"还是"跳过"，一跑就知道（同 `C20`：实跑才作数）。
   同类语义陷阱还有：`dict.update` 的 `None` 值、SQL `SET col=NULL` vs 省略列、
   部分 ORM 的"未设置"哨兵值。

**通用化**：Excel/表格库、ORM、配置合并、`patch`/部分更新 API、
任何"传部分字段去更新一条已存在的记录"的场合 ——
**"我没给这个字段" 与 "我要把这个字段清空" 是两种语义**，
缺省值/`None` 到底走哪一种，必须查文档或实跑确认，⛔ 不能靠直觉。

**关联**：`C20`（存在性≠可用性：都要**实跑**才作数）、`C05`（配置类事实查源文件）、
`C17`（写完要回读验证：同为"写文件后必须复核"）、`C08`（判"坏成什么样"要看原始数据 ——
粗粒度统计量同样会掩盖字段级污染）

---

## C26. ⭐⭐ 批量导入前必须按"生命周期状态"过滤：同一条目会同时存在"成功"和"关闭"两笔（2026-09-18）

**场景**：把电商导出订单合并进库存台账。导出表首列是「订单状态」
（`交易成功` / `交易关闭` / `卖家已发货` / 空值）。合并脚本按"名称+款式"去重后入库。

**现象**：用户凭记忆指出"**我没买过电磁炮套件**"。回头核对，该行状态确为 `交易关闭` ——
**没成交的条目被记成了库存**。顺带查出同批还有 **2 项**同类漏网
（8路巡线传感器、镀锌板 300*300*0.5mm），全部是 `交易关闭`。

**三个叠加的坑**

| # | 坑 | 后果 |
|---|---|---|
| 1 | `交易关闭` 行的**字段齐全**（名称/款式/数量都在），肉眼与成交行**无差别** | 直接入库 ⇒ 库存虚增 |
| 2 | 同一商品会**同时存在"关闭"和"成功"两笔**（下了单又取消、重新下单） | 按"名称+款式"去重会把两笔**错误合并**（本例镀锌板：0.3mm 成交、0.5mm 关闭，按名称去重就糊成一个） |
| 3 | 状态列**大量空值**，实为**同一订单的续行**（继承上一行状态） | 若把"空 = 未成交"一刀切，会误删大量真实购买 |

**可迁移的做法**
1. ⭐⭐ **去重键必须包含"状态"**：`(名称, 款式, 状态)` 而不是 `(名称, 款式)`；
   先把非成交行剔除，**再**做去重与累加，顺序不能反。
2. ⭐⭐ **状态空值先"向下继承"再判定**：同一订单内多行共用首行状态是常见导出格式；
   用**订单号/分组**而非逐行独立判断。本例中"空值行"多为同单续行，属真实购买。
3. ⭐ **人的记忆是有效判据**：用户说"我没买过这个"时，
   应当成一条**独立线索**去核对，而不是当成"数据源不会错"而忽略。
   （本例正是这句话暴露了整类问题。）
4. ⭐ **"状态"字段要当一等公民看待**：与 `C24` 同理 ——
   结构化字段（状态码）比"看起来对不对"可靠得多；
   凡导入都要先问"**这个数据源用什么字段表达『这笔到底成没成』**"。

**通用化**：电商/ERP/工单/CI 结果/日志的批量导入 ——
数据源里的记录**天然含全生命周期**（草稿、已取消、已退款、已关闭、已删除标记 `is_deleted`），
而"台账/库存/资产"只应接纳**终态为成功**的那部分。
**"记录存在" ≠ "业务成立"**，导入层必须显式过滤，⛔ 不要指望下游发现。

**关联**：`C25`（同为批量数据写入的教训：都要"备份 + 逐字段 diff"）、
`C05`（配置类事实查源文件）、`C09`（标称值≠实测值：数据源自述与实际内容可能不符）、
`C10`（空测试陷阱：先确认"这条记录真的代表一次成交"）

---

## C27. ⭐⭐⭐ "有码位" ≠ "有字形"：字体可以声明覆盖某码位却没有对应字形 ⇒ 渲染成"空框"（2026-09-20）

**场景**：用 SVG 画技术图（中文标注 + `✅ ⛔ ⚠️ ⭐` 状态符号），再渲染成 PNG 放进演示文稿。

**现象**：**SVG 在浏览器/编辑器里看着完全正常**，
但**渲染成位图后所有 emoji 变成空框**（`□`）。
⚠️ 产物是要进 PPT 的 ⇒ 空框直接毁掉观感，而且**很容易在"预览看着没问题"时漏掉**。

**根因（一次单变量对照实验就查清）**：
**没有任何一种字体同时覆盖 emoji 与中文** ——

| 字体栈（谁在前谁优先） | 结果 |
|---|---|
| 中文字体在前 | ⛔ emoji 全成空框（**有码位、无字形**） |
| emoji 字体在前 | ⚠️ emoji 对了，但**中文全成空框** |
| ⭐ 纯中文 + 几何符号 | ✅ 全部正常 |

⭐ **关键认知**：**"这个码位被字体覆盖" ≠ "这个码位有字形"**。
字体表可以**声明**覆盖某段码位，渲染器却找不到字形 ⇒ 画 `tofu`（空框）。

**可迁移的做法**

1. ⭐⭐ **面向"最终位图"的输出（SVG→PNG / Canvas / 图表库 / PDF），⛔ 不要用 emoji 表状态**。
   改用**几何符号**（跨字体普遍有字形）：
   ```text
   ✅ → √      ⛔ → ×      ⚠️ → △
   ⭐ → ★      ⏳ → …      🎯 → ▶
   ```
   📌 技术图里 emoji 的**信息量本来就低**（`★` 与 `⭐` 完全等价）⇒ 换掉**零损失**。

2. ⭐⭐ **判据要在"最终产物"上验，⛔ 不是在编辑器里验。**
   ⚠️ 编辑器/浏览器预览走的是**系统字体回退链**，与**实际渲染器**（本例 reSVG）不同
   ⇒ "预览正常"**证明不了**"产物正常"。
   ⇒ 必须**把位图渲出来肉眼看一遍**（同 `C10`：先确认被测对象真的被测到）。

3. ⭐ **定位这类问题首选"单变量对照"**：造一个**只改字体栈**的最小样例，
   一次渲染就能把"字体问题"与"渲染器问题"**切开**（同 `C03`：设计能一次否证多个假设的实验）。

4. 📌 **同族指纹**：凡是**"看着对、产物不对"**的问题，都先问
   **"我验的是哪一层？最终产物是什么？"** ——
   与本库 `C02`（开发环境通过 ≠ 真实环境一致）、`C17`（BOM 归属由**读取方**决定）
   是同一族：**中间表示正确，不代表最终消费端正确**。

**通用化**：文档导出、报表生成、图表渲染、截图流水线 ——
凡"**源码/中间格式 → 位图/PDF**"的链路，
都要问三句：**① 最终消费端是谁？② 它的字体/编码环境是什么？③ 我在哪一层验的？**

**关联**：`C02`（环境不一致）、`C10`（空测试陷阱）、`C17`（编码归属由读取方决定）、
`C03`（一次否证多个假设）、`fpga/FPGA逻辑调试经验集.md` `L64`（本例的 FPGA 侧记录）

---

## C28. ⭐⭐⭐⭐ "关于外部/他人状态的判断"必须现查，⛔ 不能用上次的快照当事实（2026-09-20）

**场景**：单人推进一个多人协作项目。连续几轮问"队友做了什么"都没看到新提交，
于是**把整轮排期建立在一个假设上**：「队友推进缓慢 ⇒ 按"我一个人推"来排」。
据此产出了**接管边界清单**（哪些文件"没人做、可放心新建"）、
**按依赖顺序排的任务表**、**给队友的交接说明**。

**现象**：推送前按流程 `git fetch` 做校验，**发现前提从头到尾就是错的** ——
远端已多出 **8 个提交 / 91 个文件 / +9601 行**（队友落了整套合成核心 + 并行采集 + 一份口径裁决）。

**为什么这是"高代价的静默错误"**

| # | 后果 |
|---|---|
| 1 | ⭐ **排期错 = 全盘错**：接管边界、任务顺序、分工表**全都基于它** |
| 2 | ⚠️ **"可放心新建"清单已失效** ⇒ 那些文件**远端已存在** ⇒ 照做就是**重复劳动 + 同名冲突** |
| 3 | 🔴 **并行两条线把编号撞了**：各自按"当时最大编号 +1"取号 ⇒ **同一编号被两个不同问题占用**（⚠️ 不报错，只会让后来人查到**错误的那条**） |
| 4 | ⚠️ **最危险**：**本地一切自洽、测试全绿、文档互相印证** —— ⛔ **只有 `fetch` 那一下才暴露**，而 `fetch` 通常是**推送前**才做 ⇒ **错误前提可能存活一整轮工作** |

**根因**：⭐ **把"关于别人的判断"当成了静态事实。**
我用的证据是"**我上次看的时候**没有" ⇒ 那是**几天的快照**，不是当前状态。
⚠️ 而**权威答案本来就在手边**（`fetch` + `log origin/main..`）——
**我用印象代替了查询**。

**可迁移的做法**

1. ⭐⭐ **凡"关于别人/远端/外部系统状态"的判断，先查再写**：
   **排期、分工、"谁没做"、接口现状**这类结论，**一律以现查为准**。
2. ⭐ **把"查"从"收尾动作"提前到"规划动作"**：
   ```powershell
   git fetch origin
   git log --oneline <上次记的SHA>..origin/main     # 别人动了什么
   git diff --stat  <上次记的SHA>..origin/main      # 动了多少
   ```
   ⭐ **"上次记的 SHA"是关键锚点** —— 有它才能一眼看出"变了没有"。
3. ⭐ **共享编号空间要留缓冲**：并行取号前先 `fetch`，或**一次往大跳几位**（如从 90 起）。
4. ⭐ **发现前提错了要当场改文档**，并保留"**原判断 → 更正 → 原因**"的痕迹
   （⛔ 不要静默覆盖，否则下一个人还会踩）。

**通用化**：任何**多人/多端/外部依赖**的规划 ——
"队友没动""这个库没更新""上游没改""服务是好的"——
**都是关于外部状态的断言**，⛔ 不能凭印象，**必须现查**。
📌 与 `C05`（配置类事实必须查源文件）、`C09`（标称值 ≠ 实测值）同族：
**"我以为"和"现在是什么"，是两件事。**

**关联**：`C05`（查源文件）、`C09`（标称≠实测）、`C11`（独立真值）、
`C24`（结构化字段比"看起来对"可靠）、`fpga/TROUBLESHOOTING.md` 68 号（本例完整过程）

---

## C29. ⭐⭐⭐⭐ "跳过"绝不能被算作"通过" —— 测试框架必须把 `skipped` 当失败（2026-09-20）

**场景**：变异测试（mutation testing）—— 注入一个已知缺陷，要求回归**必须变红**。
每个变异体的定义是"文件 + 要替换的字符串（锚点）+ 替换成什么"。

**现象**：跑完打印

```
PASS: every injected defect was caught by the regression suite
```

⚠️ **但摘要里明明有两行**：

```
  M21   skipped   anchor not found
  M22   skipped   anchor not found
```

⇒ ⭐ **"有两个变异体没跑" 和 "全部被抓到" 同时成立，且结论是 PASS。**

**根因（两层）**

| # | 层 | 说明 |
|---|---|---|
| **1** | **锚点失配** | 被测 RTL 被**重构**了（把写死的 3 处扇出改成 `generate for` 循环，以支持可变片数）⇒ 两个变异体的锚点字符串**永远匹配不上** |
| **2** | 🔴 **判定逻辑** | 脚本只统计"没抓到"（`Caught -eq $false`），而 `skipped` 的 `Caught` 是 **`$null`** ⇒ ⛔ **不计入任何失败** ⇒ **打印 PASS** |

**为什么第 2 层是致命的**

它让 **`adc_farm`（刚被队友落地、又被我改过）在【无任何变异体守着】的情况下，
被测出"变异测试全绿"**。⇒ ⭐ **"变异测试全绿"这个结论本身失去意义** ——
而它是整个验收体系"判据有效性"的唯一证据（同族 `C21`：判据本身要被验证）。

⚠️ **最危险的地方**：**本地一切自洽** —— 回归 25/25、其他 20 个变异体全 RED、
脚本退出码 0。⛔ **只有把摘要那几行 `skipped` 读出来才会发现。**

**可迁移的做法**

1. ⭐⭐ **任何"跳过/忽略/未执行"状态都必须显式失败。**
   `skip` / `xfail` / `pending` / `inconclusive` / **`0 tests run`** ——
   ⛔ **它们的语义是"没有被验证"，不是"验证通过"**。
   ```powershell
   if ($skipped -gt 0) { Write-Host "FAIL: skipped mutants guard NOTHING"; exit 4 }
   ```
2. ⭐⭐ **"锚点找不到"是一个必须被听见的信号，不是可以忽略的噪声。**
   它的含义是：**被测代码移动了，而判据没有跟着移动** ⇒ 那条保护**已经失效**。
3. ⭐ **改被测代码时，要连带检查"指向它的判据/变异体/断言锚点"是否还成立。**
   ⇒ 重构与重指锚点**应当在同一个提交里**。
4. ⭐ **测试工具的"通过条件"要写成白名单**（"全部 `caught` 才算过"），
   ⛔ **而不是黑名单**（"没有 `LEAK` 就算过"）——
   白名单天然拒绝未知状态，黑名单会把**没见过的状态**放过去。
5. 📌 **推论（同 `C21`/`L56`）**：**"测试全绿"必须连同"测试真的跑了"一起证明。**
   一个会静默跳过用例的框架，其"全绿"与"没测"不可区分。

**通用化**：CI / 单元测试 / 变异测试 / 覆盖率门槛 / 数据校验 ——
凡是"跑一批检查然后给总结论"的工具，都要问三句：
**① 有没有可能"没跑"？② 没跑会被算成什么？③ 我从哪里能看出它没跑？**

**关联**：`C21`（判据本身要被验证）、`C23`（LEAK 的第一诊断是"变异体弱"而非"判据弱"）、
`L56`（判据要能被验证）、`fpga/FPGA逻辑调试经验集.md` `L63`（工具产物归属）、
`fpga/TROUBLESHOOTING.md` 68 号（同批次发现的"外部状态必须现查"）

---

## C30. ⭐⭐⭐ 「退出码」不是唯一判据：PowerShell 会把 native 程序的 **stderr** 判成失败 ⇒ `[exit code: 1]` 假警报（2026-09-21）

**场景**：用 `pwsh` 调 `git push`。git 把**正常进度信息**写到 **stderr** ——
`To github.com:...`、`d8f21e5..757b55d  main -> main`、`Everything up-to-date`。

**现象**：命令**实际成功**（远端确实被更新了），但外层工具报告

```
[exit code: 1]
... NativeCommandError
```

⚠️ 若只看退出码，会**误判 push 失败** ⇒ 重试、回滚、或向用户报告"推送失败"。
**本例就是先报了 `[exit code: 1]`，靠重新 `fetch` 核对才发现其实成功了。**

**根因**

PowerShell 5.1 把 native 命令的 **stderr 输出包装成 `ErrorRecord`**（`NativeCommandError`）。
触发条件是 **`2>&1` 重定向 + 该命令是脚本的最后一条语句**：
重定向后的 `ErrorRecord` 落进错误流，而 **pwsh 进程的最终退出码被这个未处理的错误置为 1**，
**覆盖掉** native 程序真实的 `$LASTEXITCODE = 0`。

**证据（本机实测，PSVersion 5.1.26100.9444）**

| 命令 | 是否为最后一条语句 | 报告退出码 |
|---|---|---|
| `git push origin main 2>&1` | ✅ 是 | **1（假失败）** |
| `git push origin main 2>&1; $null` | ❌ 后面还有语句 | 0 ✅ |
| `git push origin main`（不写 `2>&1`） | ✅ 是 | 0 ✅ |
| `cmd /c "echo x 1>&2" 2>&1` | ✅ 是 | **1（假失败）** |
| `git push origin no-such-branch 2>&1` | ✅ 是 | 1（**真失败**） |

⇒ ⭐ **真假失败退出码完全相同** ⇒ **退出码本身无法区分两者**。
用 `cmd /c echo` 最小复现，证明**与 git 无关**，是 PowerShell 的语义。

**失败尝试（都不成立）**

1. ❌ 先怀疑是 harness 的 bug → 最小复现（`cmd /c "echo x 1>&2" 2>&1`）证明是 PS 语义。
2. ❌ 以为 `$LASTEXITCODE` 也被污染 → 实测 **`$LASTEXITCODE` 始终是 0（干净）**，
   被污染的是**进程退出码**，不是这个变量。
3. ❌ 以为 `$?` 更可靠 → 实测 `git ... 2>$null` 后 **`$? = False`**，同样被 stderr 影响。
4. ❌ 以为"stderr 有输出 = 命令失败" → `Everything up-to-date` 也走 stderr，而它是**成功**。

**可迁移的做法**

1. ⭐⭐ **判定操作结果要看"权威证据"，⛔ 不要只看退出码。**
   push → `git rev-parse origin/main` 对比本地 `HEAD`；
   文件操作 → 回读内容；DB 操作 → 查回读值。
   （同族 `C11`：判据要用**独立于被测量**的真值）
2. ⭐⭐ **修法（二选一）**：
   - 命令后**补一条语句**（`; $null` 或 `; exit $LASTEXITCODE`）——别让它当最后一条；
   - 或**干脆不写 `2>&1`**（stderr 仍会被收集展示，只是不再产生假错误）。
3. ⭐ **任何"用退出码自动判定成败"的逻辑，都必须先做双向标定**：
   拿一个**已知成功**和一个**已知失败**各跑一次，确认两者**真的能被区分开**。
   （同族 `C22`：判定太严/太松都会错，边界必须双向验证）
4. ⚠️ **别把"有 stderr 输出"等同于"操作失败"**：
   git / hg / docker / npm / curl 把**正常进度**写 stderr 是**常见约定**。
5. 📌 **推论**：退出码是**一层中间层的产物**，不是被观测对象的原始事实 ——
   它可能被 shell、重定向、包装器、CI 任意改写。

**通用化**：CI 脚本 / harness / Makefile / Python `subprocess` / 任何"用子进程退出码决定后续动作"的编排，
都要问一句：**这个退出码是谁写的？中间有没有层改写它？**

**关联**：`C11`（判据要用独立真值）、`C22`（判定规则必须双向验证）、
`C14`（证据与判定必须分开表达）、`C17`（写入方与读取方约定不一致）

---

## C31. ⭐⭐⭐⭐ **"能跑通"与"能被工具接受"是两回事：宽松的前端会掩盖严格前端的拒绝**（2026-09-21）

**场景**：一段代码/一个模块**长期测试全绿**，把它交给**另一个更严格的工具**处理时，
**直接被拒绝**，而且拒绝方式是**"整个单元被丢弃"**，不是逐条报错。

**具体形态**（FPGA 域，但结论是通用的）：源码里用了 **SystemVerilog 专有语法**
（带宽度强制转换 `N'(expr)`、`longint` …），

| 前端 | 结果 |
|---|---|
| 仿真器（宽松，接受 SV） | ✅ **全绿** —— 永远发现不了问题 |
| 综合器（按 Verilog-2001 子集） | ⛔ `Syntax error` + **`Module 'x' is ignored due to previous errors`** ⇒ **整个模块没进产物** |

⭐ **危险在于两者的结论同时"看起来正常"**：
测试报告是绿的，工程文件里"那个模块也登记了"，**只有真跑一次才暴露**。

**通用化**：凡是**同一个产物被多个前端/工具/环境消费**的场合都适用 ——
编译器 vs 解释器、linter vs runtime、CI 的不同镜像、不同版本的同一工具、
打包器 vs 运行时……

**可迁移的做法**

1. ⭐⭐ **"宽松前端通过"不能替代"严格前端通过"** ——
   把两者当成**两份独立证据**，各自取证。⛔ 不要用"测试全绿"去推断"能被构建/部署"。
2. ⭐ **注意"静默丢弃"类错误的签名**：
   报错文案里出现 **"is ignored" / "was skipped" / "not included" / "0 items"**
   ⇒ 含义是**该单元根本没进产物**，⛔ 不是"有个小问题"。
   （同族 `C29`：**"跳过"绝不能算"通过"**）
3. ⭐⭐ **判据要落在"产物里有没有它"上，而不是"我有没有提交它"** ——
   比如：日志里必须出现 **`Compiling module '<名字>'`**，
   而只有 `Analyzing ...`（=只解析）**不算**。
   ⭐ **并且"被处理"通常还要求"被引用到"** ——
   ⇒ 需要一个**把所有单元都引用一遍的自检入口**（本项目做法：自检顶层）。
4. ⭐ **扩展名不代表语义**：`.sv` 不代表工具会按 SV 编译 ——
   取决于**工程/构建配置里怎么登记**。⛔ 别用文件名推断编译模式。
5. 📌 **同一个坑在一个项目里复发 ⇒ 说明"记在注释/文档里"不够**，
   要升级成**可机械检查的纪律**（语法白名单 / 自检覆盖清单 / CI 门禁）。

**关联**：`C29`（跳过≠通过）、`C21`（判据本身要被验证）、`C20`（存在性≠可用性）、
`fpga/TROUBLESHOOTING.md` 70 号、`fpga/FPGA逻辑调试经验集.md` `L66`

---

## C32. ⭐⭐⭐⭐ **"这段代码会执行"本身是一个假设 —— 退化/空转的代码在宽松前端下会静默消失**（2026-09-21）

**场景**：一段参数化的组合逻辑，在**默认参数取值**下退化成**纯常量赋值**
（不读任何输入信号）。另一个前端（仿真器）于是**根本不执行它**。

**现象**：模块"语法全对、编译无错"，但**功能完全没生效**（输出恒为初值）。
⭐ 而**另一个前端（综合器）会把常量块正常折叠**，算出"正确"的结果。

⇒ ⭐⭐ **同一份代码，两个前端给出不同行为**，而"仿真通过"因此**失去意义**。

**通用化**：凡是**"某段代码在某个配置/参数/分支下退化为空操作或常量"**的场合都适用 ——
条件编译分支、feature flag 关闭、空集合上的循环、空列表的批处理、
`if (N == 0)` 的早退、模板特化、DI 注入的空实现……

**可迁移的做法**

1. ⭐⭐ **不要用"隐式敏感表"表达组合逻辑** ——
   在 HDL 里即：⛔ **不要用 `always @(*)` 写"某参数下会退化成常量"的块**，
   改用**连续赋值 / 显式依赖**。等价地：让"依赖关系"**写在代码结构里**，
   ⛔ 不要依赖工具去推断。
2. ⭐⭐ **把"零依赖/空敏感表"的警告当错误** ——
   它说的不是"这段被优化掉了"，而是"**这段永远不会跑**"。
3. ⭐ **警惕"对集合元素赋值"时的隐式依赖推断** ——
   数组/容器元素的更新是否算依赖，不同工具处理不同。
4. ⭐ **"多前端一致性"要单独取证** ——
   ⛔ 不能因为"一个前端通过"就认为另一个也会通过（同族 `C31`）。
5. 📌 **判据写成具体数值**（"输出应为 `0001`"），
   ⛔ "跑完没报错"抓不到"功能全废但语法全对"。

**关联**：`C31`（宽松前端掩盖严格前端的拒绝）、`C29`（跳过≠通过）、
`C21`（判据本身要被验证）、`fpga/TROUBLESHOOTING.md` 71 号、
`fpga/FPGA逻辑调试经验集.md` `L67`

---

## C33. ⭐⭐⭐ **"被测对象的时间语义"必须先确认，再写判据 —— 否则会去改对的代码**（2026-09-21）

**场景**：给一个模块写验收判据。被测系统用**时钟使能脉冲**（每周期**高 1 拍**）驱动。

**现象**：判据失败。⭐ **第一反应是"被测设计有 bug"** ——
而实际上**两处都是测试自己的错**：

| 我的假设 | 真相 |
|---|---|
| 使能信号"拉高一段"就行 | ⭐ 它是**脉冲**，**宽度本身有意义** ⇒ 拉长 N 倍 = 跑了 N 步 |
| "过渡过程"里的数值可以直接当判据 | ⭐ 过渡值**取决于恰好跑了多少拍** ⇒ 不可复现；判据必须落在**稳态/平台**上 |

⚠️ 若当时直接去改被测 RTL，就会**把正确的代码改坏**，
而且"改完判据过了"还会**强化这个错误结论**。

**通用化**：任何"给带时间/顺序语义的系统写判据"的场合 ——
消息队列的 ack、事件流的窗口、状态机的时序、动画的帧、异步任务的完成信号……

**可迁移的做法**

1. ⭐⭐ **判据失败时，先怀疑"我的时基/假设"，再怀疑被测对象** ——
   尤其当被测代码**已被其他证据支持**时。
2. ⭐ **区分"电平"与"脉冲/使能"语义** ——
   后者的**宽度是有信息的**，⛔ 不能随手改。
3. ⭐ **判据落在"平台/稳态"上，⛔ 不落在"过渡过程"上** ——
   过渡值不可复现，会让判据**时过时不过**。
4. ⭐ **把"假设"写下来并与被测方的约定对照**（本项目：接口冻结表 §〇 约定 1/4
   明确 `tick_*` 是**时钟使能**）——
   📌 **约定文档就是用来消除这类假设的，写 tb 前应该先读它**。
5. ⭐ **判据本身也要能被验证**（同 `C21`）：问一句"这个判据**怎样才能失败**？
   我现在的失败，是不是判据写错了？"

**关联**：`C21`（判据本身要被验证）、`C31`、`fpga/TROUBLESHOOTING.md` 73 号、
`fpga/FPGA逻辑调试经验集.md` `L55`（断言要写成不变量）

---

## C34. ⭐⭐⭐⭐ **换"执行方式"会悄悄改变"工作目录"这个隐含前提 —— 而产物丢失是不报错的**（2026-09-21）

**场景**：给一个验收脚本补上超时保护 —— 把**前台调用**外部进程
改成**后台作业**（`Start-Job` + `Wait-Job -Timeout`），超时就判失败。

**现象**：改完跑全套验收，**结果全绿**（27/27），
但日志里夹着一行"产物打不开"：

```text
VCD Error: ...:119: Unable to open build/sim/tb_xxx/tb_xxx.vcd for output.
```

⚠️ ⭐ **被测项依然报"通过"**（判定只看 `FAIL:` 行与退出码）
⇒ **证据静默丢失，而验收报告是绿的**。

**根因**：⭐ **后台作业的子进程工作目录 ≠ 原来的工作目录**
⇒ 脚本里**相对路径**的产物（`build/sim/…`）解析到了别处
⇒ 打不开 ⇒ 工具**只打印一行警告就继续**。

⚠️ 更值得记的是：**该脚本早就处理过这个坑的另一半** ——
它在**编译前**会扫描源码里的产物路径并**预建目录**（用**项目根**），
但**运行期工作目录变了** ⇒ 预建的目录**根本没被用上**。
⇒ 📌 **"一个坑修过一半"比没修更危险**：它会让你以为这类问题已经解决了。

**修法**：在作业里**显式 `Set-Location`** 回项目根（⛔ 不要假设继承）。

**通用化**：凡是把"直接调用"改成**间接执行**的场合都适用 ——
后台作业 / 容器 / CI 步骤 / 定时任务 / 服务化 / 子 shell / `sudo` 切换用户 /
远程执行（ssh、ansible）…… ⭐ **"在哪个目录、以哪个用户、带哪些环境变量"**
这三个前提**都可能变**。

**可迁移的做法**

1. ⭐⭐ **换执行方式时，显式固定"工作目录 / 用户 / 环境变量"** ——
   ⛔ 不要依赖继承，尤其**不要依赖相对路径**。
2. ⭐⭐ **"全绿" ≠ "证据还在"**：判定逻辑往往**看不见"产物有没有生成"**
   ⇒ ⭐ **验收脚本应当同时校验产物存在**（本项目有"证据落 `sim/evidence/`"的惯例，
   ⚠️ 但**没有机器检查它** ⇒ 这正是缺口）。
3. ⭐ **工具对"产物打不开"常常只是警告、不改变退出码** ⇒
   ⛔ 不能靠"退出码/关键字"兜住这类问题。
4. ⭐ **修完 bug 要跑一次"最脏"的场景**（本例：**先删掉产物目录**再跑）——
   ⛔ 否则上次留下的旧产物会让问题**看不见**。
5. 📌 **同一个坑的另一半修过 ≠ 修全了** —— 修的时候问一句
   "这个假设在**所有**调用路径下都成立吗？"

**关联**：`C29`（跳过≠通过）、`C31`（宽松前端掩盖严格前端）、`C21`（判据本身要被验证）、
`fpga/TROUBLESHOOTING.md` 72 号（本次修法的来源）与 74 号（本次新坑）

---

## C35. ⭐⭐⭐⭐ **跨边界时"结论"不会自然继承 —— 而"取不到结论"默认会被当成成功**（2026-09-21）

**场景**：把一个**前台调用**外部进程改成**后台作业**执行（为了加超时保护）。

**做法（错的）**：因为"作业里拿不到子进程退出码"，就把退出码**写死成 0**，
理由是"反正判定主要靠输出里的关键字"。

**后果**：⭐ **任何"跑挂了但没打印约定关键字"的情况都被判成功**：

| 场景 | 旧行为 |
|---|---|
| 进程崩溃 / 段错误（**零输出**） | ⛔ 判**成功** |
| 脚本自己解析失败（整段没跑） | ⛔ 判**成功** |
| 正常结束但**没给出结论** | ⛔ 判**成功** |

⚠️ 它**只在"某个用例让进程崩溃"时才暴露** ⇒ 平时全绿、**看起来一切正常**
（本项目实测：一个变异体因此报 `LEAK`，差点被误诊为"判据不够严"）。

**通用化**：凡是**结论要跨边界传递**的场合 ——
子进程 → 父进程、作业/容器/CI 步骤 → 编排器、服务 → 客户端、
异步任务 → 结果收集、RPC、消息队列……

⭐ 核心事实：**"没有结论"是一个独立的第三种状态**，
而**大多数框架的默认处理是"不失败"**（即默认成功）。

**可迁移的做法**

1. ⭐⭐ **不要为了绕过工具限制而把判据降级** ——
   ⛔ 写死 `exit=0` / 空 `catch` / 默认成功，**都是把"未知"变成"通过"**。
2. ⭐⭐ **显式跨边界传递结论** —— 本例用一行标记（`__VVP_EXIT__=<code>`）
   把退出码带出来；⭐ **并且"取不到标记"也要判失败**。
3. ⭐⭐ **判据写成白名单**：**"拿到明确的成功证据才算过"**，
   ⛔ 不是黑名单（"没看到失败就算过"）。同 `C29`。
4. ⭐ **区分三种状态**：成功 / 失败 / **未得出结论** ——
   第三种的默认处理**必须是失败**。
5. ⭐ **"只在异常路径暴露"的 bug 最危险** ——
   ⇒ 造一个**故意崩溃**的用例去验它（本例：只 `$fatal` 不打印关键字的 tb）。
6. 📌 **诊断顺序**：遇到"漏检"先问 **① 运行真的产生结论了吗？**
   再问 ② 判据看得见这个缺陷吗？ ③ 用例真的违反规格吗？
   ⚠️ 跳过 ① 会浪费大量时间去加严判据。

**关联**：`C29`（跳过≠通过）、`C34`（换执行方式改变隐含前提）、
`C21`（判据本身要被验证）、`fpga/TROUBLESHOOTING.md` 75 号

---

## C36. ⭐⭐⭐⭐ **判据的"探针"必须打在真正被改的那条线上 —— 打在上游信号上等于没测**（2026-09-21）

**场景**：给一个"相位累加器 → 查表"的模块写验收，
要证明 **"送给查表器的地址 = 相位的高 9 位"**。

**做法（错的）**：只断言了**相位寄存器**的数值
（"走 N 帧后相位应等于 3×2²³"），⛔ **从没观察真正送给查表器的地址线**。

**后果**：把地址表达式改成"**错 1 位的窗口**"，
⭐ **相位寄存器完全不变** ⇒ **判据看不见** ⇒ 变异体 **LEAK**（盲区）。

⇒ ⭐ 本质：**我测的是"上游"，而被改的是"下游"** ——
两者之间那条连线**没有任何判据守着**。

**⚠️ 补这个盲区时又踩了两次（都值得记）**：

| # | 坑 | 真相 |
|---|---|---|
| ① | 我造的"变异体"写成 `phase_q[ADDR_LSB +: AW]` | ⭐ 而 `ADDR_LSB=23, AW=9` ⇒ 与原文 `phase_q[31:23]` **语义完全相同** ⇒ **改了个等价写法**，当然抓不到。📌 **先问"这个变异体真的违反规格吗"**（同 `C23`） |
| ② | 改用 `inc=1` / `inc=512` 做测试数据 | ⭐ 相位只涨到 ~3000（bit 11）⇒ **位 [31:23] 与 [30:22] 全是 0** ⇒ "取高 9 位"与"取错窗口"**结果相同** ⇒ 仍然漏过 |

**修法（三条，缺一不可）**

1. ⭐⭐ **探针打在真正被证明的那条信号上** ——
   本例：直接观察送给查表器的地址；⛔ 不要用"上游寄存器的值"代替。
2. ⭐⭐ **判据写成"不变量"**，⛔ 不是硬编码期望值：
   ```text
   每次采样前记下 phase_before；
   断言 addr_q == phase_before[31:23]      // 恒等式，逐次核对 20 遍
   ```
   ⚠️ 我第一版硬编码"走 N 帧后应等于 X" ⇒ **差 1 拍就假失败**（同 `C33`）。
3. ⭐⭐ **测试数据必须真的落在"被观测的那几位"里** ——
   本例改用 `2²²` / `2²³` / `2²²+2²³` / `2³¹` 才让"取错窗口"与"取对窗口"**必然不同**。

**通用化**：任何"**A 经过一层变换得到 B**"的验收都适用 ——
编译器的 IR → 目标码、解析器的 AST → 求值结果、API 的入参 → 落库字段、
UI 的 state → 渲染输出……

**可迁移的做法**

1. ⭐⭐ **先问："我要证明的那个东西，是哪一个信号？"** ——
   然后**确保判据真的读到了它**。⛔ "读了它的上游"不算。
2. ⭐⭐ **判据优先写成不变量/恒等式**（"B 恒等于 f(A)"），
   ⛔ 不写"跑 N 步后 B 应等于常数"（依赖时序细节，易假失败）。
3. ⭐⭐ **造测试数据时，检查"它能不能区分对与错"** ——
   若两种实现给出**相同输出**，那这个数据**证明不了任何事**。
4. ⭐ **变异体本身要先自检**：它**真的改变语义**了吗？
   （等价改写 ⇒ 永远抓不到，且会误导你"判据太弱"）
5. 📌 **一个盲区往往需要"补三轮"**：探针位置 → 判据形式 → 数据取值。
   ⛔ 补一轮就宣布"覆盖了"是常见的过早收工。

**关联**：`C21`（判据本身要被验证）、`C23`（先问变异体是否真违反规格）、
`C33`（判据要落在稳态、先怀疑自己的假设）、`C35`（先问运行有没有产生结论）、
`fpga/TROUBLESHOOTING.md` 76 号

---

## C37. ⭐⭐⭐⭐ **"为了自检方便"而写死的常量，会把"要被证明的电路"优化掉**（2026-09-21）

**场景**：要证明一个设计里有 **64 个独立振荡器**（这是"吞吐量"的评分依据）。
把它例化进一个**综合自检工程**，想让工具报告里能数出 64 份相位寄存器。

**做法（错的）**：在自检顶层里为了"省事"，把它的**输入写死成常量** ——
例如"只让第 0 路活跃"：

```verilog
.voice_active_i ({{(N-1){1'b0}}, 1'b1})   // 🔴 只有第 0 路活跃
```

**后果**：⭐ 其余 63 路的相位寄存器**恒等于复位值、永不改变**
⇒ **综合器合法地把它们全部常量折叠掉了**
⇒ 报告里只有 **1069** 个寄存器，而**光 64×32 bit 的相位就要 2048**
⇒ 🔴 **"64 个独立振荡器"这条证据当场消失**。

⚠️ 而**仿真侧完全正常**（仿真不做这种优化）⇒ 只有看**综合报告**才发现。

**通用化**：凡是**"用工具产出来证明某个东西存在"**的场合都适用 ——
编译产物里的符号/函数、构建产物里的资源、覆盖率报告里的分支、
打包结果里的文件、链接后的段大小……

**可迁移的做法**

1. ⭐⭐ **要证明的电路，⛔ 不要在自检/测试顶层把它写死或接常量** ——
   把它**引到顶层输入**，让工具**无法常量折叠**。
2. ⭐⭐ **判据要"对得上手算值"**：
   报告里的数量应与**结构推算**一致（本例 64×32=2048）。
   ⚠️ 我看到 1069 时的第一反应应该是"这比手算小得多"，而不是"看起来有数就行"。
3. ⭐ **"仿真通过"与"综合产物正确"是两份证据** ——
   仿真器**不做**这种优化，所以它**看不见**这个问题（同族 `C31`）。
4. ⭐ **自检工程本身也要被审视**：它是"测量仪器"，
   ⚠️ 而**仪器可以改变被测对象**（观察者效应）。
5. 📌 **报数量时给"期望值 + 实测值"两个数**，
   否则"有个数"和"数对了"无法区分。

**关联**：`C31`（宽松前端掩盖严格前端）、`C36`（探针要打在真正被证明的线上）、
`C21`（判据本身要被验证）、`fpga/TROUBLESHOOTING.md` 77 号

---

## C38. ⭐⭐⭐⭐ **"改文件再还原"的工具被中途杀死 ⇒ 世界停在"半改"状态，而后续判断会把这个状态当成基准**（2026-09-21）

**场景**：一个测试工具的做法是"**把源文件改坏 → 跑测试 → 改回来**"。
它被外层**超时杀掉**（要跑 15 个用例 × 109 s ≈ 27 min，而外层上限 600 s）。

**后果链（这才是重点）**：

| # | 发生了什么 |
|---|---|
| ① | 被杀死时**改动还在文件里**（还原那一步没执行） |
| ② | 下一次运行时，工具发现"**锚点文本找不到**" ⇒ 报 **SKIP**（因为文本已经被改掉了） |
| ③ | ⭐⭐ 工具收尾时"**在干净代码上重跑一遍**"的步骤，实际跑在**改过的代码**上 ⇒ **整轮结论不可信** |

⚠️ 而且 **`git status` 只显示"1 个文件被修改"** ——
⭐ **极易被当成"我自己的正常改动"而忽略**。

**通用化**：任何**"临时改动 → 做事 → 还原"**的场合 ——
测试工具、代码生成器、codemod、补丁/回滚脚本、迁移工具、
"临时改配置再改回来"的运维脚本……

**可迁移的做法**

1. ⭐⭐ **启动时自检"上次是否干净退出"**：
   用一个**标记**（锁文件 / 哨兵文件 / 备份残留）判断，
   ⭐ **发现不干净就先把状态恢复回来，再开始**。
   📌 本例中"**备份目录里还有 `.bak`**"就是最可靠的不干净证据。
2. ⭐⭐ **恢复逻辑不能依赖"事后才知道的清单"** ——
   本例第一版从"变异体清单"里查该恢复谁，
   ⚠️ 而那份清单在脚本里**声明得比恢复代码更晚** ⇒ 拿到空清单
   ⇒ **一个都没恢复，还把证据（`.bak`）删了**。
   ⇒ ⭐ 恢复要**自给自足**（按文件名/目录直接推导）。
3. ⭐⭐ **恢复要按字节读写** ——
   用"读文本 + 写文本"会**重写行尾**（LF→CRLF）⇒
   内容没变却产生**几百行 diff**，把真正的改动淹没。
4. ⭐ **给长任务设"能被安全中断"的粒度**：
   每个用例单独还原，而不是"整轮结束才还原"。
5. ⭐ **并发保护要用"进程存活"判断，而不是"文件存在"** ——
   僵死锁必须能自动回收，否则工具再也起不来。

**关联**：`C29`（跳过≠通过）、`C35`（没有结论≠通过）、`C34`（换执行方式改变隐含前提）、
`fpga/TROUBLESHOOTING.md` 79 号 / 80 号

---

## C39. ⭐⭐⭐⭐ **"失败"被当成"成功"的最隐蔽形态：失败的原因不是你想验的那个**（2026-09-21）

**场景**：一套**变异测试**（故意把代码改坏，要求测试变红）用来证明"判据真的有效"。
判定逻辑只有一行：

```powershell
$caught = ($exit -ne 0)      # 🔴 只要"变红"就算"抓到了"
```

**发现的问题**：⭐ **一个语法错误的变异体也会让测试非零退出** ⇒
**"编译失败"被当成了"判据抓到了"**。

⇒ 🔴 那个变异体**一个判据都没跑过**，却显示绿色。

⚠️ **这比单个变异体的问题严重得多**：
它让"**变异测试全绿**"这个**整套判据有效性的证据**，
可以**由编译错误刷出来**。

**通用化**：任何"**用某种失败信号来证明某事**"的场合 ——
- 测试框架用退出码判定
- 用"抛异常"证明校验生效
- 用"构建失败"证明 lint 生效
- 用"请求被拒"证明鉴权生效
- 用"进程被杀"证明看门狗生效

⭐ **核心问题：失败信号的"来源"没有被区分。**

**可迁移的做法**

1. ⭐⭐ **区分"失败的原因"**：不是"有没有失败"，而是"**因为什么失败**"。
   本例：`syntax error` + **没有约定的失败标记** ⇒ ⭐ **判"无效"，⛔ 不是"通过"**。
2. ⭐⭐ **给"无效"单列一个状态**（本例 `Caught = $null`），
   ⛔ 不要塞进"通过/不通过"二元里 —— 否则它总会被算到某一边。
   ⭐ 与"跳过"同级别（`C29`）。
3. ⭐ **变异体的前置条件是"它能编译"** ——
   ⛔ 不是"它让回归变红"。**先证明变异体本身是活的**。
4. ⭐ **审查"判定逻辑"本身**（同 `C21`）：
   问一句"**这个判据有没有可能因为别的原因而成立？**"
5. 📌 **同族教训的第三次复发**（`C29` 跳过≠通过 → 75 号 没有结论≠通过 →
   80 号 空选择≠通过 → 本条 编译失败≠抓到）。
   ⇒ ⭐ **说明"把未知/无效当成功"是这类工具最顽固的缺陷模式**，
   每次加新判定路径都要专门检查它。

**关联**：`C29`（跳过≠通过）、`C35`（没有结论≠通过）、`C21`（判据本身要被验证）、
`C23`（先问变异体是否真违反规格）、`fpga/TROUBLESHOOTING.md` 81 号

---

## C40. ⭐⭐⭐⭐ **"模块级自检通过"可能只是因为它【不检查那一项】**（2026-09-21）

**场景**：为每个模块建了一个**自检工程**，跑通后认为"这个模块没问题"。

**发现的问题**：那个自检工程**只跑综合、不跑布局布线** ⇒
⭐ **它根本不检查时序**。于是三个模块"自检全绿"，
接进交付顶层后 **Fmax 从 55.96 MHz 掉到 8.424 MHz**、**1408 个 setup 违规**。

⚠️ 更值得记的是：**那个自检脚本的头部注释里其实写了"本工程只跑综合、不检查时序"** ——
但我在加模块时**只看了"有没有 ERROR"**，⛔ **没读那句限定**。

**通用化**：任何"**局部检查工具**"都适用 ——
单元测试（不查集成）、linter（不查运行时）、类型检查（不查语义）、
dry-run（不查实际副作用）、编译（不查链接/部署）、
mock 测试（不查真实依赖）……

⭐ **核心问题：把"某项检查通过"误读成"整体没问题"。**

**可迁移的做法**

1. ⭐⭐ **明确"这个工具检查什么、不检查什么"** ——
   并且⭐ **把"不检查的项"写进它的输出**（不只是写在注释里，
   因为注释会被跳过）。⚠️ 本例正是"限定写在注释里、没人读"。
2. ⭐⭐ **"综合通过 ≠ 时序收敛"、"测试通过 ≠ 集成可用"** ——
   ⭐ **每一项能力都要有自己的证据**（同族 `C31`：宽松前端掩盖严格前端）。
3. ⭐ **新东西落地时，跑一次"最完整的那条流程"**（本例：含 PnR 的构建），
   ⛔ 不要只跑最省事的那条。
4. ⭐ **失败定位时先看"最差路径落在哪个模块"** ——
   本例 25 条最差路径**全部**在一个模块里 ⇒ 定位极快。
   📌 **"全部集中在同一处"是极强的信号**，⛔ 不要平摊怀疑。
5. ⭐ **修完要复测"完整流程"**，⛔ 不能只看"错误没了"。

**关联**：`C31`（宽松前端掩盖严格前端）、`C37`（自检工程会改变被测对象）、
`C21`（判据本身要被验证）、`fpga/TROUBLESHOOTING.md` 82 号

---

## C41. ⭐⭐⭐⭐ **"简单变体跑通了"不能证明"完整变体可用" —— 必须把变量一个个摘掉做最小复现**（2026-09-21）

**场景**：写脚本调用外部黑盒（本例 Word COM）生成文档。脚本**一路跑到最后一步都正常**，
但**最后一步挂死**（无返回、无异常、无输出、进程一直挂着）。
而在此之前，**同一台机器、同一个 COM 接口、生成另一份更复杂的文档（32 个公式）是成功的**。

⚠️ 那个"之前成功过"的事实，很容易被当成"这条路是通的"，从而去**怀疑内容本身写错了**。

**怎么定位的：把"能用"和"不能用"的差别缩到最小**

| 试验 | 内容 | 结果 |
|---|---|---|
| 纯文字 + 最后一步 | 最小文本 | ✅ 通过 |
| 中文文件名 / 中文内容 | 编码嫌疑 | ✅ 通过 |
| 换输出目录（中文路径 / 纯 ASCII 路径） | 路径嫌疑 | ✅ 通过 |
| **只加 1 张图 + 最后一步** | 图形嫌疑 | ❌ **挂死** |
| 换掉外部依赖的配置项（默认打印机） | 配置嫌疑 | ❌ 仍挂死 |
| **只加公式（18 个）+ 最后一步** | 公式嫌疑 | ✅ 通过 |

⇒ **一句话判据：只要内容里有图片，最后一步就挂。** 与编码、路径、文件名、公式、外部配置**都无关**。

**为什么这一步值得花时间**

⭐ **"最小复现"把搜索空间从"整个脚本 300 行"压到"一个布尔条件"**，
之后**不再需要读文档、猜驱动、翻日志** —— 直接换实现即可。
⚠️ 反过来说，如果没有做这一步，最自然的动作是**去猜"是不是中文路径的问题"**
（而那一项**已经被证伪了**），会在错误的假设上耗掉大量时间（同族 `C06`：先判断问题在本地还是外部依赖）。

**可迁移的做法**

1. ⭐⭐ **"跑通"要区分是"哪个变体跑通"** ——
   本次 `COM 可用` 这个结论**只在"无图片"这个变体上成立**，⛔ 不能外推到"带图片"。
   📌 **记录"成功"时，必须连同它的前置条件一起记**（同族 `C31`：宽松前端掩盖严格前端）。
2. ⭐⭐ **挂死/静默失败要用"落盘日志"定位**，⛔ 不要靠标准输出 ——
   进程被强杀时，缓冲里的 stdout **可能整段丢失**；每步 `Add-Content` 到文件才能看到**最后一条**停在哪。
3. ⭐⭐ **任何调外部进程/黑盒的步骤都要有超时**（`Start-Job` + `Wait-Job -Timeout`）——
   ⛔ 否则"卡住"与"跑得慢"**无法区分**，会把整个会话拖死（同 `fpga/TROUBLESHOOTING.md` 72 号）。
4. ⭐ **挂死后先清理残留进程**（本例 `WINWORD`）再重试，残留会锁文件、让下一次表现更怪。
5. ⭐⭐ **当失败点已隔离清楚、但修复要依赖第三方黑盒时，先算"绕开"的代价** ——
   本例改用另一个库**直接写同一种文件格式**，一次成功、耗时从"几十秒不返回"变成毫秒级。
   📌 **"继续修黑盒"通常比重写一遍更贵。**

**关联**：`C31`（能跑通 ≠ 能被工具接受）、`C06`（先判断问题在本地还是外部依赖）、
`C17`（编码/BOM 类误判）、`fpga/FPGA工具使用经验.md` **U56**、`fpga/TROUBLESHOOTING.md` 72 号

---

## C42. ⭐⭐⭐⭐ **"它在动"不等于"数值对" —— 计数/编号类输出必须【数出来】验**（2026-09-21）

**场景**：写一个"节拍发生器"（相位累加器 + 输出脉冲/计数）。调试时连续踩两个坑：

| # | 现象 | 误导性 |
|---|---|---|
| **1** | **一个脉冲都不出**，但**使能/状态信号已正确拉高** | ⚠️ 看起来像"**使能没接对**" ⇒ 先去怀疑端口、复位、tb |
| **2** | 脉冲**正常出**了，但**上报的编号恒定差 1** | ⚠️ 波形"**看起来完全正常**" ⇒ 光看波形**永远发现不了** |

⭐ 两个坑**都不是**"语法错"或"跑不通"，而是**逻辑自洽、不报错、也不影响"有没有波形"**
⇒ **只有把数值【数出来】才能发现**。

**通用形态**：这类 bug 出现在**所有"状态既表示进度、又驱动事件"的结构**里 ——
计数器 / 累加器 / 分频器 / 状态机 / 序号生成 / 时间戳。

**可迁移的做法**

1. ⭐⭐ **判据要"数事件个数"，⛔ 不是"看信号有没有变"** ——
   本例 `120 000 clk 内 0 个脉冲` 是**最强的证据**，一句话就把范围锁死；
   而"`transport` 是不是高"这种检查**完全抓不到自锁**。
2. ⭐⭐ **凡是"脉冲 + 携带数据"（编号/时间戳/序号）的接口，
   判据必须逐拍打印 `第 N 个脉冲 → 上报值`** ——
   ⛔ 否则恒定偏移**在波形上不可见**（形状全对）。
3. ⭐⭐ **写"推进条件"时先画依赖图，确认无环** ——
   ⛔ **状态推进的门控条件里，不许出现"由该状态本身算出来的信号"**：
   那是**自锁**的标准形态（要靠结果才产生原因）。
4. ⭐⭐ **同一变量若既当"进度状态"又当"对外上报值"，语义必须先写清楚** ——
   本例"`cnt` 是本拍编号还是下一拍编号"没定义 ⇒ **连修两次都还差 1**。
   ✅ 稳妥做法：**为上报值单开一个输出寄存器**，在事件那一拍锁存**本拍的值**。
5. ⭐⭐ **测周期/间隔时，采样方式本身会制造"假抖动"** ——
   "等信号落回 0 再数"会因**采样相位 vs 脉冲宽度**吃进 0 或 1 个 clk
   ⇒ 间隔**假性差 2**，看起来像被测有抖动，**实际是测量方法引入的**。
   ✅ 固定用**同一时钟沿逐拍采样**（同族 `C36`：探针要打在真正被测的那条线上）。
6. ⭐ **"极差 = 0" 要按分频类型分档** ——
   小数分频（相位累加）的间隔只可能是 `floor`/`ceil` **两个相邻值**，
   ⛔ 所以严格"极差 = 0"**只在除数能整除时成立**；
   ✅ 判据 = ① 严格档（取 2 的幂）要求极差 **= 0**；② 一般档要求极差 **≤ 1** + **平均值**误差有界。
   📌 只写"≤1"就**验不出真抖动**，只写"=0"就**必然假失败**。
7. ⭐ **"越像成功了，越要小心"** —— 坑 2 的波形是**完全正常的**，
   差 1 只在**边界事件**（小节起点/强拍）上放大成一整格错位才被听见。

**关联**：`C33`（先怀疑自己的时基/假设）、`C36`（探针位置）、
`C35`（没有结论≠成功）、`C21`（判据本身要被验证）、`C29`（跳过≠通过）、
`fpga/FPGA逻辑调试经验集.md` **L68**、`fpga/TROUBLESHOOTING.md` **76/77 号**

---

## C43. ⭐⭐⭐ **一个访问器可能同时代表"两个字段" —— 先证明"故障真的存在"再修**（2026-09-21）

**场景**：一份 Excel 台账的交接文档里记着一条"已知坑"：

> 🧊 冻结窗格曾异常：历史上出现过 `A551`（几乎冻住整表），已修正为 `A2`。

这次维护时又读到 `ws.freeze_panes == 'A596'`（`openpyxl`），看着像**又冻住了 596 行**，
于是按旧记录"修"成 `A2`。

**实测（查 `xl/worksheets/sheetN.xml` 原始 XML）**：

```xml
<pane ySplit="1" topLeftCell="A596" activePane="bottomLeft" state="frozen"/>
```

⇒ ⭐ **`ySplit` 才是"冻结几行"，`topLeftCell` 只是"冻结区的滚动位置"**。
三个不同版本的文件（586/616/615 行）**`ySplit` 全都是 `1`** ——
**即始终只冻结首行，"几乎冻住整表"从未发生过**。

**根因（实测复现）**：`openpyxl` 的 `ws.freeze_panes` **getter 返回的是 `topLeftCell`**：

```python
ws.freeze_panes = 'A551'      # 写出 ySplit="550"  ← 这样才会真冻 550 行
ws.freeze_panes = 'A2'        # 写出 ySplit="1"
# 而 Excel/WPS 自己保存时只更新 topLeftCell（滚动位置），ySplit 保持不变
# 于是 getter 读出 'A596' —— 看着像故障，实际无害
```

| 属性 | 含义 | 决定冻结行数？ |
|---|---|---|
| `ySplit` | **真正冻结几行** | ✅ |
| `topLeftCell` | 冻结区**滚动位置** | ❌ |

**为什么危险**
1. ⭐⭐ **"修一个不存在的 bug"会持续消耗注意力**，而且**每次修完都"确认已修复"**
   （因为 setter 一写，getter 就读 `A2` 了）⇒ 形成一个**永远修不完的假故障**。
2. ⭐⭐ **错结论会被写进交接文档，并被下一代人当事实继承** ——
   本例旧记录把 `A551` 定性为"几乎冻住整表"，**误诊被固化成知识**。
3. ⭐ **它反过来掩盖真故障**：若哪天真把 `ySplit` 写大了，
   现场表现与"误诊记忆"**完全一样** ⇒ 会被当成老毛病而忽略。

**可迁移的做法**
1. ⭐⭐⭐ **修之前先证明故障存在**：读**原始工件**（XML/二进制/字节），
   ⛔ 不要只信高层 API 的读值。本例 `ySplit=1` 一句话就否证了"冻结 596 行"。
2. ⭐⭐⭐ **发现"同一个概念有两个字段"时，先查清哪个字段驱动行为**：
   凡 getter 与 setter **不是同一个字段**的 API（滚动位置 vs 冻结行数、
   显示值 vs 生效值、`top` vs `scrollTop`、UI 状态 vs 模型状态），
   ⛔ 读回来的值都可能**不是你要的那个量**。
3. ⭐⭐ **推翻旧结论要当场改旧记录**（本库硬性约定）：
   把"已修正为 A2"改成"**这是误诊**，真实机制是 `ySplit` vs `topLeftCell`"，
   **保留"错误 → 修正"的痕迹**，⛔ 不要静默删掉旧说法。
4. ⭐ **"我修好了"要有独立判据**：本例正确的判据是
   "查 XML 的 `ySplit` 是否 = 1"，⛔ 不是"getter 读出来是 `A2`"
   —— 后者**自己验证自己**（同 `C11`：判据要独立于被测量）。
5. ⭐ **区分"我改了它"与"它本来是坏的"**：
   本例改动前后 `ySplit` 都是 1 ⇒ 我的"修复"**没有改变任何实际行为**，
   只是把显示口径对齐了。**如实报告这一点**，⛔ 不要邀功成"修好了一个 bug"。

**通用化**：任何"**观测口径 ≠ 行为口径**"的场合 ——
UI 框架（滚动/尺寸/可见性）、Excel/Office 对象模型、浏览器 DOM、GUI 自动化、
ORM 的脏检查、编译器的"有效选项"、配置的"声明值 vs 生效值"……
⭐ 判据永远锚在**真正驱动行为的那一层**（同 `C05` 查源文件、`C08` 看原始数据）。

**关联**：`C05`（配置类事实一律查源文件）、`C08`（证据形态要匹配问题类型）、
`C11`（判据要独立于被测量）、`C25`（同为"API 语义与直觉不符"：
`None` 是跳过不是置空）、`C16`（同签名 ≠ 同病：这里更狠 —— **根本无病**）

---

## C44. ⭐⭐⭐⭐ **判据红了，先问"我算的那个数凭什么是对的" —— 容量/并发类算术一律写成【守恒不变量】**（2026-09-21）

**场景**：给一个"容量有限"的队列写溢出判据。队列**深度 4**，我灌 **12** 个事件，
写下的期望是：

```
期望丢弃数 = 灌入 12 − 深度 4 = 8
```

**现象**：🔴 **实测 7，判据红了。**

⚠️ **第一反应是"被测有 bug"**。逐拍追踪后真相**正好相反**：

| 事实 | 值 |
|---|---|
| 队列内部计数最大值 | ⭐ **精确到 4**（✅ 没有溢出覆盖，队列完好） |
| **被服务的事件** | ⭐ **5 个** |
| **被丢弃的事件** | **7 个** |

**根因**：⭐ **我的模型漏了一项** —— 队列深度是 4，但**消费者（状态机）同时在排空**
⇒ **一次突发里能被吸收的不止 4 个**。

**⭐ 修法：把判据从"硬编码算术"改成【守恒不变量】**

```
灌入总数 = 被服务数 + 被丢弃数        // 12 = served + lost
```

| 判据写法 | 能抓"静默丢" | 能抓"重复计数" | 会不会把"我的模型错"误报成"被测错" |
|---|---|---|---|
| ⛔ `12 − 4 = 8` | ❌ | ❌ | 🔴 **会** |
| ✅ **守恒式** | ✅ | ✅ | ✅ 不会 |

**可迁移的做法**

1. ⭐⭐ **判据红了，先问"我算的那个数凭什么是对的"** ——
   ⛔ 不要直接从"被测有 bug"开始查（同族 `C33`：先怀疑自己的时基/假设）。
   ⚠️ 本例若直接去查被测，会**白费很久**（因为被测本来就是对的）。
2. ⭐⭐ **凡涉及"容量 / 并发 / 排空"的算术，一律写成【守恒式】** ——
   ⚠️ 这类算术**人脑必然漏项**（本例漏了"消费者同时在干活"这一项）。
   ⭐ 换成守恒式后，**漏项会自己暴露**（两边对不上）。
3. ⭐⭐ **判据要打在【稳态】上，⛔ 不是过渡过程** ——
   ⚠️ 本例突发刚结束时消费者**还没开始服务**（实测"被服务数 = 0"）
   ⇒ **过早验守恒会误报**。✅ 先等它排空再验。
4. ⭐ **守恒式的额外好处：它【自解释】** ——
   打印出来就是 `12 = 5 served + 7 lost`，
   ⭐ **一眼看出到底发生了什么**，⛔ 不需要再猜"为什么是 7"。
   📌 这比"断言一个数字"**信息量大得多**。
5. ⭐ **"判据必须能失败"与"判据不能误报"是两个独立要求** ——
   ⚠️ 我为了让它"能失败"而写了个精确数字，结果**它失败了，但失败的原因是错的**。
   ⇒ ⭐ **好的判据要同时满足**：① 真 bug 时红 ② 我的模型错时不红。

**关联**：`C33`（先怀疑自己的时基/假设）、`C36`（判据的探针位置）、
`C21`（判据本身要被验证）、`C35`（没有结论≠成功）、`C29`（跳过≠通过）、
`fpga/TROUBLESHOOTING.md` **79 号**、`fpga/FPGA逻辑调试经验集.md` **L69**

---

## C45. ⭐⭐⭐⭐ **"漏检"有两种完全不同的原因：判据没覆盖 vs【变异体本身无效】—— 而后者常掩盖真缺陷**（2026-09-22）

**场景**：新写的两个模块，tb 全过，但变异测试报 **2 个 LEAK**（注入缺陷却没被抓住）。
⚠️ 我的**第一反应只有一种假设**："判据没覆盖" ⇒ 去补判据。
🔎 **查下去发现两个 LEAK 的原因【完全不同】**，而且**一个补判据根本没用**。

| | 第一反应 | 查下去的真根因 |
|---|---|---|
| **A** | 判据只验了"开机即停"，没验"**跑起来再停**" ⇒ 补一条 | ⚠️ **补完仍然 LEAK** —— 根因是：⭐ **被测对象在"停"的状态下根本不产生事件源** ⇒ **两种写法行为完全相同** ⇒ 🔴 **这个变异体是【无效】的**（不能改变行为） |
| **B** | 以为判据没覆盖"速度响应" | 🔴 根因：⭐ 变异体改的那个寄存器**只被写、从未被读**（**死状态**）⇒ 改它**不改变任何行为** ⇒ 又是无效变异体。⚠️ 而这**恰好掩盖了一个真缺陷**：原实现用组合值直接装载 ⇒ **音还在响时改参数，输出会中途突变**（违反"起音时刻锁存"） |

**⭐ 关键洞察（两条）**

1. ⭐⭐ **"漏检"必须先分两类**：

   | 类型 | 特征 | 正确动作 |
   |---|---|---|
   | **判据没覆盖** | 变异体**确实改变了行为**，只是没被观测到 | ✅ 补判据 |
   | **变异体无效** | 变异**改变不了行为**（死状态 / 被上游挡住 / 逻辑等价） | ⛔ **补判据永远没用** ⇒ 必须**换锚点**，或**先改 RTL 让那个状态真的被用** |
   ⚠️ 我第一反应只想到第一类，**多绕了一轮**。

2. ⭐⭐ **"防御性代码若从不被触发，就等于没有"**（本例 A）：
   被测模块里那道"停止时不发事件"的闸门是**冗余的**（上游已经挡住了）
   ⇒ ⚠️ **它从未被执行过** ⇒ 给了**虚假的安全感**。
   ✅ 正确做法：**构造一个能直接触发它的场景**（绕过上游，直接驱动它的输入）
   ⇒ ⭐ 防御**被真正证明**，同时那个变异体也**变成可观测**了。

**可迁移的做法**

1. ⭐⭐ **遇到"变异体没被抓到"，先问"它真的能改变行为吗"** ——
   ⛔ 不要默认是判据的问题。
2. ⭐⭐ **"只写不读"的寄存器是【死状态】** ——
   ⚠️ 它会让**指向它的变异体变成无效变异体**，从而**掩盖真实的语义缺陷**。
   ⭐ 自检：**每个寄存器都要能回答"谁读它"**。
3. ⭐⭐ **"冗余的防御"要被证明，否则删掉** ——
   ⚠️ 未被触发的防御 = 未验证的代码 = 假安全。
   ✅ 构造**直接驱动**的测试（绕过上游）来证明它有效。
4. ⭐ **基线的"两个方向"都要验** ——
   ⚠️ 本例：只验了"**从未启动**"，没验"**启动后再停**"。
   ⭐ 凡"状态 A → 状态 B"的约束，**两个方向都要有判据**。
5. 📌 **一个正面的副产品**：⭐ 为了"让无效变异体变得有效"，我**改进了一个真实的语义缺陷**
   （把"参数实时跟随"改成"起音时刻锁存"）⇒
   ⭐ **追查无效变异体这件事本身，常常能挖出真问题。**

**关联**：`C23`（无效变异体）、`C36`（探针位置）、`C44`（判据写法）、
`C21`（判据本身要被验证）、`fpga/TROUBLESHOOTING.md` **80 号**、
`fpga/FPGA逻辑调试经验集.md` **L70**

---

## C46. ⭐⭐⭐⭐ **生成脚本的产物一旦被【人工编辑】过，就再也不能"重新生成"——重跑会静默覆盖人工成果**（2026-09-22）

**场景**：用脚本（python-docx）产出一份文档交付。用户随后**在成品里手工插入了照片**。
之后需要再改一处内容，于是**重跑生成脚本**。

⇒ ⭐ **用户手工插入的照片被整体覆盖、无声丢失**。
⚠️ 脚本"跑成功"、文件也生成了，**没有任何报错** —— 损失只有打开文件才发现。

**为什么危险**：生成脚本的**输入是源码，不是当前产物**。
"重新生成"在语义上等于"**丢弃当前产物、从头再来**"，
⛔ 而人的直觉是"重新生成一下顺便把新改动加上"。

**正确做法：产物被人工编辑过 → 改为【定点修改产物】**

产物是 zip 容器（docx / xlsx / pptx / jar）时，**只替换目标部件，其余条目逐条原样复制**：

```python
zin = zipfile.ZipFile(doc)
with zipfile.ZipFile(tmp, "w", zipfile.ZIP_DEFLATED) as zout:
    for item in zin.infolist():
        data = zin.read(item.filename)
        if item.filename == TARGET:          # 只换这一个部件
            data = new_bytes
        zi = zipfile.ZipInfo(item.filename, date_time=item.date_time)
        zi.compress_type = item.compress_type      # ⭐ 保留原压缩方式/属性
        zi.external_attr = item.external_attr
        zout.writestr(zi, data)
```

⭐ **必须保留 `compress_type` / `date_time` / `external_attr`**，
否则"内容没变"却出现大面积 diff。

**操作纪律**

1. ⭐ **动手前先备份产物**（`copy` 一份 `.bak`）。
2. ⭐ **改完必须让原程序打开验证一次**（本例：Word 读 `OMaths.Count` /
   `InlineShapes.Count` / `Tables.Count` / 页数），确认人工成果还在。
3. 确认无误后再删备份。
4. ⭐⭐ **判断依据**：问"**当前产物里有没有脚本不知道的东西？**"
   有（人工插图 / 手改排版 / 批注）→ ⛔ **禁止重跑生成脚本**。

**通用化**：任何"**源码 → 产物**"的流程都适用 ——
代码生成器、报表导出、静态站点构建、PPT/文档模板渲染、配置文件生成。
⭐ **人工编辑过的产物就是"活的"，它已脱离生成脚本的管辖范围。**

**关联**：`C34`（换执行方式会改变隐含前提）、`C38`（半改状态被当成基准）、
`fpga/FPGA工具使用经验.md` **U56**（Word COM 挂死）、**U57**（定点压缩同一技法）

---

## C47. ⭐⭐⭐ **被要求"仿照某物的风格"时，要【量】不要【看】——把参考物解析成可计数的指标**（2026-09-22）

**场景**：用户要求"参照我原来的报告，写得简洁一点"。

**错误做法**：凭印象打开文件扫一眼，觉得"大概是这样"就开始写
⇒ 交出来的东西**格式对但"密度"不对**（每个小项写成 2～3 句，而参考物其实每个小项只有 1 句）。
⚠️ 用户只能反复说"还是有点啰嗦"，因为**双方都没有量化口径**。

**正确做法：把参考物解析成可计数的指标再动笔**

本例是 docx，直接解包逐段打印：

| 量什么 | 得到什么 |
|---|---|
| 段落**文本长度** | 每段是长句还是短句 |
| 每段的**句数** | ⭐ 参考物是"一个小项 = 一句" |
| **标签与内容是否同段** | `（1）名称：内容` 还是拆成两行 |
| 有无**总起段** | 参考物标题后直接进小项 |
| 字号 / 段前段后距 | 排版参数 |

```python
# 把参考 docx 逐段的长度/字号/段后距打出来
for para in Document(ref).paragraphs:
    t = para.text.strip()
    print(len(t), para.paragraph_format.space_after, t[:60])
```

**效果**：一量就发现"参考物每小项 1 句、标签与内容同段、无总起段"，
照此改写后**一次通过**，⛔ 不用来回猜。

**通用化**

1. ⭐⭐ **"风格"这类模糊要求，先找可量化的代理指标**（长度 / 句数 / 段数 / 缩进 / 字号），
   把"像不像"变成"数字对不对"。
2. ⭐ **量出来的口径要落成文档**（本例写成一份写作规范 md），
   下次直接照抄，⛔ 不要每次重新猜。
3. ⭐ **参考物本身可能有多个版本**（历史文件风格不统一）→
   量完要指出"**以当次模板为准**"，⛔ 不要照抄旧版标题。

**关联**：`C41`（记录成功要连同前置条件）、`C08`（证据形态要匹配问题类型）

---

## 配套文档（互相索引）
- FPGA 环境/操作/报错 → `fpga/FPGA问题排查手册.md`（纯数字编号）
- FPGA 逻辑/时序/设计 → `fpga/FPGA逻辑调试经验集.md`（L 编号）
- FPGA 工具操作 → `fpga/FPGA工具使用经验.md`（U 编号）
- FPGA 完整排查过程（含失败路径） → `fpga/TROUBLESHOOTING.md`（T 编号）
- 电子元器件 → `器件积累.md`
- 本文件 → 跨语言共性（C 编号）
---

## C48. ★★★★★ **用“写模式”打开文件 = 先清空再写；中途报错就只剩一个空文件**（2026-09-22）

**场景**：用脚本往一份已有文档里插内容。脚本报了一个编码错，
修完再跑，**文档变成了 0 字节**。

**根因**：`open(path, "w")` 是**截断式**的 ——
它**在写第一个字节之前就把原内容丢了**。
而编码错误发生在**后面的 `write()`** 阶段
⇒ **异常抛出之时，文件已经空了**。

**为什么危险**：人会把“脚本报错”当成“什么都没发生”，
但实际上**两件事是独立的**：操作失败了，**但破坏已经发生了**。

**正确写法（先写临时，再原子替换）**：

```python
import io, os
tmp = p + ".tmp"
with io.open(tmp, "w", encoding="utf-8", newline="\n") as f:
    f.write(new_text)     # 失败只会死在 tmp，目标文件完好
os.replace(tmp, p)        # 原子替换（同盘符下是原子的）
```

**可迁移的做法**：

1. **任何“用程序改文件”都不要用截断式打开** ——
   这不是 Python 特有：**Shell 的 `>`**、很多语言的 `open(w)` **都是先清空**。
2. **“操作报错”≠“没有副作用”** ——
   判断一个脚本是否安全，要看它**在报错之前已经做了什么**。
3. **改文件前先 commit** —— 本例能完整恢复完全是因为刚好 commit 过；
   **没 commit 过的就真丢了**。
4. **写入类操作要“先构建完整内容再落盘”**。
5. **脚本里避免超出 BMP 的字符**（或用完整代理对）——
   否则会在写入阶段报 `surrogates not allowed`。

**关联**：`fpga/TROUBLESHOOTING.md` **81 号**、`fpga/FPGA工具使用经验.md` **U58**、
`C17`（BOM/编码）、`C34`（换执行方式改变隐含前提）、`C06`（先判断问题在哪里）

---
---

## C49. ★★★★ **“完美输入”测不出“选错了参数” —— 判据要引入真实偏差**（2026-09-22）

**场景**：一个接收器在“位中点采样”。
变异体把采样点改到**位边沿**，
而测试向它发送的信号**每一位都分毫不差** ⇒
⭐ **边沿采样也能读对** ⇒ **测试完全没发现**。

**为什么**：⭐ “完美对齐”下，边沿与中点的区别**不会体现出来**。
⭐ 而真实系统**一定有偏差**（时钟、波特率、温度、噪声…），
偏差会让采样点**逐位漂移**；⭐ 此时中点采样能容忍近半位，
而边沿采样**一点偏差就错**。

**可迁移的做法**：

1. ⭐⭐⭐ **判据里必须引入“真实会遇到的非理想因素”** ——
   ⛔ 只用理想输入 = 只能证明“它在理想下能工作”。
2. ⭐ 具体做法：发送方**故意偏快/偏慢几个百分点**。
   ⭐ 实测：加了 ±3~4% 偏差的判据后，变异体**立刻被抓**。
3. ⭐ 同理适用：**边沿检测 / 去抖 / 跟踪 / 阶梯响应 / 阈值判决**
   —— ⛔ 都不能只用完美波形测。
4. ⭐ 这是“探针要打在真正被区分的那条线上”的另一种形态：
   ⚠️ 不是探针**位置**错，而是**激励信号太干净**。

**关联**：`fpga/FPGA逻辑调试经验集.md` **L71**、`C36`（探针位置）、
`C21`（判据本身要被验证）、`fpga/TROUBLESHOOTING.md` **82 号**

---


## C50. ★★★★★ **"只有一条分支坏"的缺陷，会被"整体看起来正常"掩盖**（2026-09-22）

**这个类的形状**：

> 一段逻辑在**绝大多数输入下都对**，
> ⚠️ **只有某一种特定的输入组合**走错；
> ⭐ 而**这个组合恰好不是"日常路径"** ⇒ 平时完全看不出来。

**本次实例**（两路事件合流模块）：

```verilog
wire push_n = (push_a ? 16'd1 : 16'd0) + (push_b ? 16'd1 : 16'd0);
// wire 不写位宽 = 1 位 ⇒ 1+1 截断成 0
```

| 输入 | 结果 |
|---|---|
| 单路（日常） | ✅ 正常 |
| **两路同拍** | 🔴 **静默什么都不做** |

**⭐⭐ 为什么这个类特别危险**：

| # | 机制 |
|---|---|
| **1** | ⭐ **"整体功能看起来是对的"** —— 该模块 8 条判据里 **7 条全绿** |
| **2** | ⚠️ **同模块的旁路指标可能【照常工作】** —— 相撞计数器 +1 了，⛔ 但数据没进去 ⇒ ⭐ **"检测到了但没处理"看起来像"处理了"** |
| **3** | ⚠️ **现象指向错误方向** —— 打印"相撞被吞了"，让人去查仲裁逻辑，⛔ 而真因是位宽 |
| **4** | ⚠️ **只有"专打那个组合"的判据能抓到** —— 覆盖率/用例数**全部骗人** |

**⭐ 通用对策（跨语言跨领域都成立）**：

1. ⭐⭐ **为"边界输入组合"单独写判据** ——
   ⛔ 不要指望"用例多"能覆盖，⚠️ **要专门点名那个组合**；
2. ⭐⭐ **旁路指标（计数器/日志）"正常"不能当作"主路径正常"** ——
   ⚠️ 要断言**最终结果**，⛔ 不是"我检测到了"；
3. ⭐ **发现"某条判据红了但别的都绿"时，第一反应应该是
   "这个组合是不是被特殊处理了"** ——
   ⚠️ 而不是"是不是 DUT 整体坏了"；
4. ⭐⭐ **可用"变异体"反向验证判据有效性**：
   本次把**这个 bug 本身**做成变异体 ⇒ 若那条判据不存在，**变异体会 LEAK**
   ⇒ ⭐ **用变异体证明"这条判据是必要的"，而不只是"它是绿的"**。

> 📌 **同族**：`C45`（漏检的两种原因：判据没覆盖 / 变异体本身无效）、
> `C36`（探针要打在真正被改的那条线上）、`L72`（同一次实测的逻辑侧结论）。


## C51. ★★★★★ **"全绿"证明的是【你构造过的场景】，⛔ 不是"东西是对的"**（2026-09-22）

**这次实测的形状**：

> 一个模块：**16 项判据全过** + **变异体 4/4 全部被抓** + 回归全绿。
> ⭐ 事后发现里面藏着**三个真缺陷**，⚠️ **一条判据都没抓到**。

**⚠️ 危险的推理链**（我实际走过的）：

```
判据全绿          -> "功能是对的"
变异体全被抓      -> "判据是有效的"
两条合起来        -> 🔴 "这个模块可以信了"
```

⭐ **第三步是错的。** 前两条为真，**推不出第三条**：

| 已有证据 | ⭐ 它真正证明的 |
|---|---|
| 判据全绿 | ⭐ **"我构造的那些输入"** 是对的 |
| 变异体全被抓 | ⭐ **"我的判据能区分这些变异体"** |
| ⚠️ **两条都没有涉及** | 🔴 **"还有哪些输入我没构造过"** |

**⭐ 这个类的通用特征**：

| # | 特征 |
|---|---|
| **1** | ⭐ **缺陷藏在"没人想到要构造的输入组合"里**（⚠️ 本次是"同一时刻多条"） |
| **2** | ⚠️ **判据条数越多，越容易产生"覆盖很全"的错觉** —— ⭐ 16 条全是同一维度的 |
| **3** | ⚠️ **变异体也救不了** —— ⭐ 变异体只能检验"已有判据"，⛔ **不能凭空生成维度** |

**⭐⭐ 通用对策（跨领域）**

1. ⭐⭐ **把"绿"当成【假设】，⛔ 不是【结论】** ——
   ⭐ 它值得信，但**必须能被推翻**；
2. ⭐⭐ **主动问"我漏掉了哪个维度"** ——
   ⭐ 本次的答案是**"输入组合"**：判据按**功能点**铺开，⛔ 没按**组合**铺开；
3. ⭐ **用真实数据反推组合空间**（⚠️ 本次实测"一个时间点最多 24 条"，
   ⭐ 而我的直觉是"4~6 条"，**差了 4 倍**）；
4. ⭐⭐ **换个下游需求去反推上游接口** ——
   ⭐ 本次缺陷**不是被测出来的**，是"准备做上游时顺手确认下游需要什么"时发现的
   （⚠️ 只 grep 了一个常量，就看出它**从未被使用**）；
5. ⭐⭐ **"声明了但从未被使用"是极强的信号** ——
   ⚠️ 常量/端口/字段**存在却没被读**，通常意味着一整块逻辑漏了。

> 📌 **同族**：`C45`（漏检的两种原因）、`C36`（探针要打在真正被改的线上）、
> `C50`（只有一条分支坏的缺陷）、`L73`（判据要覆盖输入组合）、`87 号`（本次全流程）。

**📌 一句话**：
> ⭐⭐ **"我测过的都对" ≠ "它是对的"。**
> ⚠️ 两者的差距，等于**你没测过的那部分**。


## C52. ★★★★★ **长任务运行期间的一切"快照"都是中途状态，⛔ 不是最终结果**（2026-09-22）

**这次实测的形状**：

> 一个批处理脚本，对**每一项**都做"改文件 → 跑测试 → 恢复文件"。
> ⭐ 它**运行期间**会不断重写一批**被跟踪的证据文件**。
> ⚠️ 我在它运行中 `git add` 了那批文件
> ⇒ 🔴 **提交进去一批 0 字节 / 满篇 FAIL 的"验收证据"**。

**⭐⭐ 这个类的通用形状**：

| # | 特征 |
|---|---|
| **1** | ⭐ **长任务会反复改写某批文件**（日志、报告、缓存、生成物） |
| **2** | ⚠️ **那些中间状态看起来"像是结果"** —— ⚠️ 文件名一样、格式一样，⭐ **只是内容不对** |
| **3** | ⚠️ **没有任何东西会报错** —— git 提交成功、文件都在、脚本也没崩 |
| **4** | 🔴 **最坏的情况是"空文件"** —— ⭐ 它**看起来像一个通过的结果** |

**⭐⭐ 通用对策（跨领域）**

1. ⭐⭐ **"有长任务在跑"必须是机器可判定的状态** ——
   ⭐ 一个 `.lock` 文件就够了（⚠️ 本次脚本本来就有 `build/mutation/.lock`！）
   ⇒ ⭐ 守卫据此**自动拒绝提交**，⛔ 不靠人记得；
2. ⭐⭐ **提交前看增删行数**（见 `L75`）—— ⚠️ `-2255` 就是红旗；
3. ⭐⭐ **读脚本自己的注释** —— ⚠️ 本次那个脚本**文件头就写明了**
   "每次变异运行都会覆盖已提交的证据，所以最后一步是干净重跑"，
   ⭐ **它把坑和解法都写了，我没读**；
4. ⭐ **任务结束后要"确认它真的结束了"** ——
   ⚠️ 进程赖着不退时，**"在跑"和"卡住"外观一样**（⭐ 同族：`88 号`）；
5. ⭐ **中间态被提交后，要能干净地回退** ——
   ⭐ 前提是"那个提交还没 push"（⚠️ 所以**本地多提交、晚 push** 是有价值的）。

> 📌 **同族**：`88 号`（构建卡住⇒证据没更新⇒拿旧数字当新数字）、
> `L74`（退出码 ≠ 产物新鲜）、`C51`（"全绿"只证明你构造过的场景）、
> `C45`（漏检的两种原因）。

**📌 一句话**：
> ⭐⭐ **"文件在那儿" ≠ "内容是对的"。**
> ⚠️ **运行中的系统，任何快照都只是过程量。**

---

## C53. ★★★★ **Windows 上工具链遇到"非 ASCII 路径"会以【多种互不相似】的方式碎掉，且没有一种会提示是路径问题**（2026-09-23）

**这次实测的形状**：

> 在 Windows 中文环境下配 ESP-IDF，工程目录含中文（`桌面\项目\...`）。
> ⭐ 同一个根因，在**四个不同组件**上以**四种完全不同的报错**出现。

| # | 组件 | 报错形态 | 像是什么问题 |
|---|---|---|---|
| 1 | Python `json.load` | `UnicodeDecodeError: 'gbk' codec can't decode byte 0x80` | ⚠️ 像"文件损坏" |
| 2 | `ccache`（C++） | `filesystem error: Cannot convert character sequence` | ⚠️ 像"ccache 自身 bug" |
| 3 | Python → 原生 `objdump.exe` 传参 | `.../目/esp32-/...: No such file or directory` | ⚠️ 像"文件真的不见了" |
| 4 | PowerShell `Expand-Archive` | **不报错**，静默解压出残缺目录树 | 🔴 **像"安装成功"** |

**⭐⭐ 这个类的通用形状**：

| # | 特征 |
|---|---|
| **1** | ⭐ **同一根因，多种面孔** —— 每个组件各自用自己的方式失败（locale 解码 / `std::filesystem` / argv 编码 / MAX_PATH） |
| **2** | 🔴 **没有一种报错会提到"路径"** —— 全部指向别处，⭐ 极易误诊 |
| **3** | ⚠️ **最危险的是第 4 种：不报错** —— ⭐ 它让"残缺"看起来像"成功"（同族 `C20`） |
| **4** | ⭐ **修好一个不等于修好全部** —— 本次修完工具链仍失败，因为后面还排着 3 个 |

**⭐⭐ 定位方法（跨领域通用）**

1. ⭐⭐⭐ **先做对照实验，⛔ 不要先咬定原因**：
   同一份输入，在 **ASCII 目录**与**非 ASCII 目录**各跑一次。
   ⚠️ 本次一开始怀疑"中文路径"，但对照实验显示 ASCII 目录**也失败**
   ⇒ ⭐ **真因是工具链残缺**（另一码事）。
   ⭐ 若不做这一步，会一路去改路径，**永远修不好**；
2. ⭐⭐ **"换纯 ASCII 路径试一次"是判决性实验** ——
   一次就能把"路径类问题"与"其它问题"分开，成本极低；
3. ⭐⭐ **判据落在交付物上，⛔ 不是日志的结论行** ——
   ⚠️ 安装器的 `Decompression completed successfully` **是假的**，
   ⭐ 真正的判据是"全树能不能搜到 `cc1.exe`""能不能产出 `.bin`"；
4. ⭐ **计数是最省事的完整性判据** ——
   文件数/体积与预期差一个数量级（150MB vs 1241MB、131 vs 2055 文件）**一眼就能看出**。

**⭐⭐ 通用对策**

| 场景 | 做法 |
|---|---|
| **新建工程 / 项目根目录** | ⭐⭐ 一律**纯 ASCII 路径**（⛔ 中文、⛔ 空格、⛔ emoji） |
| **Python 处理含非 ASCII 的路径** | 开 `PYTHONUTF8=1`（PEP 540）—— ⚠️ 但它**只管文件 I/O，管不了 argv 传给原生程序** |
| **构建缓存类工具（ccache 等）** | ⚠️ 含非 ASCII 路径时**直接关掉**，⭐ 其报错与路径毫无关联，排查成本极高 |
| **命令行工具（objdump/7z/tar 等）** | ⭐ 用**长路径感知**的工具替代（如 `tar.exe`/bsdtar），⛔ 不用 `Expand-Archive` |

> ⚠️ **注意**：`LongPathsEnabled=1` 只对声明了 `longPathAware` 清单的程序生效。
> **Windows PowerShell 5.1 不在其列** ⇒ 即使系统开了长路径，`Expand-Archive` 照样截断。

**📌 一句话**：
> ⭐⭐ **在 Windows 上，"路径里有没有非 ASCII 字符"是一条【必须先排除】的变量。**
> ⚠️ 它的症状五花八门，**而对照实验只要一分钟**。

> 📌 **同族**：`TROUBLESHOOTING.md` **90 号**（完整排查过程）、
> `C17`（编码/BOM 归属）、`C20`（存在性 ≠ 可用性）、`C16`（严重性由下游指标裁决）。

---
---

## C54. ★★★★★ **"看起来是集合"的东西可能是【标量】，而索引它不报错 —— 错得完全静默**（2026-09-23）

**场景**：用脚本按"行号"改一份文档，读进来 `$c[i]` 改完写回。
**没有任何报错**，但 272 行的文档变成了 **1 行**。

**根因（两个机制叠加，缺一不可）**

1. ⭐⭐ **"读所有行"的返回类型【由内容决定】，不由函数名决定**：
   - **多行** → 数组（元素是行）；
   - **单行 / 空** → **裸标量**。
   ⚠️ 而**对标量做整数索引在很多语言里是合法的**（取第 i 个字符/元素）⇒
   ⭐ **"我以为是第 28 行，实际是第 28 个字符"**，且**不抛异常**。
2. ⭐⭐ **写回时的"不追加分隔符"选项**把数组元素**首尾相连** ⇒
   N 行变 1 行。⚠️ **即使数组本身是对的**，这个选项照样把文件压平。

**可迁移的做法**

1. ⭐⭐⭐ **改文件优先用"字面量查找/替换"语义的编辑工具**（读全文 → 精确子串替换），
   ⛔ 不要用"取第 i 行、改它、写回"这种**依赖集合类型**的写法。
2. ⭐⭐⭐ **任何"读全部 → 改 → 写回"的批处理，写完必须验【结构量】**
   （行数 / 增删行数 / 条目数），⛔ **不是只看"有没有报错"** ——
   ⭐ 本类故障**零报错**（同 `L74`/`C52`：**成功退出码 ≠ 产物正确**）。
3. ⭐⭐ **⛔ 不要用"抑制行分隔符"的写入选项**去写"多行内容"，
   ⚠️ 它在语法上完全合法，会把所有行拼成一行。
4. ⭐⭐ **改文件前必须留"可回退点"**（版本控制提交 / 备份）——
   ⚠️ 这类破坏是**整份覆盖**而非零星改动，**没有回退点就是永久丢失**（同 `C48` 第 3 条）。
5. ⭐ **先排除"编码"再怀疑别的**：出现"文件内容看着不对"时，
   **先回读首字节/编码**（`C17`）——⚠️ 本次第一反应就是编码，**方向错了**，
   真实原因是类型与分隔符。

**与 `C48` 的区别（⭐ 重要：这是【不同】根因）**

| | `C48` | `C54`（本条） |
|---|---|---|
| 触发 | **截断式打开**（写模式先清空） | **类型/分隔符**（集合退化成标量 + 抑制换行） |
| 报错 | ⚠️ **有报错**（但破坏已发生） | ⭐ **完全无报错** |
| 结果 | 文件**变空**（0 字节） | 文件**被压成 1 行**（内容还在，但结构没了） |
| 对策 | 先写 `.tmp` 再原子替换 | 用字面量替换语义 + **验结构量** |

> ⭐ **两者共同的教训**：⭐ **"操作报错"与"有没有副作用"是两件独立的事**；
> ⭐ 而**"没有报错"更不能当"没改坏"**。

**📌 一句话**
> ⭐⭐ **凡是"按行/按条目索引"的批处理，先确认拿到的【到底是集合还是标量】；
> 写完立刻数行数。** ⚠️ 这类错误的特征是**静默**，只能靠**结构量核对**发现。

> 📌 **同族**：`fpga/TROUBLESHOOTING.md` **91 号**（完整复现过程）、
> `C48`（截断式打开，**不同根因**）、`C25`（写入型 API 的静默跳过/串行）、
> `C17`（编码先查首字节）、`L74` / `C52`（成功 ≠ 产物对）、`C30`（退出码不是唯一判据）。
---
---

## C55. ★★★★★ **"结论的寿命 = 载体的寿命" —— ⛔ 不要让结论只活在【调用方的临时设施】里**（2026-09-23）

**场景**：一个耗时 4 小时的后台任务。调用它时拼了个"存日志"的管道
（`... | Tee-Object -FilePath <log>`），看起来结果有落地。
后来这次调用被中断 ⇒ **4 小时的成果一条都没拿到**。

**根因（两个独立缺陷叠加）**

1. ⭐⭐ **被调用的程序自己不保存结果** —— 它**只往 stdout 打印**。
   ⇒ 🔴 **结论的寿命 = stdout 的寿命 = 调用它的那个进程的寿命。**
2. ⭐⭐ **"调用方临时搭的落盘设施"与被调方【共享命运】** ——
   那个日志文件由**调用方的管道**创建，位于**同一个进程树**里。
   ⇒ ⚠️ 调用方被中断 ⇒ **管道断开、日志停在半路、被测任务一起死**。

⭐ **而它造成的是"虚假的安全感"**：你**确实**有一个日志文件，
⚠️ 所以**看起来**"已经存下来了"，只有真出事才发现它**什么都没保住**。

**可迁移的做法**

1. ⭐⭐⭐ **凡"跑很久才有结论"的任务，先问：「这个结论【独立】存在哪里？」**
   ⚠️ 若答案里出现 **"调用方的 stdout / 管道 / 当前会话 / 我起的那个 tee"**
   ⇒ ⭐ **它就是没存**。
2. ⭐⭐⭐ **让"保存结果"由一个【独立进程】负责**，它只读、不参与业务、
   ⛔ 不碰锁与被测文件。⭐ 这样**它的死活与被测任务、与调用方都无关**。
3. ⭐⭐ **⛔ 不要用"阻塞等待"去等长任务** ——
   ⚠️ 等待与被等的任务常在**同一个进程树**里 ⇒
   🔴 **等待被中断 = 被等的任务被连带杀死**（本次的实锤）。
   ✅ 正确做法：**结束本轮等通知**，或**用独立进程轮询一个"完成信号"**。
4. ⭐⭐ **判"任务是否还在跑"要看【进程是否存活】，⛔ 不是看锁文件** ——
   ⚠️ 锁是**文件**，进程死了它**不会自己消失**；
   ⭐ 反过来，**"锁还在"曾被用来误判"还在跑"**。
5. ⭐⭐ **判据里要包含"结果文件存在且非空"** ——
   ⛔ 不能只说"任务跑完了"（同 `C34`：**"全绿" ≠ "证据还在"**）。
6. ⭐ **文件被中途改过之后，要先恢复再重跑** ——
   ⚠️ 本次若不先清掉残留，**带着旧缺陷重跑会产出整轮错误结论**。

**判据一句话**
> ⭐⭐ **"结果存在哪"必须能回答成一个【不随任何一次调用死亡】的位置。**
> ⚠️ 任何"临时搭起来的"载体，都要假定它**随时会消失**。

> 📌 **同族**：`fpga/TROUBLESHOOTING.md` **92 号**（完整过程）、`fpga/FPGA工具使用经验.md` **U63**（工具写法）、
> `C38`（"改文件再还原"的工具被杀 ⇒ 世界停在半改状态）、`C34`（换执行方式 ⇒ 产物静默丢失）、
> `C52`（运行中的快照都是过程量）、`C29`（跳过 ≠ 通过）、`C35`（"没有结论"是第三种状态）。
---
---

## C56. ★★★★★ **多线并行 / 多分支的工作区里，⛔ "提交"前不查分支 = 把工作交到别人手上**（2026-09-23）

**场景**：同一个仓库里**两条线并行推进**（A 线做主体功能、B 线做外围集成）。
我在 A 线的任务里做完了一轮工作，直接 `git add <我的文件>` + `git commit`。
⚠️ **提交成功了**，但我没注意 —— **当前分支早已被切到 B 线的特性分支上**
（是**之前某次会话**切的，我接手时没察觉）。

⇒ 🔴 **结果**：A 线的工作**落到了 B 线分支上**。

**为什么危险（不是"提交失败"，而是"提交成功但归属错了"）**

| 后果 | 说明 |
|---|---|
| ⭐ **后续 push 会替别人做决定** | 提交混在 B 线分支上 ⇒ 压提交/推送时**无法把两条线分开** |
| ⭐ **B 线可能不知道自己的分支多了东西** | 他们以为自己分支上只有自己的提交 |
| ⚠️ **A 线的主分支反而"没有"这次工作** | 看 `main` 会以为这轮什么都没做 |
| ⚠️ **回滚/归档时切错基准** | 按分支打 tag、按分支回退，都会带错内容 |

⭐ **最坑的地方：全程【零报错】**。`git commit` 输出完全正常，
⚠️ **不查 `git branch --show-current` 就看不出来**。

**可迁移的做法**

1. ⭐⭐⭐ **提交前第一件事：确认【当前分支】和【当前仓库】**
   （`git branch --show-current` / `git rev-parse --show-toplevel`）。
   ⭐ 尤其在**多条线共用一个工作区**、或**接手别人会话**的场景下 ——
   ⚠️ **分支状态是上一个会话留下的**，⛔ 不能假设它是你想要的那个。
2. ⭐⭐⭐ **提交后立刻看 `git status -sb`** ——
   ⭐ 它会打印 `## <分支名>...<upstream>`；
   ⚠️ **若分支名不是预期的那个，或没显示 upstream**，⇒ **当场停下**。
3. ⭐⭐ **一次提交只做一条线的事**，且**用显式文件路径**
   （⛔ 不用 `-A` / `.`）—— ⭐ 这样即使分支错了，**至少能清楚地 cherry-pick 搬回来**。
4. ⭐⭐ **发现归属错了，用 `cherry-pick` 搬，⛔ 不要 reset 强推** ——
   ⭐ 本例修法：切回目标分支 → `cherry-pick <我的提交>` →
   **把误落的分支用 `git branch -f` 退回它原来的位置**（⚠️ 前提：已确认那个提交确实是自己的、且只含自己的文件）。
5. ⭐ **共享仓库里，"谁的分支"要能一眼看出** ——
   ⛔ 不要让两条线的提交长期混在同一分支上（⚠️ 这会同时破坏**归属**与**可回滚性**）。

**判据一句话**
> ⭐⭐ **提交的归属由【当前分支】决定，而当前分支是【上一个会话】留下的状态。**
> ⇒ ⭐ **凡"提交"这类有归属语义的动作，先读一次当前状态，⛔ 不要靠记忆。**

> 📌 **同族**：`C28`（关于外部状态的判断必须现查）、`C57`（同类：多线并行下的越界）、
> `C38`（工具中途被杀 ⇒ 状态停在半改）、`C52`（运行中的快照都是过程量）。

---

## C60. ★★★★★ **"用 shell 做读-改-写"时，未指定编码 ⇒ 内容被【静默转码】损坏；而代码是 ASCII 所以构建照样通过**（2026-09-23）

**场景**：要给一批 C 源文件做**批量标识符重命名**（`BRIDGE_*` → `BLE_*`）。
为了"临时切一个编译开关做验证"，我先用 PowerShell 做了：

```powershell
(Get-Content $f -Raw) -replace 'OLD', 'NEW' | Set-Content -Encoding UTF8 $f
```

⚠️ **看起来完全合理**（甚至显式写了 `-Encoding UTF8`），但：

> 🔴 **`Get-Content` 默认按【系统本地代码页】（中文 Windows = GBK）解码文件。**
> 而文件是 **UTF-8** ⇒ 中文注释在这一步就已经变成乱码字符串，
> `Set-Content -Encoding UTF8` 再把**乱码**写成 UTF-8 ⇒ ⭐ **内容被永久损坏**。

**实测损伤**：一个 121 行的头文件，**28 行中文注释全部变成 `馃敶` / `鈬?` 这类乱码**。

**🔴 为什么特别危险 —— 它不报错、且构建照样通过**

| 层 | 后果 |
|---|---|
| **注释** | 🔴 全坏 |
| **代码** | ✅ **没事** —— `#define`、标识符全是 ASCII |
| **编译** | ✅ **零 error 零 warning** ⇒ ⭐ **构建全绿完全掩盖了损坏** |
| **发现方式** | ⚠️ **只有肉眼看 diff / 读注释**才能发现 |

⇒ ⭐⭐ **这正是本项目最怕的一类**：**判据（编译）通过，但产物已经被破坏**。
⚠️ 而且**损坏在"读-改-写"的读那一步就发生了** ——
`-Encoding UTF8` 写在**写**的一侧，**救不了读的一侧**。

**⭐⭐ 这个类的通用形状**

| # | 特征 |
|---|---|
| **1** | ⭐ **"读"和"写"各有独立的编码假设**，只设一个等于没设 |
| **2** | ⚠️ **默认编码 = 系统代码页**，在非英文 Windows 上**不等于 UTF-8** |
| **3** | 🔴 **损坏过程静默** —— 没有异常、没有警告、退出码 0 |
| **4** | 🔴 **判据（编译/测试）可能照样通过** ⇒ ⭐ 构建绿 ≠ 文件没坏 |
| **5** | ⚠️ **只在"内容含非 ASCII"时才发作** ⇒ 纯英文项目永远碰不到，**没有肌肉记忆** |

**可迁移的做法**

1. ⭐⭐⭐ **批量改含非 ASCII 的文件，用【显式编码】的脚本语言**（Python `io.open(..., encoding="utf-8")`），
   ⛔ **不要用 shell 的 `Get-Content`/`Set-Content`（或 `cat`/`sed -i` 未指定 locale）做读-改-写**。
2. ⭐⭐⭐ **能用编辑工具就用编辑工具** —— 它按文件本身的实际编码处理，
   ⛔ 不引入"系统默认编码"这个隐藏变量。
3. ⭐⭐ **改完必须验证非 ASCII 部分**，⛔ 不能只看构建通过 ——
   ⭐ 判据要**打在会被损坏的那一层**（注释/中文字符串），⛔ 不是打在编译结果上（同族 `C36`）。
4. ⭐⭐ **改文件前先 `git commit`（或至少确认工作区干净）** ——
   ⚠️ 损坏后是 `git checkout` 恢复的，**没提交过就没得恢复**。
5. ⭐ **临时切开关不要改文件** —— 改用**编译期 `-D`**（见 `C61`），
   从根上避免"为了验证而破坏源文件"。

**📌 一句话**：
> ⭐⭐ **"读"和"写"是两个独立的编码决策；只设一个，等于给文件做了一次静默转码。**
> ⚠️ 而**构建全绿**会让这件事看起来像没发生。

> 📌 **同族**：`C17`（BOM 归属由读取方决定）、`C48`（写模式打开=先清空）、
> `C53`（非 ASCII 路径的多种碎法）、`U61`（`.ps1` 必须纯 ASCII）、
> `C36`（探针必须打在真正被改的那条线上）、`TROUBLESHOOTING.md` **94 号**。

---

## C61. ★★★★★ **构建开关"设了但不生效"，而**报错指向完全无关的方向** —— 三个真实案例（2026-09-23）**

**场景**：给一个 ESP-IDF 工程加"编译期开关"（模式 A/B、是否接外围）。
⚠️ 连续撞上**三个形状完全不同、但本质相同**的故障。
⭐ 它们的共同点是：**报错信息全部指向错误的方向**。

### 案例一：`-D` 只进了构建系统缓存，到不了编译器

```powershell
idf.py -DXXX=1 build      # ⚠️ 看起来成功
```

🔴 **实际**：`-D` 只创建了**构建系统的缓存变量**
（实测 `build/CMakeCache.txt` 里只有 `XXX:UNINITIALIZED=1`），
⭐ **从未变成 `-D` 传给 C 编译器**。

**症状**：⚠️ **两种"不同配置"编出【逐字节相同】的二进制**，而构建**一声不响**。

**✅ 修法**：在组件 CMakeLists 里**显式转发**成编译定义：
```cmake
if(DEFINED XXX)
    target_compile_definitions(${COMPONENT_LIB} PRIVATE XXX=${XXX})
endif()
```
⭐ 并让头文件的默认值包在 `#ifndef XXX` 里，好让命令行覆盖它。
⚠️ **附带坑**：构建系统的缓存**会粘住**上次的值 ⇒
⭐ **换开关时先删 `build/`**，否则"以为切回去了、其实没有"。

### 案例二：配置项名字**随目标芯片不同**，写错只报 warning

```text
warning: unknown kconfig symbol 'BTDM_CTRL_MODE_BLE_ONLY' assigned to 'y'
```

🔴 **实际**：同一个功能，**两代芯片用完全不同的前缀**：
- 旧芯片：`BTDM_CTRL_MODE_*` / `BTDM_CTRL_BLE_MAX_CONN`
- 新芯片：`BT_CTRL_*`

⭐ 且新芯片**复用另一颗芯片的配置文件**（其 `Kconfig.in` 只有一行 `source ...`）
⇒ ⚠️ **相关的 choice 可能压根不存在**。

**症状**：⚠️ **只 warning，不 error** ⇒ 🔴 **"以为设了、其实没设"**。

### 案例三：`undefined reference` —— 实现被 CONFIG **条件编译掉**了

```text
undefined reference to `esp_ble_gap_start_advertising'
```

🔴 **实际**：该 API 的**声明**在头文件里（所以**编译能过**），
但**实现**被包在 `#if (FEATURE == TRUE)` 里，而那个配置**默认是关的**。

**症状**：⚠️ 报错长得像"函数名写错"或"库没链接"，
⭐ **完全看不出是配置问题**。

### 🔴🔴 还有第四个：报"头文件找不到"，其实是**组件没使能**

```text
fatal error: esp_bt.h: No such file or directory
```

🔴 **实际**：工程的配置文件**压根不存在** ⇒ 总开关没开 ⇒ **整个组件没使能**
⇒ 它的头文件自然不在 include 路径里。
⚠️ 看起来像"include path 写错了"。

### ⭐⭐ 这一族的通用形状

| # | 特征 |
|---|---|
| **1** | 🔴 **报错指向的不是原因** —— 三类报错分别像"名字写错""库没加""路径不对" |
| **2** | ⚠️ **多数只 warning / 只链接期报** ⇒ **编译"成功"不代表配置生效** |
| **3** | ⭐ **"配置没生效"和"配置不存在"长得一模一样** |
| **4** | ⭐ **默认值随芯片/版本变** ⇒ ⛔ 不能假设"不设就是我要的" |

**可迁移的做法**

1. ⭐⭐⭐ **判据落在【产物】上，⛔ 不是"没有报错"** ——
   ⭐ 让**两种配置产出【可区分的产物】**（如体积/版本串），
   ⚠️ **两者相同 ⇒ 开关没生效**（同族 `C36` / `C16`）。
2. ⭐⭐⭐ **改完配置【回读生成的实际配置**】，确认真的落进去了 ——
   ⛔ 不要只看"build 没报错"。
3. ⭐⭐ **报"头文件找不到"→ 先查提供它的组件有没有被开**；
   **报 `undefined reference` → 先查该符号是否被某个 CONFIG 挡住**；
   **报 `unknown symbol` → 先查该符号在【当前目标】上是否存在**。
4. ⭐⭐ **跨版本 / 跨芯片时，配置项名字一律现查**（在 SDK 源码里 grep），
   ⛔ 不照抄另一颗芯片的配置文件。
5. ⭐ **开关尽量做成"命令行可覆盖 + 头文件给默认"** ——
   ⚠️ 避免为了验证而改源文件（会引出 `C60` 那类损伤）。

**📌 一句话**：
> ⭐⭐ **"配置类报错"的第一嫌疑人是【配置没生效】，⛔ 不是报错信息字面指向的东西。**
> ⭐ 而"没生效"最可靠的判据是：**换个配置，产物变了吗？**

> 📌 **同族**：`C16`（严重性由下游指标裁决）、`C20`（存在性 ≠ 可用性）、
> `C36`（探针要打在真正被改的那条线上）、`C29`（跳过不能算通过）、
> `C60`（静默损坏）、`TROUBLESHOOTING.md` **95 号**。

---

## C62. ★★★★★ **"给人看的排版"被当成了"要发的数据" —— ⛔ 两栏对照表会让人把结束符当字符敲进去**（2026-09-23）

**场景**：串口设备调试。我给出"可直接复制粘贴的指令表"，写成**两栏对照**：

```text
t3.txt="BT OK"           FF FF FF
```

⚠️ 我的意思：**左边是文本、右边要在另一个模式（HEX）下补发**。
⚠️ 用户的理解：**"这是要发的一整行"** ⇒
🔴 **把 `FF FF FF` 当 6 个 ASCII 字符（`F`、空格、`F`、`F`…）一起打进去了。**

**后果**

| # | 现象 |
|---|---|
| **1** | 🔴 **设备永远收不到真正的结束符**（发的是字母 `F`，不是 `0xFF` 字节） |
| **2** | 🔴 **设备连"指令不合法"都不报** —— 它认为**这条指令还没结束**，一直在等 |
| **3** | ⚠️ **表现为"发了完全没反应"**，且**无任何回显** |

⭐⭐ **这个类的形状**：

| # | 特征 |
|---|---|
| **1** | 🔴 **排版是给人看的，数据是给机器看的 —— 两者常长得一样** |
| **2** | 🔴 **我以为是"说明"，对方以为是"内容"** ⇒ ⚠️ **误解毫无报错** |
| **3** | ⚠️ **"无反应"最容易让人往【物理层】查**（接线/波特率/电平），🔴 **真因却在一行排版上** |
| **4** | ⭐ **对照表越"整齐"，越容易让人整块复制**（⚠️ 整齐反而增加风险） |

**可迁移的做法**

1. ⭐⭐⭐ **凡是要粘贴进设备/命令行/协议里发的，一律给【整行、可直接用】的形式**，
   ⛔ **不要用"命令行 + 注释列"两栏对照** ——
   ⭐ 要么**转义写法一行搞定**（`t3.txt="BT OK"\xff\xff\xff`），
   ⭐ 要么**明确写"分两步：先文本、后 HEX"**。
2. ⭐⭐⭐ **机器要吃的字节，绝不用【可打印字符的样子】表示** ——
   ⚠️ `FF FF FF` 看起来就是 6 个字符。⭐ **写成 `0xFF` / `\xff` / "三个十六进制字节"**，
   ⚠️ 并在**同一句里**说明"这是字节，不是字符"。
3. ⭐⭐ **给"分模式发送"的指令时，把模式写在【每一行】上** ——
   ⚠️ 只在开头说一次"右边那栏用 HEX 发"**必然被漏**。
4. ⭐⭐ **排查"完全无反应"时，先问一句"你到底是【怎么】发的"** ——
   ⭐ **先看发送侧的实际字节**，⛔ 不要先怀疑接线/波特率（同族 `C28`：先查实际状态）。
5. ⭐ **"无回显"是有诊断价值的判据** ——
   ⭐ 若协议规定"收到合法指令会回显"，则
   **无回显 = 指令根本没被当成一条完整指令** ⇒ ⚠️ **优先怀疑【结束符/格式】，⛔ 不是设备坏了**。

**📌 一句话**：
> ⭐⭐ **给机器发的字节，⛔ 不要用"给人看"的排版来表达。**
> ⚠️ 两栏对照表看起来在解释，**实际上是在制造一次静默的格式错误**。

> 📌 **同族**：`C28`（关于外部状态的判断必须现查）、`C30`（"命令成功" ≠ "做对了事"）、
> `C17`（编码/格式的归属由**读取方**决定）、
> `TROUBLESHOOTING.md` **96 号**（完整排查过程）。

---

## C63. ★★★★★ **"官方点名的常见原因" ⛔ 不等于"本次的原因" —— 而且它常常【盖住】那个更简单的解释**（2026-09-23）

**场景**：设备（串口屏）**能通讯但下载失败**。我去查官方文档，
⭐ 官方**确实**点名了一个常见原因：
> "CH340 假货太多、延迟太大，容易出现**无法下载**的问题……建议改用 CP2102 / FT232"

⚠️ **我据此让用户去换下载器。**

🔴 **结果方向错了。** 用户随后给出一条**关键反证**：

> ⭐ **"我第一次已经下载成功了，而且就是 921600 的高波特率。"**

⭐⭐ **这一条直接否证了"芯片能力不够"** ——
⚠️ **同一块芯片成功过，能力就是够的**。

⭐ 而真正的线索**一直在现象列表里，只是被我当成了次要细节**：
> 🔴 **"屏接 USB-TTL 点不亮，但接可调电源就亮"**

⇒ 🎯 **真因：USB-TTL 的 `5V` 引脚【带不动】负载 ⇒ 电压被拉低到下限以下 ⇒ 负载根本没起来。**
⚠️ 而**下载比通讯对供电敏感得多**（数据量差几十上百倍），
⭐ **所以"能通讯"和"下载失败"同时成立，一点都不矛盾**。

**⭐⭐ 这个类的形状**

| # | 特征 |
|---|---|
| **1** | 🔴 **"官方点名的原因"有【权威光环】** ⇒ ⚠️ **一旦对上号，就不再看别的证据了** |
| **2** | 🔴 **它会盖住更简单的解释** —— ⚠️ 本例中"点不亮"是**一眼可见**的供电证据，却被跳过 |
| **3** | ⚠️ **"常见原因"是【先验概率】，不是【本次的判据】** —— ⭐ 二者不能互相替代 |
| **4** | ⭐ **"以前成功过"是最强的一类反证**，⚠️ 但很容易被忽略（⭐ 它藏在"用户背景信息"里） |

**可迁移的做法**

1. ⭐⭐⭐ **官方/文档点名的原因，只能用来【排候选】，⛔ 不能用来【下结论】**
   —— ⭐ **必须有本次的实测证据**（同族 `C36`）。
2. ⭐⭐⭐ **把"以前成功过 / 以前没这个问题"当成【硬判据】对待** ——
   它能**一次性排除一整类**"能力不足/不兼容"的假设，
   ⭐ 把嫌疑逼到**环境变量**（供电、线材、接触、时机、温度）上。
3. ⭐⭐ **"换个东西就好了"这类对照实验，是成本最低的判决性实验** ——
   ⚠️ 本例："接 A 不亮、接 B 就亮" ⇒ ⭐ **只改一个变量，结论无可争议**。
   ⛔ **别把它当成"次要现象"跳过**（⭐ **它通常就是答案**）。
4. ⭐⭐ **排查顺序按【当前证据】排，⛔ 不按"文档里先写了什么"排** ——
   ⚠️ 本例中我推荐的"冷启动同步技巧"在**"灯都不亮"**的前提下**毫无意义**：
   🔴 **东西都没启动，谈不上同步时机**。
5. ⭐ **报"握手失败/联机失败"时，先问"对端到底有没有正常上电"** ——
   ⚠️ 握手失败最常见的原因**不是协议**，而是**对端根本没起来**。

**📌 一句话**：
> ⭐⭐ **"文档说这很常见" 和 "这次就是它"，是两件事。**
> ⚠️ 而**权威的原因**最容易让人**停止看证据**。

> 📌 **同族**：`C36`（判据要打在真正被改的那条线上）、
> `C28`（关于外部状态的判断必须现查）、`C20`（存在性 ≠ 可用性）、
> `C62`（"无反应"有多个病因，别咬第一个假设）、
> `TROUBLESHOOTING.md` **97 号**（完整排查过程）、
> `fpga/FPGA问题排查手册.md` **27**（可复用结论）。

---


---


---

## C64. ★★★★★ **"证据文件"和"它证明的代码"必须一起提交 —— 只 add 证据目录会让提交变成【无凭据的断言】**（2026-09-23 实测）

**状态**：`[已确认]`

### 现象

```
⚠️ 提交信息写着"22 个 ball 核对一致"，
🔴 而那个【约束文件本身】根本没进版本库。
⇒ ⭐ 别人拉到的引脚表是【旧的】⇒ 按它接线就会接错。
```

### 🔴 为什么极难发现

| 反直觉点 | 说明 |
|---|---|
| ⭐ **测试/PnR 【全部通过】** | ⚠️ 因为它们读的是**工作区文件**，⛔ 不是 `HEAD` |
| ⭐ **证据文件确实提交了** | ⚠️ 所以 `git log` 看着"很完整" |
| 🔴 **`git status` 当时是干净的** | ⚠️ 因为**源码改动还没被 add 过**（⭐ 它在 untracked/modified 里默默待着） |
| 🔴 **只有在 push 前审 diff 才暴露** | ⭐ 而那时**差一步就推出去了** |

### 🎯 根因

⭐ **把"提交"理解成了"提交我这次操作产生的文件"，⛔ 而不是"提交一个自洽的状态"。**
⚠️ 证据类文件（`sim/evidence/`）在 `git add` 时**最容易顺手只 add 它**
—— ⭐ 因为它名字里就带"证据"，⚠️ 会让人以为"提交了证据 = 提交了这件事"。

### ✅ 判据（机械可执行）

```
⭐ push / commit 前，**必须**看一遍：
     git diff --name-only origin/main..HEAD
⭐ 然后问：**这份清单能让别人复现我的结论吗？**
     · 有证据文件 ⇒ 🔴 必须同时有【产生它的源码/约束】
     · 只有文档 ⇒ ⚠️ 确认文档没引用未提交的文件
```

### 📌 同族

`C36`（判据要打在真正被改的那条线上 —— ⭐ 本条是它的**版本控制版**）、
`C20`（存在性 ≠ 可用性 —— ⭐ "提交了" ≠ "提交对了"）。
---

## C65. ★★★★★ **改名/重构之后，受影响面必须【机械扫描】得出，⛔ 不能靠"我记得谁用了它"**（2026-09-23 实测）

**状态**：`[已确认]`

### 现象

```
⚠️ 把一个信号的接口从 `spi_miso_l/r` + `spi_cs_l/r` 改成 `spi_miso[5:0]` + `spi_cs_n[5:0]`。
⭐ 我"记得"有两个 tb 用了它 ⇒ 改了那两个。
🔴 结果【第三个 tb】也有 ⇒ 回归里它【编译失败】。
```

### 🔴 为什么"凭记忆列举"必然漏

| 原因 | 说明 |
|---|---|
| ⭐ **同一个人写的东西，记忆里会"合并同类项"** | ⚠️ "我改过 `tb_e2e` 和 `tb_top_m0`" —— ⭐ 而 `tb_top_sound` 被大脑归进了"`tb_top_*` 那一类" |
| ⭐ **引用形式多样** | ⚠️ 端口连接（`.spi_miso_l(...)`）、wire 声明、`assign`、注释里提及 —— ⭐ **每种都不长一样** |
| 🔴 **编译器只报【第一个】错** | ⚠️ 修一个再跑一次，⭐ **下一个可能还在后面** |

### ✅ 判据（机械可执行）

```
⭐ 改名后，立刻【全仓库】搜旧名，⛔ 不要限定目录：
     grep -r '<旧名>' --include=*.v --include=*.sv --include=*.md .
     # 或项目里的 grep 工具，**path 给仓库根**，⛔ 别给 tb/ 或 rtl/
⭐ 判据：**旧名的命中数 = 0**（⭐ 除历史文档/变更记录外）
```

### 📌 同族

`C36`（判据要打在真正被改的那条线上 —— ⭐ 本条的"扫描范围"就是那个"线"）、
`C28`（关于外部状态的判断必须现查 —— ⭐ "谁引用了它"就是外部状态）。

---

## C66. ★★★★★ **"官方文件"必须连同【它属于哪块板/哪个版本】一起确认 —— 名字像、内容对得上，⛔ 都不等于它适用于本次**（2026-09-23 实测）

**状态**：`[已确认]`

### 现象

```
⚠️ 一直拿 `xxx_60K_pins.cst` 当"官方权威引脚表"用。
🔴 结果发现它里面有 `RGMII_TXD[0/1]` —— ⭐ 而**这块板根本没有以太网 PHY**。
⇒ ⭐ **那份文件是【另一块 60K 板】的**（同系列、同主芯片、⛔ 不同板型）。
```

### 🔴 为什么极难发现 —— 它"对得上"了一大半

| 迷惑点 | 说明 |
|---|---|
| ⭐ **主芯片型号相同** | ⚠️ 文件名里的 `60K` 是真的 |
| ⭐⭐ **大部分引脚【确实一致】** | ⚠️ 我核对过的 6 个脚**全部对得上** ⇒ ⭐ **于是建立了"它可信"的信念** |
| 🔴 **少数几个不一致** | ⭐ 而**正是那几个**会被用来接线（⭐ `sdram_cs` 就差一个 ball） |
| ⚠️ **文件里通常没有"适用板型"字段** | ⭐ 机器可读的约束文件**往往只有引脚，没有"我是哪块板"** |

### 🎯 根因

⭐ **"对得上"是【部分匹配】给出的虚假信任。**
⚠️ 一旦前几项核对通过，后面就会**默认它整体可信**（⭐ 而不再逐条核对）。
🔴 **这正是 `C63` 的镜像**：⭐ `C63` 说"常见原因 ≠ 本次原因"，
⭐ 本条说"**官方文件 ≠ 本次适用**"。

### ✅ 判据（机械可执行）

```
⭐ 用任何"外部权威文件"之前，先回答**三个问题**：
   ① ⭐ **它的名字/路径里有没有【板型或版本】标识？**
      ⚠️ 若没有 ⇒ ⭐ **按"来源不明"处理**
   ② ⭐ **找一条【该文件的独有特征】，看本次目标有没有** ——
      ⭐ 例：文件里提到某个外设/接口 ⇒ ⚠️ **本次目标若没有该外设，
      就说明文件不适用**（⭐ 本次正是靠 `RGMII` 抓出来的）
   ③ ⭐ **部分核对通过 ≠ 整体可信** ——
      ⚠️ **必须逐条核对【本次要用的每一个】**，⛔ 不能"前几个对了就信剩下的"
```

### 📌 附带的一条

⭐ **部分匹配是最危险的**：⚠️ **完全对不上**会立刻引起怀疑，
⭐ 而"**对上一大半**"会让人**停止怀疑**。

### 📌 同族

`C63`（"官方点名的常见原因"⛔ 不等于"本次的原因" —— ⭐ 本条的权威文件版）、
`C20`（存在性 ≠ 可用性 —— ⭐ "有官方文件" ≠ "文件适用于我"）、
`C28`（关于外部状态的判断必须现查 —— ⭐ "它属于哪块板"就是外部状态）。

---

## C67. ★★★★★ **限流开关的"配置文件里写了" ⛔ 不等于"生效了" —— 必须用【产物本身】反查**（2026-09-24 实测）

**状态**：`[已确认]`

### 现象

```
⭐ 一个"改了源码会红"的守卫，配置里明明设了禁用项，
🔴 但实测【根本没生效】 —— 它列的文件仍被扫描、仍会红。
```

### ⭐⭐ 根因：配置项只看"路径字符串匹配"，而实际路径【对不上】

| 层 | 实际值 |
|---|---|
| 配置里写的 | `esp32/…`（⭐ 相对仓库根） |
| 实际扫描到的 | `esp32\…`（⭐ 反斜杠）或**根本不在同一层** |

⇒ ⚠️ **匹配失败 ⇒ 静默放行**（⭐ **⛔ 不报"配置无效"，⭐ 也不报"文件被检查"**）

### ✅ 正解：⭐ 用【产物】反查，而不是读配置

```powershell
# ⛔ 不要问"我配了吗"
# ✅ 要问"它实际扫了什么" —— 看产物/日志
#    若产物里仍出现你以为是排除掉的文件 ⇒ 配置没生效
```

### 📌 通用化

> ⭐ **任何"排除/豁免/白名单"机制，都必须验【实际行为】，⛔ 不能验"配置文本"。**
> ⚠️ 因为**匹配失败是静默的** —— 它不报错，只是"照常处理"。

⭐ **判据**：⭐ **故意放一个【应该被排除】的样本** ⇒ ⭐ **若它仍被处理 ⇒ 开关没生效**。

⭐ **与 `C63` 的关系**：⭐ `C63` 讲"官方点名的原因不等于本次原因"，
⭐ 本条讲 **"我配了"不等于"它认了"** —— ⚠️ 共性都是 **"中间那层没被验证"**。

---

## C68. ★★★★★ **"两个失败长得一样" ⛔ 不等于"根因一样" —— 编码问题会伪装成逻辑问题**（2026-09-24 实测）

**状态**：`[已确认]`

### 现象

```
⭐ 一批检查跑完，报出【2 个】同类型的失败：
   "锚点文本在文件里找不到"
🔴 第一反应：⭐ "RTL 改过，两个锚点都过时了" —— ⚠️ 这个判断【只对了一半】。
```

### 🔴 实测：两个失败，根因【完全不同】

| 失败 | ⭐ 真因 | ⭐ 判别 |
|---|---|---|
| **第 1 个** | ⭐ **真·过时** —— 信号**改名**了（`mx_*` → `mm_*`） | 手动 `Contains` 检查 ⇒ **False** ✅ |
| **第 2 个** | 🔴 **编码** —— 锚点里**混了非 ASCII**（中文注释）<br>⇒ 宿主脚本按 **GBK** 解码无 BOM 的 UTF-8 ⇒ **锚点变乱码**<br>⇒ 而目标文件按 UTF-8 读 ⇒ **正常中文** ⇒ 🔴 **永不相等** | 手动 `Contains` 检查 ⇒ ⭐ **True（矛盾！）** |

### ⭐⭐ 关键判别法：⭐ 当"找不到"却又能手动找到时，⭐ 一定是【编码】

```powershell
# ⭐ 一步机械检查：锚点里有没有非 ASCII 字节
$b = [Text.Encoding]::UTF8.GetBytes($anchor)
($b | Where-Object { $_ -gt 127 }).Count     # > 0 ⇒ 几乎必然是本条
```

### 📌 通用化

> ⭐ **"同一个报错"是【症状层】的巧合，⛔ 不是【根因层】的结论。**
> 🔴 **多个同类失败必须【逐个】查根因，⛔ 不能一次下结论。**

⚠️ **为什么危险**：⭐ 若按"都是过时"一起处理，
🔴 第 2 个会因为**只改注释部分**而**继续失败**（⭐ 或者更糟：**改对了但以为是自己改好的**）。

⭐ **与 `C65` 的关系**：⭐ `C65` 讲"改名后受影响面要**机械扫描**"，
⭐ 本条补一句：⭐ **扫描出来的结果，还要逐个确认【为什么】** ——
⚠️ **因为"扫描报错"这一层就可能是假的**（⭐ 编码/转义都会造成**假阳性**）。

---

## C69. ★★★★★ **"模块的 tb 全过" ⛔ 不等于 "接进顶层没问题" —— 组合环只在【跨模块回路】里成立**（2026-09-24 实测）

**状态**：`[已确认]`

### 现象

```
⭐ 一个模块：⭐ 自己的 tb 【全过】（⭐ 判据齐全、变异体全抓）
🔴 接进顶层后：⭐ 整个顶层仿真【在时间 0 挂死】—— ⚠️ 零输出、看门狗都不触发
```

### 🎯 根因：⭐ 一个"组合选择"把两个模块连成了环

```verilog
// ⭐ 顶层里的一行（⭐ 看起来完全无害）：
wire [2:0] cal_ch_mux = busy_a ? ch_a : ch_b;   // ⚠️ 纯组合 ? :
       │
       ├──> 模块 A 的 cal_ch 输入
       └──> 模块 B 的 cal_ch 输入
                │
                ↓ （A 的输出经采集通路回到 B）
       ┌────────┴─────────┐
       ↓                  ↓
  B.busy_o = f(B.state)  A.busy_o = f(A.state)   ← ⚠️ 两者都是 assign（组合输出）
       │                  │
       └────────┬─────────┘
                ↓ 又回到 cal_ch_mux 的选择条件
🔴 ⇒ 跨模块组合环 ⇒ 仿真器在时间 0 反复求值 ⇒ 不收敛 ⇒ 挂死
```

⚠️ **为什么单模块 tb 测不出来**：
```
⭐ 单模块 tb 给 cal_ch 的是【常量】⇒ ⭐ 环根本不存在
⇒ ✅ "模块独立可测" 与 "集成后无环" 是【两个正交的维度】
```

### ⭐⭐ 定位方法（⭐ 可复制：三分实验法）

| 步 | ⭐ 做法 | ⭐ 结论 |
|---|---|---|
| **0** | ⭐ **先用 `git worktree` 检出【旧版本】跑同一个 tb** | ⭐ **旧版 PASS / 新版挂** ⇒ ✅ **确认是"新引入"**（⭐ ⛔ 排除环境） |
| **1** | ⭐ **实验 A**：把嫌疑模块的**输入**接常量（⭐ 如 `start_i` 接 0） | ⭐ **若解挂** ⇒ 嫌疑模块**参与**了环 |
| **2** | ⭐ **实验 B**：恢复输入，但把**选择条件**改掉（⭐ 如 `busy_a` 换成延迟一拍） | ⭐ **若解挂** ⇒ ⚠️ **就是那个组合选择** |
| **3** | ⭐ 全部改动**只在 worktree 里做** | ⭐ **主仓库零污染** |

⭐ **判据**：⭐ **"接常量就好" ⛔ 不等于 "那个输入有错"** ——
⚠️ 它只说明**该模块参与了环**，⭐ 真正的元凶是**连接方式**。

### ✅ 修法：⭐ 把组合选择**打一拍**

```verilog
// 🔴 改前（⭐ 组合环）
wire [2:0] sel = busy_a ? ch_a : ch_b;

// ✅ 改后（⭐ 环被切断）
reg busy_a_r;
always @(posedge clk) busy_a_r <= busy_a;
wire [2:0] sel = busy_a_r ? ch_a : ch_b;   // ⭐ 用寄存器版
```

⚠️ **代价**：⭐ **仲裁晚一拍** ⇒ ⭐ 判断"能不能接受"：
```
⭐ 若原语义是"谁先在忙"（⭐ 如资源共享仲裁）⇒ ✅ 晚一个周期可接受
⭐ 若语义是"同拍必须一致"（⭐ 如握手）⇒ ⚠️ 要另想办法（⭐ 如锁存选择）
```

### 📌 通用化

> ⭐ **跨模块的"组合选择/组合使能"是组合环的高发地。**
> ⚠️ 凡是"用 A 的状态去选 B 的输入，而 B 又反过来影响 A 的状态" ⇒ 🔴 **必须打拍**。

⭐ **一条机械检查**（⭐ 集成后做）：
```
⭐ 列出所有"组合信号"（assign / ? :），⭐ 逐个问：
   "它驱动的模块，会不会通过任何路径影响它的选择条件？"
⚠️ 答案是"可能" ⇒ ⭐ 打一拍，⛔ 不要赌。
```

### ⚠️ 为什么 `iverilog` 不报错

⭐ **组合环不是语法错误** ⇒ ⚠️ **编译 exit 0、零 error**（⭐ 顶多一个 warning）
🔴 **综合器通常也只给 warning** ⇒ ⚠️ **"编译过了" ⛔ 不是"没有环"**。

---

## C70. ★★★★★ **"证据文件是 PASS" ⛔ 不等于 "它证明的是最后一次改动"**（2026-09-24 实测）

**状态**：`[已确认]`

### 现象

```
🔴 一个缺陷在提交 `X` 时引入，⭐ 但 `X` 的提交里【证据文件显示 PASS】
⇒ ⚠️ 事后排查时，⭐ "证据说通过了" 会让人【排除】那个提交
🔴 结果绕远路
```

### 🎯 根因

```
⭐ 提交 `X` 时，⭐ 只跑了【部分】测试（⭐ 或压根没跑那个用例）
⇒ ⚠️ 那个用例的日志文件【还是更早一次运行的】
⭐ 而日志文件名一样、内容一样 ⇒ 🔴 看起来像"这次也过了"
```

### ⭐⭐ 判别法（⭐ 一步机械检查）

```powershell
# ⭐ 比对【日志的写入时间】与【被它证明的源码的修改时间】
$logTime  = (Get-Item $log).LastWriteTime
$codeTime = (git log -1 --format=%cd -- $rtlfile)
# ⚠️ 若 logTime < codeTime ⇒ 🔴 这份证据证明的是【旧代码】
```

⭐ **或者更直接**：⭐ **看证据文件里有没有【那次改动引入的新日志行】**
（⭐ 如新模块的 `PARAM_OK` 自检 ⇒ ⚠️ 没有 ⇒ 说明是旧日志）。

### 📌 通用化

> ⭐ **证据必须自带"新鲜度"** —— ⚠️ 只有"内容 PASS"不够，
> ⭐ 还要能证明"这份 PASS 对应的是【当前】的代码"。

⭐ **与 `L74` 的关系**：⭐ `L74` 讲 **"'退出码 0' 不等于 '产物是新的'"**，
⭐ 本条讲 **"'证据 PASS' 不等于 '证据是这次的'"** ——
⚠️ 共性是 **"结果的正确性"与"结果的时效性"是两个独立维度**。

### ✅ 对策

| # | ⭐ 做法 |
|---|---|
| **1** | ⭐ **提交前必跑一次【全量】**（⭐ ⛔ 不要只跑部分就提交证据） |
| **2** | ⭐ **证据里带上"代码版本"**（⭐ 如 commit hash 或时间戳） |
| **3** | ⭐ **CI/守卫检查新鲜度**（⭐ 比对日志时间 vs 源码时间） |
| **4** | ⚠️ **审查别人的提交时**：⭐ **"证据显示 PASS" 要问一句"是哪一次跑的"** |

---

## C71. ★★★★★ **"已经发出去的文档" ⛔ 不能改 —— 改了就产生【两个版本共存】，而对方手里的那份你控制不了**（2026-09-24 实测）

**状态**：`[已确认]`

### 现象

```
⭐ 我把一份【给同事的通知】发出去了。
⭐ 之后我又发现两件事：
     ① 通知里有一处【技术写法是错的】（⭐ 后来修 RTL 时改掉了）
     ② 还有一个【很重要的教训】没写进去
⇒ 🔴 我"顺手"把通知文档改了（⭐ 加了新章节 + 改掉了那处错写法）
🔴 结果：⭐ 同事手里的版本 ≠ 仓库里的版本
```

### 🎯 根因：⭐ **发出去的文档是"已交付物"，⛔ 不是"草稿"**

```
⭐ 我改的时候想的是"让文档更完整"
🔴 但没意识到：⭐ 那份文档【已经不在我手里了】——
     同事可能已经读完、已经按它做了、或者已经转发给别人了
⇒ ⚠️ 我的修改【到不了他们那里】，⭐ 只会让【仓库版本】和【他们认知】分叉
⇒ 🔴 而更糟的是：⭐ 之后别人对着仓库看，会以为"大家都看过新版"
```

### ⚠️ 为什么危险（⭐ 不是"文档不一致"这么轻）

| # | ⚠️ 后果 |
|---|---|
| **1** | 🔴 **对方可能照【旧版】里的错做法执行**（⭐ 本次就是：旧版写了有缺陷的写法） |
| **2** | ⚠️ **重要的补充对他【完全不可见】**（⭐ 他不知道自己漏了什么） |
| **3** | 🔴 **事后追溯困难** —— ⚠️ "你当时看的是哪一版？"没有答案 |
| **4** | ⚠️ **责任模糊** —— ⭐ 出了事分不清是"发了错的"还是"改了没通知" |

### ✅ 正确做法（⭐ 按代价从低到高）

| # | ⭐ 做法 | ⭐ 适用 |
|---|---|---|
| **1** | ⭐⭐ **发之前把待改的都改完 —— 即"发送"是【最后一步】** | ⭐ **永远优先这个** |
| **2** | ⭐ **要改就发【勘误/补充】，⛔ 不是偷偷改原文档** | ⭐ 小修正（⭐ 如本次的"写法已改"）<br>⭐ 勘误要**显式说明"哪一版、哪一条、改成什么"** |
| **3** | ⭐ **原文档【保持原样】+ 加"⚠️ 本节已过时，见勘误"标记** | ⭐ 需要留档 |
| **4** | ⚠️ 版本化（⭐ `xxx-v2.md`） | ⚠️ 大改，⭐ 但要**明确告知"以哪一版为准"** |

### ⭐⭐ 一条机械检查（⭐ 发之前问自己）

```
📌 "这份文档接下来还有人要改吗？"
   ⭐ 会 ⇒ ⚠️ 先别发，⭐ 等改完
   ⭐ 不会 ⇒ ✅ 发
📌 发完之后又想改 ⇒ 🔴 【默认动作是"发勘误"，⛔ 不是"改原文件"】
```

### 📌 通用化

> ⭐ **"交付"是一个不可逆的时点。**
> ⚠️ 交付前：⭐ 文档是**草稿**，随便改；
> 🔴 交付后：⭐ 文档是**已发布制品**，⭐ 改动**必须走"通知"这个渠道**，
> ⛔ **不能靠"改源文件"**（⭐ 因为**源文件不在对方手上**）。

⭐ **与 `C64` 的关系**：⭐ `C64` 讲 *"'证据文件'和'它证明的代码'必须一起提交"*，
⭐ 本条讲 **"'已发布文档'和'它的后续修改'不能混在一起"** ——
⚠️ 共性都是 **"有多份副本时，必须明确【哪一份是权威】"**。

### ⚠️ 一条附带教训：⛔ 不要"顺手"改

⭐ 我当时的心态是"**反正在手边，顺手加上**" —— 🔴 **这正是问题**。
⭐⭐ **规则**：⭐ **对【已交付物】的任何修改，都必须先问"这需要通知对方吗"** ——
⚠️ 如果答案是"需要"，⭐ **那就不是"顺手"，而是一次新的交付**。

---

## C72. ★★★★★🔴 **无 BOM UTF-8 脚本 + PS 5.1 的 GBK 解码 ⇒ 会【静默吞掉】数组元素**（2026-09-24 实测，⭐ 比"报错"危险得多）

**状态**：`[已确认]`

### 现象

```
🔴 一个 107 元素的 PowerShell 数组（变异体 catalogue）
   · 正则数 `id = "M…"`  ⇒ ⭐ 107 个（⭐ 文本确实在）
   · Invoke-Expression 解析 ⇒ ⭐ 107 个
   🔴 而脚本【实际运行】时 ⇒ ⚠️ 只有 105 个！
   ⇒ ⭐ 于是 -Filter M98 报 "matched 0 mutants" —— ⚠️ 而 M98 明明写在文件里
```

### 🎯 根因：⭐ **GBK 解码吃掉了换行**

```
⭐ 脚本是【无 BOM 的 UTF-8】，⭐ 含中文注释
⇒ PS 5.1 按 **GBK(936)** 解码 ⇒ ⚠️ 某些"中文 + 后续字节"被吃成【一个 GBK 字符】
⇒ 🔴 **连紧跟的换行也被吃掉** ⇒ ⭐ **注释行与下一行【合并】**
⇒ 🔴 **下一行如果是数组元素 `@{...}` ⇒ 它就不再是元素了**（⭐ 变成注释的一部分）
```

### ⭐⭐ 实测证据（⭐ 决定性）

```
⭐ UTF-8 读取行数 : 585
⭐ GBK  解码行数 : 581     ← 🔴 少 4 行（4 个换行被吃）

⭐ GBK 解码后的那一行：
   [#] len=554 : # 锚点全部【纯 ASCII】…静默 SKIP），  #   ⚠️ …
                   ↑ ⚠️ 两行注释【合并成一行】（554 = 两行之和）
   ⇒ 下一行直接跳到 M99 ⇒ 🔴 **M98 整个消失**
```

### ⭐ 验证（⭐ 一步到位）

```powershell
# ⭐ 让脚本自己打印它实际读到的元素（⭐ 别信"文件里有"）
Write-Host ("DIAG_ALL: " + (@($mutations | ForEach-Object { $_.id }) -join ","))
# ⭐ 或独立验证：
$b = [System.IO.File]::ReadAllBytes($script)
$gbk  = [System.Text.Encoding]::GetEncoding(936).GetString($b)
$utf8 = [System.IO.File]::ReadAllText($script)
"GBK 行数: $((($gbk -split "`r?`n")).Count)  / UTF-8 行数: $((($utf8 -split "`r?`n")).Count)"
# ⚠️ 两者不同 ⇒ 就有换行被吃 ⇒ 多半有元素被吞
```

### ⚠️ 为什么极难发现（⭐ 三条）

| # | ⭐ |
|---|---|
| **1** | ⭐ **文本还在文件里** ⇒ ⚠️ **`grep`/正则都能数到** ⇒ ⭐ **"我明明写了"** |
| **2** | ⭐ **不报错、不警告** ⇒ 🔴 **只是元素数少了** |
| **3** | ⚠️ **只在【脚本被 `-File` 执行】时才发生** —— ⭐ **`Invoke-Expression` / PS 7 都正常** ⇒ ⚠️ **本地测不出来** |

### ✅ 修法（⭐ 按优先级）

| # | ⭐ 做法 | ⭐ 评价 |
|---|---|---|
| **1** | ⭐⭐ **给脚本加 UTF-8 BOM** | ⭐ **最彻底** —— ✅ **有 BOM ⇒ PS 5.1 正确按 UTF-8 解码**<br>⚠️ 但项目纪律是"**无 BOM**"（⭐ `K-38`）⇒ ⚠️ **要改纪律** |
| **2** | ⭐⭐ **把【数组元素】前的注释改成纯 ASCII** | ✅ **不需要改编码纪律**<br>⭐ **本次采用** —— ⭐ **实测：删掉 3 行中文注释 ⇒ 105 → 106** |
| **3** | ⭐ **数组元素前【不留注释】** | ⭐ 最保险 |
| **4** | ⚠️ **脚本正文全 ASCII** | ⭐ 项目已有此纪律（⭐ `AGENTS.md`）⚠️ **但没被执行** |

### 📌 与既有条目的关系

| ⭐ | ⭐ 说明 |
|---|---|
| **`K-38`** | ⭐ 讲 **"PS 5.1 按 GBK 解码无 BOM 文本 ⇒ 语法错"** |
| **`K-116`/`C17`** | ⭐ 讲 **BOM/编码** |
| ⭐⭐ **本条** | ⭐ **同一个根因，但后果【升级】了**：<br>⚠️ 从 **"报错"**（⭐ 能发现）⇒ 🔴 **"静默少元素"**（⭐ **发现不了**） |

⭐⭐ **一句话**：⭐ **同一个编码坑，⚠️ 报错反而是好事 —— 最怕的是它【不报错】。**

### 📌 通用化

> ⭐ **"文件里有" ⛔ 不等于 "运行时读到了"。**
> ⚠️ 对**任何"数据文件/脚本里的表"**，⭐ **判据都应该是"运行时实际读到的数量"**，
> ⛔ **不是"我在文件里数到了几个"**。

⭐ **与 `C70` 的关系**：⭐ `C70` 讲 **"证据 PASS 不等于证明最后一次改动"**，
⭐ 本条讲 **"文本存在不等于运行时可读"** ——
⚠️ 共性都是 **"中间那一层（解析/时效）没被验证"**。

---

## C73. ★★★★★🔴 **同一个坑反复踩，往往不是因为"没记" —— 而是因为【规则自相矛盾】，或规则被写成了"描述"而不是"要求"**（2026-09-24 实测，⭐ 用户点名"怎么老是遇到这个编码问题"）

**状态**：`[已确认]`

### 🎯 现象：⭐ 编码坑【第 N 次】复发

```
⚠️ 用户："写经验，怎么老是遇到这个编码问题，之前也是 PowerShell 编码有问题"
🔴 而经验库里【早就】有两条：
   · `C17`（2026-09-17）：⭐ "配置文件该不该带 BOM，由【读取方】决定"
   · `U46`（2026-09-17）：⭐ "带中文的 `.ps1` 必须 UTF-8 with BOM"
   ⇒ ⭐ 而且它们【写得都对】！
```

### 🔴🔴 根因：⭐ **规则写对了，但被后来的文档【改成了相反的描述】**

| ⭐ 位置 | ⭐ 说法 | ⚠️ 性质 |
|---|---|---|
| `U46`/`C17`（⭐ 经验库） | ⭐ **"带中文的 `.ps1` 【必须】UTF-8 with BOM"** | ✅ **要求**（⭐ 正确） |
| `K-55`（⭐ 项目 K 清单） | ⭐ **"编辑 `sim/*.ps1` 后【必须复查 BOM】—— 工具会剥掉"** | ✅ **要求**（⭐ 正确） |
| 🔴 **某处文档** | 🔴 **"`mutation_test.ps1` 是【无 BOM 的 UTF-8】，而 PS 5.1 按 GBK 解码它"** | 🔴 **描述**（⭐ **把缺陷当成事实接受了**） |

⇒ ⭐⭐ **关键区别**：
```
✅ 要求写法：⭐ "脚本【必须】带 BOM"
🔴 描述写法：⭐ "脚本是【无 BOM】的，所以 PS 5.1 按 GBK 解，所以……"
                ↑ ⚠️ 读的人会认为"这是环境特性，只能适应它"
                ⇒ 🔴 **于是所有人都在"适应"，⛔ 没人去修根因**
```

⭐⭐ **这就是反复踩坑的机制**：⭐ **不是"忘了"，而是"规则被降级成了描述"**。

### ⭐⭐ 三条可迁移的教训

| # | ⭐ |
|---|---|
| **1** | ⭐⭐ **"必须 X" 和 "现在是 X" 要写得【分得清】** —— ⚠️ 前者是可执行的规则，⭐ 后者只是观察<br>🔴 一旦把缺陷写成"现状描述"，⭐ **它就会被当成不可改变的环境** |
| **2** | ⭐⭐ **同一件事出现【多条规则】时，必须检查它们是否【冲突】** —— ⚠️ 本次就是"经验库说必须带 BOM" vs "项目文档说它是无 BOM 的" |
| **3** | ⭐⭐ **复发 = 规则失效的信号** —— ⚠️ 此时**补一条新经验是【没用的】**，<br>🔴 **必须去找"为什么旧经验没起作用"** |

### ✅ 本次的修法（⭐ 示范"怎么真正止住复发"）

| # | ⭐ 动作 |
|---|---|
| **1** | ⭐ **把"描述"改回"要求"** —— ⚠️ 不要写"它是无 BOM 的"，⭐ 而是写"**它必须是……**" |
| **2** | ⭐ **在项目硬约束里明确**：⭐ **`.ps1` 一律 UTF-8 with BOM**（⭐ 与 `U46`/`C17`/`K-55` 一致） |
| **3** | ⭐ **加一条【机械检查】**（⭐ 见下），⭐ 让"忘了"能被自动抓住 |
| **4** | ⭐ **检讨现有所有 `.ps1` 的 BOM 状态**（⭐ 实测：有的有、有的没有 ⇒ 说明纪律没被执行） |

### ⭐⭐ 机械检查（⭐ 可复制）

```powershell
# 列出所有 .ps1 的 BOM 状态 与 非 ASCII 字节数
Get-ChildItem -Recurse -Filter *.ps1 | ForEach-Object {
    $b = [System.IO.File]::ReadAllBytes($_.FullName)
    $bom = ($b.Length -ge 3 -and $b[0] -eq 0xEF -and $b[1] -eq 0xBB -and $b[2] -eq 0xBF)
    $na  = ($b | Where-Object { $_ -gt 127 }).Count
    [pscustomobject]@{
        文件 = $_.Name
        BOM  = if ($bom) { '有' } else { '无' }
        非ASCII = $na
        # 判定：有非 ASCII 就必须有 BOM
        判定 = if ($na -gt 0 -and -not $bom) { 'FAIL: 有中文却无 BOM' }
               elseif ($na -gt 0) { 'OK' }
               else { 'OK (纯 ASCII)' }
    }
} | Format-Table -AutoSize
```

📌 **判据**：⭐ **`非 ASCII > 0` 且 `无 BOM` ⇒ 🔴 FAIL**。

### 📌 通用化

> ⭐⭐ **反复出现的同一个问题，通常不是"知识缺失"，而是【三条之一】：**
> 1. ⭐ **规则自相矛盾**（⭐ 两处文档说法不同）
> 2. ⭐ **规则被写成描述**（⭐ "现状如此" ⇒ ⚠️ 被当成不可改）
> 3. ⭐ **规则没有机械检查**（⭐ 只能靠人记得）
>
> ⇒ ⭐ **此时补新经验【无效】**，⭐ **必须去修那三条中的一条**。

⭐ **与 `C71`（已发出的文档不能改）的关系**：
⭐ `C71` 讲 **"交付后改动要通知"**，⭐ 本条讲 **"规则冲突要先解决冲突"** ——
⚠️ 共性都是 **"文档层面的问题，会在人的行为层面显形"**。

⭐ **与 `C68`（同形失败≠同因）的关系**：
⭐ `C68` 讲 **"别把多个失败当成一个原因"**，⭐ 本条讲 **"别把复发当成'没记住'"** ——
⭐ 都是 **"要先分清层次，再动手"**。

### ⭐⭐ `C73` 的一个**当场复现**（⭐ 2026-09-24，说明"规则 + 机械检查"才是解药）

```
⭐ 我刚给全部 .ps1 加完 BOM（12/12 通过，检查器 exit 0）
⭐ 然后【用编辑器改了一行】那个检查脚本自己的代码
🔴 复查：前 3 字节 = 23 20 63 ⇒ BOM 被【静默剥掉】了！
⇒ ⭐ 这【正是】`K-55` 记录的现象（"编辑 .ps1 后 BOM 会被工具剥掉"）
```

⭐⭐ **但这次和以前不一样**：⭐ **检查脚本【当场抓到了】** ✅
```
⭐ 因为它就是为这件事写的 ⇒ ⭐ 一句命令就能发现
🔴 而以前"靠人记得复查 BOM" ⇒ ⚠️ 10 次里有 9 次会忘
```

📌 **这就是本条主张的实证**：
> ⭐ **光有"规则"不够** —— 🔴 **必须有【机械检查】**，
> ⚠️ 否则规则会在"编辑一次文件"这种日常动作里**默默失效**。

⭐ **操作纪律**（⭐ 已写进入库文档 `files/20-代码/开发纪律-20260924.md`）：
```
🔴 改完任何 .ps1 ⇒ ⭐ 立刻跑一次 tools\check_ps1_encoding.ps1
⭐ 能不改就不改（⚠️ 每改一次都可能把 BOM 剥掉）
```

---

## C74. ★★★★★🔴 **改工程配置时，⚠️ "注释"也可能被解析器当真 —— Gowin `.gprj` 会因为注释里的旧料号而拒绝整个工程**（2026-09-24 实测）

**状态**：`[已确认]`

### 现象

```
⭐ 板子到手 ⇒ 用户确认器件型号是 GW5AT-LV60PG484AC1/I0
⭐ 我改 Gowin2026.gprj 的 <Device pn="...">
   ⇒ 顺便在【头部注释】里写了一句：
      "(Previously this file carried GW5AT-LV60PG484AC2/I1 ...)"
🔴 结果：gw_sh 报 **"invalid project file"** ⇒ ⭐ **整个综合/布线跑不动**
```

### 🎯 根因（⭐ 二分定位出来的）

```
🔴 Gowin 的 .gprj 解析器【会扫注释里的器件料号】
   ⭐ 文件里出现【两个不同的料号】⇒ 判定非法
```

| ⭐ 版本 | ⭐ 注释里的料号 | ⭐ `<Device>` 的料号 | ⭐ 结果 |
|---|---|---|---|
| 旧版（能编译） | `…AC2/I1` | `…AC2/I1` | ✅ **一致 ⇒ 通过** |
| 🔴 我改的 | 🔴 `…AC2/I1`（⭐ 作为"以前是"） | `…AC1/I0` | 🔴 **不同 ⇒ `invalid project file`** |

📌 **实测判据**：⭐ **删掉注释里那句旧料号 ⇒ 立刻 `OPEN_OK`**。
### 🔴🔴 **第二个形态（⭐ 2026-09-24 又踩一次）：⭐ XML 注释里【禁止出现 `--`】**

⭐ **同一个报错、同一个文件，但原因完全不同** —— ⚠️ 我加新模块到 `.gprj` 时写：

```xml
<!-- 2026-09-24 new: 'P' diagnostic command -- replies with the current pos
     reading so the operator can see whether it is stable. -->
                                     ↑ 🔴 这个 `--` 就是凶手
```

🔴 **结果**：⭐ 同样的 **`invalid project file`** ⇒ ⚠️ **整个 PnR 跑不动**。

| ⭐ 判据 | ⭐ 结果 |
|---|---|
| ⭐ **料号出现次数** | ✅ **1 个**（⭐ 不是 `C74` 第一形态） |
| ⭐ **非 ASCII 字节** | ⚠️ 1750（⭐ 但**第一形态已证明非 ASCII 本身不是原因**） |
| 🔴 **`.NET` 的 `[xml]` 解析** | 🔴 **直接抛异常** ⇒ ⭐ **这才是判据** |

⭐⭐ **关键**：⚠️ 我当时**误读了 `.NET` 的报错**（⭐ 以为它只是"显示不完整"），
⭐ 实际它**明确说了 XML 非法** ⇒ ⚠️ **差点又去做二分定位**。

### ⭐⭐ 通用化：**改配置文件的注释时，先过一遍【该格式的注释规则】**

| ⭐ 格式 | 🔴 注释里不许有 |
|---|---|
| **XML / `.gprj`** | 🔴 **`--`**（⭐ 且 ⛔ 不能嵌套） |
| **HTML / Markdown 内嵌** | ⚠️ 同上（⭐ XML 继承） |
| **Shell / Tcl** | ⚠️ 未闭合引号 |
| **Verilog** | ⚠️ `/* */` 不能嵌套 |

> ⭐⭐ **教训**：⭐ **"注释是给人看的、解析器不关心"是个【错误直觉】** ——
> ⚠️ 在 XML / Gowin `.gprj` 这类文件里，**注释照样进解析器**。

### ⭐ 最快的判据（⭐ 一步，⛔ 不用二分）

```powershell
# ⭐ 改完 .gprj 立刻跑这个 —— 秒级，⛔ 不用等 10 分钟的 PnR
try { $null = [xml](Get-Content Gowin2026.gprj -Raw); "XML OK" }
catch { "XML BAD: $($_.Exception.Message)" }
```
📌 **再配一次**最小 `open_project`（⭐ 见上方代码块）⇒ ✅ **两道保险**。

### ⭐⭐ 定位方法（⭐ 可复制：逐行删除 + 最小验证）

```python
# ⭐ 1) 先做一个【最小验证脚本】（⭐ 只 open_project，不综合 ⇒ 几秒出结果）
#    build/_t.tcl:  open_project Gowin2026.gprj \n puts "OPEN_OK" \n exit

# ⭐ 2) 逐行删掉可疑注释，每次跑一次最小验证
for i in range(len(lines)):
    trial = lines[:i] + lines[i+1:]
    write(trial); if "OPEN_OK" in run(): print(f"删掉第 {i} 行 ⇒ PASS")
```

⭐⭐ **要点**：⭐ **用"最小验证"而不是"完整综合"** ——
⚠️ 完整综合要 10 分钟，⭐ 而最小验证**几秒** ⇒ ⭐ **二分 15 行只要 1 分钟**。

### 🔴 我在这上面踩的三个坑（⭐ 都写出来）

| # | ⚠️ 我猜的 | 🔴 实际 |
|---|---|---|
| **1** | ⭐ "是 emoji `✅`/`⚠️` 破坏了 XML" | ⚠️ **不是** —— ⭐ 旧版【也有】`⚠️`，⭐ 而旧版能编译 |
| **2** | ⭐ "是非 ASCII 字节太多" | ⚠️ **不是** —— ⭐ 旧版 1783 个 > 新版 1759 个 |
| **3** | ⭐ "是 `.NET` XML 解析器报的 `version="1"` 错" | ⚠️ **不是** —— ⭐ 那行【我一直没改】，⭐ 且 Gowin 比 .NET 宽松 |

⇒ ⭐⭐ **三次猜测全错** ⇒ ⭐ **最后靠【二分】才定位**。

### 📌 通用化

> ⭐⭐⭐ **"注释"不是绝对安全的 —— 有些解析器会把配置文件的注释【一起扫】。**
> ⚠️ 改配置时，⭐ **注释里⛔ 不要出现"另一种可能的取值"**
> （⭐ 旧料号 / 旧版本号 / 旧路径）—— 🔴 **它可能被当成真配置**。

⭐ **与 `C71`（已发出的文档不能改）的关系**：
⭐ `C71` 讲 **"改文档要通知"**，⭐ 本条讲 **"改配置要小心注释"** ——
⚠️ 共性都是 **"改一处 ⇒ 影响面可能比想象的大"**。

⭐ **可迁移的检查**（⭐ 改配置文件后）：
```
⭐ 列出文件里所有"疑似配置值"的出现
   （料号 / 版本号 / 路径 / 端口号 …）
🔴 若它们【不一致】⇒ ⚠️ 解析器可能选错、或直接报错
```

⚠️ **另一条附带教训**：⭐ **`git stash` 会把整个文件还原** ——
⚠️ 我一度以为"测试 2 通过"是因为"只改了 pn"，🔴 其实是 `stash` 把我改的注释**一起还原了**。
⇒ ⭐ **二分时⛔ 别用 `stash`，⭐ 要用"从已知好的版本复制 + 只改一处"**。

---

## C75. ★★★★★🔴 **"验收工具误报"和"验收漏报"一样危险 —— 判据本身也要被判**（2026-09-24 实测）

**状态**：`[已确认]`

### 现象

```
🔴 全量回归报 4 个 tb `TIMEOUT`（tb_top_sound / tb_e2e / tb_calib_span / tb_uart_calib_chain）
⚠️ 但打开它们的日志 —— 末尾明明写着：
      PASS: top sound chain (T57) - HP_DIN is live ...
      $finish called at 42047900000 (1ps)
⇒ ⭐⭐ **仿真【成功结束】了，只是没在 120 秒内返回**
```

### 🎯 根因：⭐ **超时预算对【重 tb】太紧**

| ⭐ | ⭐ |
|---|---|
| `tb_top_sound` 单独跑 | ⭐ **93 秒**（⭐ 它要采 2048 个 I2S 样点 ⇒ 42 ms 仿真时间） |
| 脚本超时 | 🔴 **120 秒** |
| 全量回归时 | ⚠️ 机器同时在编译下一个 tb / 跑变异测试 / PnR ⇒ ⚠️ **必然超** |

⇒ 🔴 **于是"通过了"被报成"挂死了"**。

### 🔴🔴 我在这上面**连错三次**（⭐ 都写出来）

| # | ⚠️ 我的判断 | 🔴 实际 |
|---|---|---|
| **1** | ⭐ "是 `c6aa509` 引入的**组合环**" | ⚠️ **不完整** —— ⭐ 修完组合环后**又"挂"了** |
| **2** | ⭐ "组合环是根因，已修好" | 🔴 **错** —— ⭐ 同样的代码 13:19 过、13:34"挂" |
| **3** | ⚠️ "产物损坏" | ⚠️ **不是** —— ⭐ 清产物后照样过/挂交替 |

⭐⭐ **真因靠【读日志末尾】才发现**：⭐ **有 `PASS:` 就不是挂死**。

### ⭐⭐ 关键教训：⭐ **"挂了却算通过"和"过了却算挂"是同一类病**

| ⭐ | ⭐ 方向 | ⭐ 后果 |
|---|---|---|
| **`K-94`** | ⚠️ **漏报** —— 挂死却判 PASS | 🔴 **坏代码进仓库** |
| ⭐ **本条** | 🔴 **误报** —— 通过却判 FAIL | ⚠️ **好代码被当成坏的**；<br>🔴 **更坏的是：人会开始"习惯性忽略"这个失败**<br>（⭐ "反正它老报 TIMEOUT"）⇒ ⭐ **等于把守卫关掉了** |

> ⭐⭐⭐ **一条判据如果会【定期误报】，它的实际价值会被降到 0** ——
> ⚠️ 因为**大家会学会绕过它**。

### ✅ 修法（⭐ 已落地）

```powershell
# sim/tb_all.ps1 —— 按 tb 分类给预算
$heavyTb = @('tb_top_sound', 'tb_e2e_key_to_sound', 'tb_calib_span',
             'tb_uart_calib_chain', 'tb_latency_m2', 'tb_note_manager',
             'tb_i2s_tx_prbs', 'tb_wavetable_bank_prom')
$tbTimeoutSec = if ($heavyTb -contains $name) { 300 } else { $TimeoutSec }
```

⚠️ **同时给超时消息加"自证"**：⭐ **若日志里已经出现 `PASS:` ⇒ 明确提示"很可能是预算太紧，不是真挂死"**
⇒ ⭐ **让下次的判断不用再靠人翻日志**。

### ⭐⭐ 通用化

> ⭐ **判断"是不是挂死"，⛔ 不要只看"超时了没有"** ——
> ⭐ **要看【被守护的东西有没有给出结论】**：
> ```
> ⭐ 日志里有 PASS: 或 FAIL: ⇒ 仿真【已经跑完】⇒ ⚠️ 超时是【工具的问题】
> 🔴 日志里什么都没有（0 字节 / 只有超时行）⇒ ⭐ 才是【真挂死】
> ```

⭐ **与 `C70`（证据 PASS ≠ 证明最后一次改动）的关系**：
⭐ `C70` 讲 **"证据的时效性"**，⭐ 本条讲 **"判断的超时预算"** ——
⚠️ 共性都是 **"中间环节（时效/预算）没被验证，却用它下结论"**。

⭐ **与 `C72`（无 BOM 静默丢元素）的关系**：
⭐ **两次都是【验收工具自己坏了】** ⇒ ⭐ **而且两次都是靠"机械守卫"抓到的**：
```
⭐ C72：靠"加诊断打印，看脚本实际读到几个"
⭐ C75：靠"读日志末尾，看有没有 PASS:"
```
⇒ ⭐⭐ **可迁移的做法**：⭐ **让工具【自证】它读到了什么**，⛔ 不要相信"它说失败了所以就是失败"**。

---

## C76. ★★★★★🔴 **"读全文再写回"会静默重写【整个文件的行尾】—— 一次编辑变成 1896 行改动**（2026-09-24 实测，⭐ **复发**）

**状态**：`[已确认]`

### 现象（⭐ 触目惊心）

```
🔴 我为了在 rtl/top.v 里插入 2 行 wire 声明，写了：
      $raw = ReadAllText(f); $lines = $raw -split "`n"
      ... 插入 2 行 ...
      WriteAllText(f, ($out -join "`n"))
⇒ ⭐ git diff --numstat 报：🔴 **1896 insertions / 1851 deletions**
   ⚠️ 而我实际只改了 **2 行**！
```

### 🎯 根因

| ⭐ | ⭐ |
|---|---|
| ⭐ **git 里**（`HEAD:rtl/top.v`） | ⭐ **纯 LF**（1939 个 LF、⭐ 0 个 CRLF） |
| ⭐ **工作区**（`core.autocrlf` 转换后） | ⭐ **纯 CRLF**（1852 个 CRLF、⭐ 0 个 LF） |
| 🔴 我的代码 | ⭐ `-split "`n"` ⇒ ⚠️ **每行末尾留下 `\r`**<br>⭐ `-join "`n"` ⇒ ⚠️ **重新拼时用 LF** |

⇒ ⭐⭐ **结果：整个文件的行尾从 CRLF 变成 LF** ⇒ 🔴 **每一行都被 git 视为改动**。

### ⚠️ 为什么特别危险

```
⭐ 危害【不是】"diff 难看" ——
🔴 而是：⭐ 这么改完，⭐ **就再也看不出【我真正改了什么】了**
   ⇒ ⚠️ code review 失效
   ⇒ 🔴 **而且如果混进了别的意外改动，⭐ 根本发现不了**
```

### ✅ 正确做法（⭐ 三条，⭐ 优先级从高到低）

1. ⭐⭐⭐ **用 `edit` 工具**（⭐ 最稳）——
   ⭐ 它按**字面替换**，⭐ 不碰其他任何字节（⭐ 包括行尾）。
2. ⭐⭐ **真要脚本改**：⭐ **先探行尾，⭐ 用同一种拼回去**
   ```powershell
   $raw = [IO.File]::ReadAllText($f)
   $nl  = if ($raw -match "`r`n") { "`r`n" } else { "`n" }
   # ⚠️ 且【不能】用 -split "`n"（⭐ 会留下 \r）——
   #    ⭐ 要用 $raw -split "`r`n" 或先统一
   ```
3. ⚠️ **改完立刻核对**（⭐ 机械判据）：
   ```powershell
   git diff --numstat <file>     # 🔴 增删数与"你以为改了几行"对不上 ⇒ 就是踩了
   ```

### ⭐⭐ 通用化

> ⭐ **凡是"读整个文件 → 改 → 写回"的操作，都必须【显式处理行尾】。**
> ⚠️ 这在**跨平台**仓库（⭐ Windows 工作区 + LF 仓库）里**必然踩**，
> ⭐ 因为**工具链默认会自动转换**，⚠️ 而**我的脚本不会**。

### 📌 与既有规则的关系

⭐ **这条【已经在项目 `AGENTS.md` 里写过】**（⭐ "编辑本仓库的 .md/源码：⛔ 不许用读文本+写文本整体重写"）
⇒ 🔴 **但我还是踩了** ⇒ ⭐⭐ **说明"规则在库里" ≠ "动手时想得起来"**。

⭐ **可迁移的做法**：⭐ **把"改完就 `git diff --numstat` 核对"变成【无条件的收尾动作】** ——
⭐ 不依赖"我记得这条规则"。

## C77. ★★★★★🔴 **"改了没效果"≠"这个改动无关" —— ⚠️ 必须先确认"该改动影响的代码路径【真的执行到了】"**（2026-09-24 实测，⭐ 同一晚踩了两次）

**故障模式**：修一个 bug 时改了 A 和 B 两处，**A 没修好 ⇒ 代码根本没走到 B**
⇒ 看到"改了 B 没效果" ⇒ 🔴 **据此判定"B 与故障无关"并把 B 回退**
⇒ ⭐ **实际上 B 是【必要条件】**，只是被 A 掩盖了。

**实测案例（ESP32 BLE 广播起不来，报 `0xD`）**

| 我改的 | 当时的现象 | 🔴 我的错误结论 |
|---|---|---|
| `MAX_ACT` 1→6 | 仍报 `0xD` | "无关" ⇒ 回退成 1 |
| `BLE_50` y→n | 仍报 `0xD` | "无关" ⇒ 回退成默认 |

⭐ **真相**：当时**另一个 bug 未修** ——
GAP 回调只 `case` 了 `ADV_DATA_SET_COMPLETE_EVT(=0)`，
而 raw API 回调的是 `ADV_DATA_RAW_SET_COMPLETE_EVT(=4)`
⇒ 🔴 `start_advertising()` **压根没被调用**
⇒ ⭐ **这两个开关【根本没机会生效】** ⇒ "改了没效果"是必然的。

⭐ **最终对照实验**（固定代码、只改配置）：
```
代码完全相同（git diff 为空）
  BLE_50=n + MAX_ACT=6  ⇒ ✅ advertising as "..." 
  BLE_50=y + MAX_ACT=6  ⇒ 🔴 0xD
  BLE_50=n + MAX_ACT=1  ⇒ 🔴 0xD
⇒ ⭐ 两项【都是必要条件】
```

**判据（可机械执行）**：
> 在判定"某改动无关"之前，**必须先证明"该改动所在路径被执行到了"**。
> ⭐ 手段：打日志 / 设断点 / 观察该路径的副作用（如体积变化、计数器变化）。
> ⛔ **"改了没效果"作为"无关"的证据，是不成立的** —— 它只说明"这条路径可能没走到"。

**同族**：参见 `C75`（判据本身也要被判）——⭐ 两者都是"**结论缺少可证伪的证据链**"。

---

## C78. ★★★★★🔴 **改 sdkconfig / CMake 配置时【必须连 `build/` 一起删】—— ⚠️ 只删 `sdkconfig` 不够，缓存会粘住旧配置**（2026-09-24 实测）

**现象**：改了 `sdkconfig.defaults` 里的配置项、也删了 `sdkconfig`，
⚠️ 但**重新编译后配置【没有生效】**，行为与改之前完全一样。

**根因**：`build/` 目录里的 **CMake 缓存（`CMakeCache.txt` 等）粘住了旧的 `-D` 值与 Kconfig 结果**。
⇒ 🔴 **只删 `sdkconfig` 只是删掉了"结果"，没删掉"缓存的输入"。**

**实测证据**：
```
只删 sdkconfig         ⇒ 体积 0xB5A90（旧配置的产物）
连 build/ 一起删       ⇒ 体积 0xC1610（新配置正确生效）
```

**正确做法**：
```powershell
Remove-Item 'build'      -Recurse -Force
Remove-Item 'sdkconfig'  -Force
# 然后再 build
```

> ⭐ **本仓库的 `build.ps1` 注释里【早就写了这一点】**（`-Mode` 切换时会自动删 `build/`），
> 引用的是经验 `C61` / `T95` —— ⚠️ **我没有照做，白绕一轮。**

**同族**：`C61` / `T95`（CMake 缓存粘滞）。

**判据**：
> ⭐ **改配置后，用【产物体积 / 运行时日志】验证配置真的生效了**，
> ⛔ 不要只相信"我改了配置文件"。

---

## C79. ★★★★🔴 **⛔ 不要用"过滤后的输出"判断一条命令【是否执行完成】** —— 会把中间态当结果（2026-09-24 实测）

**故障模式**：
```powershell
.\build.ps1 -Action flash 2>&1 | Select-String -Pattern 'error|Hash|Done'
```
⭐ 过滤后**只剩几行**，看起来"跑完了" ⇒ ⚠️ **实际上 ninja 还在编译，或者烧录步骤刚失败。**
⇒ 🔴 **后续分析全部基于【上一次】烧进去的旧固件** ⇒ **结论全错。**

**实测后果（同一晚两次）**：
- 把**旧固件的日志**（`BLE_50:1`）当成**新配置**（`BLE_50=n`）的结果
- 据此得出"配置改了没用"的错误结论，来回改了 4 次

⭐ **而且更隐蔽的次生伤害**：
中途读输出会**打断/截断**长任务的输出管道 ⇒ 🔴
后来出现 `ninja: warning: premature end of file; recovering`
⇒ **`build/` 被损坏，必须删掉重建。**

**正确做法（三选一）**：
| # | 做法 |
|---|---|
| **1** ⭐ | **输出重定向到日志文件**（`*>&1 \| Tee-Object -FilePath x.log`），跑完再读 |
| **2** | 用 `run_in_background` 挂后台，**等完成通知**再读 `job_output` |
| **3** | 读输出时 **⛔ 不要过滤**，看完整的尾部（`Select-Object -Last N`） |

**判据（机械）**：
> ⭐ **判断"执行完成"要看【工具自己的完成标志】**，例如
> `Project build complete` / `Hash of data verified` / 非零退出码，
> ⛔ **而不是看"我过滤出来的那几行"**。

**同族**：`C75`（判据本身也要被判）· `U63`（长任务结果要独立落盘）。

---
